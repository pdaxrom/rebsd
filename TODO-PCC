# ReBSD PCC Port TODO

Status: active
Scope: maintain the imported Portable C Compiler as a supported opt-in
ReBSD/MIPS C userland compiler while keeping the default GCC-based kernel and
userland build unchanged.  TLS/shared libraries and `p++`/C++ are future work,
not active items for the current C/static-a.out PCC milestone.

## Ground Rules

- Kernel builds stay on the existing GCC flow for now.
- Userland builds default to GCC.
- `MIPS_ROOTFS_COMPILER=pcc` and `N64_USERLAND_COMPILER=pcc` are supported
  opt-in C userland modes after the Malta, Malta64/R4000, and real N64 gates.
- ReBSD must keep using its existing `as`, `ld`, `ar`, and `ranlib`; no GNU
  binutils or LLVM dependency is allowed for the target toolchain.
- First working target is big-endian MIPS o32 for Malta and N64/VR4300-class
  hardware.
- PCC may expose ReBSD macros, but it must keep RetroBSD compatibility macros
  while the tree and ports still expect them.

## Completed Gate

- [x] Rebrand committed as ReBSD with RetroBSD origin documented.
- [x] GCC-based Malta build completed.
- [x] Malta QEMU smoke tests completed.
- [x] GCC-based N64 build completed.
- [x] N64 image confirmed on hardware.
- [x] PCC userland selector is supported as an opt-in mode.
- [x] PCC-built MIPS kernel control gates completed.
  - The MIPS backend now emits `.weak` for external
    `__attribute__((weak))` declarations, so optional board hooks such as
    `mips_board_intr` and `mips_board_microtime` stay weak undefined symbols
    instead of becoming hard link dependencies.
  - The shift/move/ALU-before-load reorder was initially VR4300-only.  Commit
    `11f66467` enables the same dependency-checked four-instruction window for
    MIPS32R2 after cross/native and six-profile validation.  VR4300 output is
    byte-identical; MIPS32R2 uses the independent operation to hide interlocked
    load latency before the first dependent use.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed with `PCC_SMOKE_ALL_RC:0`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
- [x] VR4300 plain jump delay slots can use strict simple stores.
  - The MIPS backend now allows the plain `j`/`jal` delay-slot postpass to move
    only simple numeric `imm16(base)` stores on `-march=vr4300`: `sb`, `sh`,
    `sw`, `sd`, `s.s`, `s.d`, `swc1`, and `sdc1`.
  - Symbolic, macro-like, and otherwise non-plain store operands are rejected by
    the parser; the earlier broad store-delay rule was discarded after it
    exposed a Malta64 hard-float boot failure ending in `panic: no procs`.
  - MIPS32R2 broad store-delay filling stayed disabled in this VR4300 change. A
    native Malta `as` run rejected a PCC smoke `types-cc.s` store in a jump
    delay slot at line 634 while the host cross assembler accepted the same
    file, so the follow-up MIPS32R2 rule below deliberately uses a stricter
    memory-operand parser.
  - Current VR4300 Linpack assembly baseline:
    GCC `1138` instructions / `42` nops, PCC `2508` instructions / `180` nops,
    PCC/GCC instruction ratio `2.204`, nop ratio `4.286`, branch/jump delay
    nops `130`, load-followed-by-nop `0`, and bytes `80242`.
  - Current MIPS32R2 control baseline after the return-delay fill:
    GCC `1082` instructions / `5` nops, PCC `2704` instructions / `123` nops,
    PCC/GCC instruction ratio `2.499`, nop ratio `24.600`, branch/jump delay
    nops `123`, load-followed-by-nop `0`, and bytes `81677`.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again on Malta64, Malta, and
    Malta little-endian, both hard-float rootfs and `MIPS_ROOTFS_FLOAT=soft`.
    PCC kernel builds included the existing `-fomit-frame-pointer` path and the
    kernel COP1 guard.
- [x] MIPS32R2 plain jump/call delay slots can use strict simple memory ops.
  - The late MIPS assembly peephole now allows `-march=mips32r2` plain `j` and
    `jal` delay slots to use simple stores and base-offset loads in addition to
    the existing register moves and cheap GPR ALU operations.
  - Symbol-only and macro-like memory operands are still rejected, matching the
    native assembler policy seen during the Malta smoke debug pass. VR4300
    remains on the earlier stores-only rule for this path.
  - Plain calls still reject candidates touching `$ra`, and indirect
    `jal $reg` calls now also reject candidates touching the call target
    register. This avoids unsafe slots such as loading `$25`/`$t9` in the delay
    slot of `jal $25`.
  - MIPS32R2 Linpack assembly baseline improved from PCC `2704` instructions /
    `123` nops to `2695` instructions / `114` nops. GCC remained `1082`
    instructions / `5` nops; the PCC/GCC instruction ratio moved from `2.499`
    to `2.491`, and branch/jump delay nops moved from `123` to `114`.
  - Final static checks passed: `git diff --check`, no
    `macro instruction expanded into multiple instructions in a branch delay
    slot` diagnostics in the six QEMU smoke logs, and no generated
    `jal $reg` delay-slot candidate touching `$25`/`$t9` in Malta/MaltaEL
    kernel assembly.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with `-msoft-float` and
    `-fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Linpack QEMU results for the final matrix were approximately:
    Malta64 hard `13759/13549`, Malta64 soft `805/801`, Malta hard
    `13263/13563`, Malta soft `1024/1041`, MaltaEL hard `13609/13509`, and
    MaltaEL soft `1047/1041` KFLOPS.
  - Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
- [x] Safe integer conditional branch delay slots can use simple GPR work.
  - The final MIPS assembly peephole now recognizes integer conditional
    branches (`beq`, `bne`, `beqz`, `bnez`, `bgez`, `bgtz`, `blez`, and
    `bltz`) and can move a preceding simple GPR operation into the delay slot
    when it does not write a register read by the branch.
  - The rule deliberately refuses `$ra`, refuses `$sp` except for exact
    `addiu $sp,$sp,imm` stack adjustment, and refuses the first instruction
    after a label so branch targets keep their original first instruction.
  - The existing VR4300 FP multiply erratum path is unchanged: the final
    `-mfix4300` / `-mno-fix4300` repair still prevents unsafe FP multiply
    adjacency and the branch-delay rule does not schedule FP multiplies.
  - Malta64/VR4300 Linpack assembly counters improved from PCC `2508`
    instructions / `180` nops / `130` branch-jump-call delay nops to `2507`
    instructions / `179` nops / `129` branch-jump-call delay nops.  MIPS32R2
    userland size counters also moved downward on the same conservative rule.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with `-msoft-float` and
    `-fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Linpack QEMU results for the final matrix were approximately:
    Malta64 hard `13387/13760`, Malta64 soft `896/899`, Malta hard
    `13118/13349`, Malta soft `1003/1001`, MaltaEL hard `13723/13574`, and
    MaltaEL soft `1023/1013` KFLOPS.
  - Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
- [x] Safe integer conditional branch delay slots can also use strict FP stores.
  - The final MIPS assembly peephole now allows a preceding simple FP store in
    an integer conditional branch delay slot when the store is accepted by the
    existing jump-delay store parser.  This covers strict numeric
    `imm16(base)` forms for `s.s`, `s.d`, `swc1`, and `sdc1`, including
    common Linpack FP spill stores before loop branches.
  - General integer stores and symbolic stores such as `sw $v0,mempool`
    deliberately remain untouched for conditional branch delay slots.  A
    broader integer-store rule exposed an unsafe `malta64` soft-float
    `ccom-stress` path in native `cpp`, so the committed rule keeps the
    measured hard-float FP win without taking that risk.
  - The rule does not allow loads into VR4300 branch delay slots and does not
    schedule FP multiply instructions, so the `-mfix4300` / `-mno-fix4300`
    erratum workaround path remains unchanged.
  - Malta64/VR4300 Linpack assembly counters improved from the previous PCC
    `2507` instructions / `179` nops / `129` branch-jump-call delay nops to
    `2503` instructions / `175` nops / `125` branch-jump-call delay nops.
    Malta/MIPS32R2 improved from `2696` instructions / `115` nops / `115`
    branch-jump-call delay nops to `2692` instructions / `111` nops / `111`
    branch-jump-call delay nops.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with `-msoft-float` and
    `-fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Linpack QEMU results for the final matrix were approximately:
    Malta64 hard `13025/13214`, Malta64 soft `848/860`, Malta hard
    `12517/12406`, Malta soft `978/970`, MaltaEL hard `12657/12683`, and
    MaltaEL soft `991/975` KFLOPS.
  - Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
- [x] VR4300 `jr $ra` epilogues can use the strict frame-load delay slot.
  - The final MIPS assembly peephole now lets selected CPU `VR4300` use the
    existing exact `lw $fp,imm16($sp)` frame-restore rule in a `jr $ra` delay
    slot.  The helper still accepts only `lw` to `$fp` from `$sp` with a
    numeric base-offset operand; it does not enable arbitrary loads or stores
    in return delay slots.
  - This relies on the already adopted VR4300 integer load-use interlock
    policy.  No FP multiply scheduling changed, and the `-mfix4300` /
    `-mno-fix4300` erratum workaround path remains unchanged.
  - Malta64/VR4300 Linpack assembly counters improved from PCC `2503`
    instructions / `175` nops / `125` branch-jump-call delay nops to `2490`
    instructions / `162` nops / `112` branch-jump-call delay nops.
    Malta/MIPS32R2 stayed at `2692` instructions / `111` nops / `111`
    branch-jump-call delay nops.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with `-msoft-float` and
    `-fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Linpack QEMU results for the final matrix were approximately:
    Malta64 hard `13124/12897`, Malta64 soft `794/797`, Malta hard
    `12444/12668`, Malta soft `970/974`, MaltaEL hard `12930/13042`, and
    MaltaEL soft `985/938` KFLOPS.
  - Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
- [x] VR4300 cheap constant multiplies lower to shift/add/sub sequences.
  - The MIPS backend now has a `FEATURE_VR4300`-gated table rule for positive
    non-power-of-two constants whose odd factor is `2^n +/- 1`; this covers
    common small constants such as `3`, `5`, `6`, `7`, `9`, `15`, `17`, `24`,
    and `36` without using HI/LO multiply latency.
  - The lowering preserves the original operand in `$at` before writing the
    result register, because PCC's `XSL(A)` allocation may choose the same
    register for source and destination. This avoids clobbering o32 argument
    registers in calls such as `bdevsw[major(dev)].d_strategy(bp)`.
  - The rule is intentionally VR4300-only for now. MIPS32R2 still uses its
    existing multiply path until there is a measured MIPS32R2-specific win.
  - No floating-point multiply scheduling changed here; the VR4300 multiply
    erratum controls remain in the `-mfix4300` / `-mno-fix4300` path.
  - Linpack instruction counters are unchanged because the current Linpack hot
    multiply by `3125` is not one of the intentionally small shift/add forms:
    GCC `1138` instructions / `42` nops, PCC `2837` instructions / `509` nops.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with `-msoft-float` and
    `-fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
- [x] MIPS32R2 FPU compare branches no longer keep a redundant spacer `nop`.
  - The late assembly peephole now removes `nop` from
    `c.eq.s/d`/`c.lt.s/d`/`c.le.s/d`; `nop`; `bc1t`/`bc1f` only for
    `MIPS_CPU_MIPS32R2`, matching the interlocked MIPS32R2 FPU condition path.
  - VR4300 is deliberately excluded. The `-mfix4300`/`-mno-fix4300` multiply
    erratum controls and FP multiply delay-slot rules are unchanged.
  - The Malta MIPS32R2 Linpack baseline improved from PCC `2746` instructions /
    `165` nops to `2730` instructions / `149` nops. GCC remained `1082`
    instructions / `24` nops; the PCC/GCC instruction ratio moved from `2.538`
    to `2.523`.
  - The current omit-frame audit found that PCC already honors
    `-fomit-frame-pointer` through the MIPS late frame-reference rewrite. Future
    improvement should focus on reducing call/FUNARG cases that still force
    a real `$fp`, not on adding another omit-frame path.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with `-msoft-float` and
    `-fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
- [x] Omit-frame leaf functions skip redundant `$ra` saves.
  - The MIPS prologue now trusts pass2 leaf status independently of
    `-fomit-frame-pointer`.  The omit-frame epilogue skips the `$ra` reload
    for true leaf functions and keeps the final 16-byte ABI stack pop in the
    `jr $ra` delay slot.
  - Hidden helper calls that can still appear in pass2 leaf functions keep the
    existing leaf-only `$ra` spill/reload wrappers, so structure-copy,
    soft-float, integer emulation, and hard-float conversion helper calls do
    not clobber the caller return address.
  - A Malta64 kernel assembly audit after the soft-float gate found 1056
    generated kernel functions: 287 no-`jal` leaf-like functions had no
    `sw/lw $ra`, while the remaining 769 call-path functions still saved and
    restored `$ra`.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with
    `-msoft-float -fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Logs:
    `/private/tmp/rebsd-omitfp-leaf-malta64-hard.log`,
    `/private/tmp/rebsd-omitfp-leaf-malta64-soft.log`,
    `/private/tmp/rebsd-omitfp-leaf-malta-hard.log`,
    `/private/tmp/rebsd-omitfp-leaf-malta-soft.log`,
    `/private/tmp/rebsd-omitfp-leaf-maltael-hard.log`, and
    `/private/tmp/rebsd-omitfp-leaf-maltael-soft.log`.
- [x] Omit-frame save slots are allocated only for used callee-saved registers.
  - The MIPS frame-size calculation now uses pass2's `p2env.p_regs[0]` for
    omit-frame functions too, instead of reserving 32 bytes for all
    `$s0..$s7` registers whether they are used or not.
  - The fixed 16-byte o32 argument home area is unchanged.  Register argument
    spills in omit-frame leaf stubs now land at `16($sp)`, `20($sp)`, and so
    on after the standard 16-byte prologue allocation, instead of being pushed
    out to `48($sp)` by unused save slots.
  - A Malta64 kernel assembly audit after the hard-float gate found 289
    `$sp`-frame functions: 237 had no extra local/save allocation, 52 still
    had a real allocation, and none kept the old artificial 32-byte `$s`
    register reservation.  Simple stubs such as `nulldev` now consist of the
    16-byte ABI allocation, body, and `jr $ra` with the ABI pop in the delay
    slot.
  - Full PCC-kernel/PCC-rootfs QEMU gates passed again with
    `PCC_SMOKE_ALL_RC:0`, including PCC kernel builds with
    `-msoft-float -fomit-frame-pointer`:
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta64 MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/malta MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc pcc-smoke-all-runtime`
    `make -C sys/mips/maltael MIPS_KERNEL_COMPILER=pcc MIPS_ROOTFS_COMPILER=pcc MIPS_ROOTFS_FLOAT=soft pcc-smoke-all-runtime`
  - Logs:
    `/private/tmp/rebsd-used-sregs-malta64-hard.log`,
    `/private/tmp/rebsd-used-sregs-malta64-soft.log`,
    `/private/tmp/rebsd-used-sregs-malta-hard.log`,
    `/private/tmp/rebsd-used-sregs-malta-soft.log`,
    `/private/tmp/rebsd-used-sregs-maltael-hard.log`, and
    `/private/tmp/rebsd-used-sregs-maltael-soft.log`.
- [x] ReBSD `ld` accepts `-L` and `-l` library search options.
  - `ld` now collects all `-L` search directories before resolving any `-l`
    library, so `cc file.c -lm` works even when the compiler driver appends
    default library paths after user arguments.
- [x] ReBSD `ld` preserves 8-byte MIPS alignment across linked a.out inputs.
  - `TEXT_ALIGN`, `DATA_ALIGN`, and `BSS_ALIGN` are target-selectable so
    MIPS FPU literals and BSS `double` objects do not move to 4-byte-only
    addresses when multiple objects and libraries are linked together.
  - N64 native host tools define `TARGET_MIPS_STRICT_ALIGN64` through
    `sys/mips/n64/Makefile.kconf`; this avoids editing generated Makefiles.
  - This fixed Malta SIGSEGVs in `c99/basic008`, `c99/init007`,
    `jira/PCC-62`, and `jira/PCC-225`.
- [x] `libpcc/ssp.c` builds without local ReBSD hacks.
  - ReBSD headers expose `__progname` as the mutable pointer used by `crt0`.
  - ReBSD `as` accepts GCC constructor/destructor sections as data sections.
- [x] BSD make is included in the GCC-built ReBSD rootfs.
  - Built from the existing `src/cmd/make` sources.
  - Installed as `/usr/bin/make` with `/usr/share/man/cat1/make.0`.
  - Malta QEMU smoke ran `/usr/bin/make -f /tmp/mk` and printed
    `MAKE_SMOKE_OK`.

## Future Work, Not Current PCC Milestone

- [ ] TLS support.
  - The current ReBSD/MIPS PCC target is static a.out and deliberately rejects
    thread-local storage.
  - Keep TLS-only upstream tests as expected-fail policy cases until ReBSD has
    a concrete TLS ABI and runtime plan.
- [ ] Shared libraries/PIC support.
  - The current target has no `-shared`, runtime shared object loader, `rpath`,
    or shared-library ABI.
  - Keep shared-library/PIC upstream tests as expected-fail policy cases until
    ReBSD has a concrete shared-library ABI and loader plan.
- [ ] `p++`/C++ frontend and runtime.
  - Do not publish `/usr/bin/p++` or `/usr/libexec/pcc/cxxcom` in default
    rootfs images for now.
  - Revisit only as a separate C++ phase with frontend coverage, target
    compile/link/run gates, and a ctor/dtor startup policy.
- [ ] Optimize hot MIPS `libpcc.a` helpers in assembly.
  - The current canonical helper implementations are C, which keeps the
    correctness work auditable while Malta64 and N64 validation are still
    being hardened.
  - After the C helper set stays stable, add MIPS assembly implementations for
    selected hot 64-bit arithmetic, shift, and conversion helpers and keep the
    C versions as the reference oracle/fallback.
- [ ] Broaden MIPS32R2 jump-delay filling only behind direct assembler smokes.
  - The current safe MIPS32R2 rule covers register moves, cheap GPR ALU ops,
    simple stores, and base-offset loads. Future work can investigate
    symbol-address memory operands, macro-expanded forms, and conditional
    branch delay slots, but each extension needs a direct native `as` smoke
    before changing backend scheduling.
- [ ] Verify VR4300/o32 `ld`/`sd` instruction policy.
  - `ld` is a real MIPS III 64-bit integer load instruction on VR4300, not a
    generic pseudo-op.  Confirm where PCC emits or accepts `ld`/`sd` for o32
    VR4300/N64, whether each use is ABI-safe for 32-bit C objects and stack
    slots, and keep the delay-slot parser and assembler compatibility rules
    aligned with that decision.
  - Also verify whether any ReBSD assembler context still treats `ld`/`sd` as
    macro-like syntax; such cases must stay out of delay slots until proven
    single-instruction.
- [x] Fix PCC backend-to-board object dependency tracking.
  - Cross PCC builds now produce
    `$(MIPS_PCC_HOST_PREFIX)/.rebsd-pcc-toolchain.stamp`.  The stamp has normal
    prerequisites on all imported PCC frontend, middle-end, and backend
    sources, and records `cksum` results for the installed driver and `ccom`.
  - PCC kernel objects, the PCC rootfs/userland stamp, native runtime/native
    PCC, Linpack, and regression targets depend on this stamp.  GCC kernel and
    rootfs paths do not.
  - The 2026-07-10 no-clean regression touched `arch/mips/local2.c` after an
    initial Malta64/VR4300 soft-float PCC build.  The installed `ccom`, stamp,
    `subr_rmap.o`, libc `abs.o`, Linpack binary, and `misc__nestedcall001`
    regression binary all received newer timestamps.  The GCC-generated
    `malta64-stage0.o` timestamp and checksum did not change.  A second build
    without source changes reported Linpack ready and `unix.elf` up to date.
  - Logs and before/after records are
    `/private/tmp/pcc-phase1-{initial-build,rebuild,noop}.log` and
    `/private/tmp/pcc-phase1-{before,after}.txt`.
  - The post-change clean QEMU matrix passed `PCC_SMOKE_ALL_FAILURES 0` and
    `PCC_SMOKE_ALL_RC:0` for Malta64/VR4300 hard and soft, Malta/MIPS32R2
    big-endian hard and soft, and MaltaEL/MIPS32R2 little-endian hard and soft.
    Every PCC kernel command contained `-fomit-frame-pointer` and
    `-msoft-float`.  Linpack ranges were about `13662-13780`, `994-995`,
    `13545-13635`, `1008-1019`, `13649-13943`, and `1041-1046` KFLOPS,
    respectively.  Logs are `/private/tmp/pcc-phase1-<board>-<float>-smoke.log`.
- [x] Separate MIPS instruction legality from CPU tuning and costs.
  - `struct mips_target` now carries independent ISA, tuning, capability,
    cost, endian, float-ABI, and VR4300-fix state.  ISA profiles cover MIPS I,
    II, III, MIPS32r1, and MIPS32r2; tuning profiles cover generic, VR4300,
    R4000, 24Kc, 34Kc, 74Kc, and JZ4780.
  - Capability bits describe three-operand multiply, rotate, count-leading-zero,
    byte/halfword operations, bitfield operations, conditional moves, 64-bit
    GPRs, HI/LO, integer and FP load interlocks, branch delay slots, and the
    VR4300 FP-multiply erratum.  Existing codegen decisions now use the
    equivalent ISA/tune predicates, so this refactor does not intentionally
    change assembly.
  - `-march=vr4300` remains shorthand for MIPS III plus VR4300 tuning;
    `-mips3`, `-march=mips32r2`, and `-mips32r2` remain compatible.
    `-march=mips3` selects generic MIPS III tuning, and `-mtune=` accepts the
    tuning profiles above.  Unsupported MIPS32r1 (`-march=mips32`) is rejected
    instead of silently enabling r2 instructions.
  - `-mfix4300` is effective only when the selected ISA/tuning combination has
    the VR4300 erratum capability.  Compile-only host smokes verify r2 `mul`,
    VR4300 `mult/mflo`, endian and float macros, compatibility aliases,
    incompatible tuning diagnostics, and fix/no-fix assembly.
  - The initial cost table has generic MIPS III, exact VR4300, and generic
    MIPS32r2 entries.  VR4300 integer multiply/divide and FPU costs come from
    `docs/VR4300-Users-Manual.pdf`, tables 3-12 and 7-14.  Costs are not yet
    connected to instruction selection, preserving output in this phase.
  - Pre/post-refactor Linpack assembly compared byte-identical for VR4300 and
    MIPS32r2.  Cross compile regression passed `318/321` plus the three expected
    failures and produced `281` runtime candidates with no unexpected compile
    failures.
  - The clean six-profile QEMU matrix passed `PCC_SMOKE_ALL_FAILURES 0` and
    `PCC_SMOKE_ALL_RC:0`.  Linpack ranges were approximately `13589-13613`,
    `992-1005`, `13411-13550`, `1044-1072`, `13639-13794`, and `1040-1049`
    KFLOPS for Malta64 hard/soft, Malta hard/soft, and MaltaEL hard/soft.
    Logs are `/private/tmp/pcc-phase2-<board>-<float>-smoke.log`.
- [x] Add disabled-by-default optimizer and register allocator statistics.
  - `-fopt-stats` (or the internal PCC option `-Wc,-Zp`) writes one stable
    `PCC_OPTSTATS` key/value line per function and one compilation summary to
    standard error.  Normal builds do not print or collect a report.
  - The machine-independent `mip/optstats.c` records CFG blocks/edges and
    natural-loop depth, TEMP/live pressure, interference/coalescing work,
    actual TEMP spills/reloads/stores/stack bytes, register-class pressure,
    frame size, allocator caller/callee-saved use, and calls.  Interference and
    coalescing totals include every allocator retry; callee-save prologue slots
    are deliberately not reported as TEMP spills.
  - Host smoke compiles a nested-loop FP pressure probe with statistics off and
    on, verifies byte-identical assembly, checks every required key, and
    compares two complete reports byte-for-byte.  Linpack also produced
    byte-identical assembly.  Its MIPS32r2 hard-float summary was `13`
    functions, `296` blocks, `392` CFG edges, `440` TEMPs, maximum live/GPR/FPR
    pressure `14/14/2`, `36983` interference edges, and `1108` coalescing
    attempts.  Logs are `/private/tmp/phase3-linpack-stats-{a,b}.log`.
  - Cross compile regression passed `318/321` with only the three documented
    expected failures and `281` runtime candidates.  Cross runtime passed
    `281/281`.  Native PCC passed `291` compilations plus `30` expected
    compile failures and `281/281` runtimes; both unexpected counts were zero
    and `NATIVE_PCC_REGRESS_RC:0`.
  - The six PCC-kernel/PCC-rootfs QEMU profiles all reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `13875/13582`, Malta64 soft `1007/1016`, Malta hard
    `13390/13439`, Malta soft `1071/1064`, MaltaEL hard `13689/13683`, and
    MaltaEL soft `1049/1030` KFLOPS.  Kernel commands retained
    `-fomit-frame-pointer` and the kernel soft-float policy.  Logs are
    `/private/tmp/pcc-phase3-<board>-<float>-smoke.log`.
  - Explicit aggregate logs are
    `/private/tmp/pcc-phase3-{regress-compile,regress-runtime}.log`,
    `/private/tmp/pcc-phase3-native-regress-runtime.log`, and
    `/private/tmp/pcc-phase3-linpack-runtime.log`.  QEMU cannot validate the
    physical VR4300 erratum; no codegen changed in this statistics phase, and
    the existing N64 hardware images were left byte-for-byte untouched.
- [x] Replace first-fit allocator spills with measured spill costs.
  - `mip/regs.c` now records weighted uses and definitions, capped loop depth
    weights `1/4/16/64`, live-range length, exact call crossing, interference
    degree, move relation, GPR/FPR class, and safe rematerialization state.
    Named target-independent weights select the lowest rewrite cost divided by
    interference degree; calls already exclude caller-saved colors from live
    values, leaving profitable callee-saved colors to the existing allocator.
  - Safe rematerialization is restricted to integer/symbol constants and
    simple `$fp` plus/minus constant addresses.  Volatile or arbitrary loads,
    calls, trapping expressions, and side effects are never candidates.  The
    extra metadata is a separate array allocated only during graph coloring,
    so the permanent `REGW` representation did not grow.
  - Added `spillcost001`, `spillfp001`, `spillabi001`, and `spillremat001` for
    nested GPR/FPR pressure, call-crossing and move-related values, constants
    and symbol addresses, struct copy/return, varargs, and hard/soft FP ABI.
    Cross compile regression passed `322/325` with only the three documented
    failures and cross runtime passed `285/285`.  Native PCC passed `295`
    compilations plus `30` expected failures and `285/285` runtimes, with zero
    unexpected failures and `NATIVE_PCC_REGRESS_RC:0`.
  - On the deterministic MIPS32r2 hard-float GPR pressure case,
    `scalar_pressure` retained `11` selected spills and `22` spill stores but
    reloads fell from `54` to `33`.  Whole-file instructions fell `468 -> 448`,
    loads `84 -> 63`, and assembly bytes `12565 -> 11985`; stores and nops
    stayed `45` and `9`.  Focused FPR and ABI assembly remained byte-for-byte
    unchanged.  `extendsfdf2` exercises actual rematerialization (`2` values)
    and compiles without the previous spill rewrite.
  - All six PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `13614/13666`, Malta64 soft `975/975`, Malta hard
    `13565/13580`, Malta soft `973/969`, MaltaEL hard `13631/13800`, and
    MaltaEL soft `929/949` KFLOPS.  The separate Malta aggregate reported GCC
    `31662/31777`, PCC `13807/13822`, and `LINPACK_RC:0`; Phase 4 therefore
    improves a spill-heavy case but does not close the broader GCC gap.
  - `make -C sys/mips/n64 pcc-debug-image` produced the hardware ROM
    `builds/20260710-phase4-spill-cost/pcc-debug.z64`, SHA-256
    `bbd782c7f581c1e8af05df886e92c36f497792c5b1105fc8c9efca9c076a6457`.
    The tested allocator source is commit `c20bf9e6`.  Real N64 hardware ended
    the debug runner with `N64_PCC_DEBUG_RUNNER_RC 0` followed by
    `N64_PCC_DEBUG_RC_END`, validating this hard-float a.out image and its
    default `-mfix4300` path.  A distinct `-mno-fix4300` hardware run was not
    performed, and QEMU cannot reproduce the physical multiply erratum.
    The four earlier `builds/20260710-hard-float` sets were hash-checked and
    remain byte-for-byte unchanged.  Logs are `/private/tmp/pcc-phase4-*`.
- [x] Add pass2 CFG, dominator, and phi consistency verification (Phase 5A).
  - New machine-independent `mip/cfgverify.c` verifies contiguous basic-block
    coverage, label maps, exact branch/fallthrough/computed-goto successors,
    reciprocal predecessor/successor multiplicity, and rebuilds after CFG
    invalidation.  It also computes an independent iterative dominator solution
    and checks DFS parents, immediate dominators, the dominator tree, phi arity,
    duplicate phi destinations, and pre/post-rename TEMP ranges.
  - The normal `xtemps` path always runs the allocation-free CFG checks.  PCC's
    imported driver already passes `-xssa` for optimized compilation, so the
    more expensive dominator/phi checks cover the normal `-O` path as well as
    explicit `-Wc,-xssa` tests.  Phase 5A did not newly enable SSA, add an IR,
    or add MIPS-specific code.  The verifier is included in C, C++, F77,
    split-pass, host-cross, and native PCC builds; host smoke also exercises a
    nested-loop SSA probe explicitly.
  - A compiler built from `ce61008c` and commit `3f6f628e` produced
    byte-identical MIPS32r2 hard-float assembly for `optim003`, the three Phase
    4 pressure/ABI cases, `extendsfdf2`, and Linpack.  VR4300 Linpack was also
    identical (`79974` bytes, SHA-256 `ab7f5876e2d2fb599cc1674d8a1298c44972afc2d61b37b6fb3f21935a869b86`).
  - Cross regression passed `322/325` with only the three documented failures
    and runtime `285/285`.  Native PCC passed `295` compilations plus `30`
    expected failures and runtime `285/285`; both unexpected counts were zero
    and `NATIVE_PCC_REGRESS_RC:0`.  Stripped host `ccom` grew from `526104` to
    `542712` bytes (`16608`, about 3.2%); normal verification allocates no new
    per-function bitsets.
  - All six PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Full-smoke Linpack
    pairs were Malta64 hard `11571/11473`, Malta64 soft `924/908`, Malta hard
    `13413/13568`, Malta soft `883/895`, MaltaEL hard `13378/13679`, and
    MaltaEL soft `911/908` KFLOPS.  Three isolated reruns of the identical
    Malta64 hard image ranged `12880-13230` KFLOPS, confirming that the lower
    post-stress sample was QEMU/host load rather than codegen.
  - The build-only N64 hard-float a.out ROM is
    `builds/20260710-phase5a-cfg-verifier/pcc-debug.z64`, size `6619136`,
    SHA-256 `3ce8f732949fb0d885b6e1bc55c95663aafeff45cfd55f338dd240a4b17ce806`.
    Real N64 hardware reported `N64_PCC_DEBUG_END 0`,
    `N64_PCC_DEBUG_RUNNER_RC 0`, and `N64_PCC_DEBUG_RC_END`, validating this
    native-compiler image and its default `-mfix4300` path.  Soft-float and a
    distinct `-mno-fix4300` image were not tested.  Earlier N64 build
    directories were hash-checked and remain unchanged.  Logs are
    `/private/tmp/pcc-phase5a-*`.
- [x] Split SSA critical edges and lower phi nodes as parallel copies (Phase
  5B, implementation commit `e1f21c62`).
  - New machine-independent `mip/ssalower.c` splits critical `CBRANCH` edges,
    verifies that none remain, and emits each phi edge as one parallel-copy
    set.  A destination is written only after its old value is no longer a
    live source; true cycles use one typed TEMP.  This replaces the old
    sequential `removephi()` implementation, which could overwrite a source
    required by a later copy.
  - Fallthrough pads stay immediately after their source.  Taken-edge pads are
    placed immediately before their destination after making any physical
    predecessor jump over them.  The PCC-kernel Malta64 gate caught an earlier
    source-adjacent taken-pad layout that changed live-value placement in
    `hardclock()`; the final layout restores byte-identical code for the
    focused reproducer and boots the complete PCC kernel.
  - A critical computed-goto edge cannot be retargeted in the current pass2
    IR.  Such a function now disables SSA locally and continues through the
    established non-SSA optimizer; later functions still use SSA.  Optimized
    `gcccompat/extension004` permanently covers this fallback.  New
    `misc/ssaphi001` covers cyclic integer and hard/soft-float copies.
  - Cross regression passed `324/327`, with only the three documented expected
    failures, and runtime `287/287`.  Native PCC passed `297` compilations plus
    `30` expected compile failures and runtime `287/287`; both unexpected
    counts were zero and `NATIVE_PCC_REGRESS_RC:0`.  The final `ssalower.o`
    also builds in the C++, F77, split-pass, host-cross, and native build
    layouts.  Unoptimized `optim003` assembly remains byte-identical to Phase
    5A, SHA-256 `bff0d1f27f8ab0b61e07de14db8313979e788d03be521282055fb327f6fb08ef`.
  - All six PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `11520/10754`, Malta64 soft `872/872`, Malta hard
    `13149/13189`, Malta soft `999/1000`, MaltaEL hard `13004/12964`, and
    MaltaEL soft `999/1004` KFLOPS.  These are post-stress QEMU samples, not
    real N64 measurements.
  - Safe parallel copies leave a small cleanup opportunity for Phase 5C.
    VR4300 Linpack remains `2477` instructions, `158` nops, `643/244`
    loads/stores; MIPS32r2 changes from `2533` to `2551` instructions and from
    `765/286` to `772/293` loads/stores.  Stripped host `ccom` changes only
    `542712 -> 542792` bytes, so Phase 5B replaces the unsafe lowering without
    materially enlarging the compiler.  TEMP copy propagation and DCE must
    recover avoidable copies before broader scalar optimization.
  - `make -C sys/mips/n64 pcc-debug-image` produced the build-only hard-float
    a.out ROM `builds/20260710-phase5b-ssa-lowering/pcc-debug.z64`, size
    `6619136`, SHA-256
    `c4368d550fece2b21a9ba0481e4d088e2d6bfa058d1e1b9bbf5db6a4446dd7c5`.
    It uses the default `-mfix4300` path.  Real N64 hardware and a distinct
    `-mno-fix4300` image have not been run; QEMU cannot reproduce the physical
    multiply erratum.  The Phase 4 and Phase 5A ROM hashes remain unchanged.
- [x] Propagate SSA TEMP copies and eliminate trivial phi copies (Phase 5C,
  implementation commit `1fd10752`).
  - Before phi lowering, a pure `TEMP = TEMP` assignment is propagated only
    when source/destination types match and the destination has exactly one
    tree definition.  Uses in trees and phi inputs are renamed together, then
    the dead copy is removed.  No memory expression, volatile access, call,
    inline asm, or potentially trapping operation is substituted.
  - Phi nodes are collapsed only when every defined non-self input names the
    same TEMP.  A phi with an undefined (`0`) input is deliberately retained,
    because the candidate source need not dominate that path.  Iteration lets
    copy propagation expose additional trivial phis without adding a general
    SCCP implementation.
  - Removed copy nodes use a private sentinel until phi edge insertion is
    complete.  Cleanup compares the sentinel address, so user `asm("")` and
    `asm volatile("" ::: "memory")` barriers remain in the IR.  The permanent
    `misc/ssacopy001` test covers integer/double branch copy chains; the
    existing `ssaphi001` continues to cover cyclic parallel copies.
  - Cross regression passed `325/328`, with only the three documented expected
    failures, and runtime `288/288`.  Native PCC passed `298` compilations plus
    `30` expected compile failures and runtime `288/288`; unexpected counts
    were zero and `NATIVE_PCC_REGRESS_RC:0`.  Final C++, F77, split-pass,
    host-cross, and native pass2 objects build successfully.  Unoptimized
    `optim003` remains byte-identical to Phase 5A/5B, SHA-256
    `bff0d1f27f8ab0b61e07de14db8313979e788d03be521282055fb327f6fb08ef`.
  - All six final PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `11753/11175`, Malta64 soft `945/944`, Malta hard
    `12766/12991`, Malta soft `1044/1034`, MaltaEL hard `13162/13035`, and
    MaltaEL soft `1060/1064` KFLOPS.  Logs are
    `/private/tmp/pcc-phase5c-sentinel-*.log`.
  - Relative to Phase 5B, VR4300 Linpack changes from `2477/158` to
    `2475/157` instructions/nops and `79967 -> 79931` assembly bytes;
    MIPS32r2 changes from `2551/113` to `2549/112` and
    `79329 -> 79293`.  Loads/stores remain `643/244` on VR4300 and `772/293`
    on MIPS32r2.  Stripped host `ccom` changes only `542792 -> 542872` bytes.
    Phase 5C removes proven aliases but intentionally leaves nontrivial phi
    coalescing and the remaining Phase 5B overhead for later allocator work.
  - The build-only N64 hard-float a.out ROM is
    `builds/20260710-phase5c-ssa-copyprop/pcc-debug.z64`, size `6619136`,
    SHA-256
    `ceff865c1a04b08afea35384ae2fe41abfb59036959a8017d300170eb910e955`.
    It uses the default `-mfix4300` path.  Real N64 hardware and a distinct
    `-mno-fix4300` image have not been run; QEMU cannot reproduce the physical
    multiply erratum.  Phase 4, Phase 5A, and Phase 5B ROM hashes remain
    unchanged.
- [x] Propagate direct integer constants through SSA and equal-value phis
  (Phase 5D1, implementation commit `922085b9`).
  - The lattice recognizes only unnamed integral `ICON` assignments with
    identical source/destination types.  A phi becomes constant only when all
    defined non-self inputs have the same known value and type.  Undefined
    inputs, mixed values, pointers/symbols, FP constants, expressions, and
    branches remain untouched for later Phase 5D substeps.
  - Substitution is all-or-nothing for each TEMP.  A libc `flsbuf.c` rebuild
    exposed PCC IR where one SSA TEMP appears with a different `n_type`; such
    TEMPs are now retained rather than guessing conversion semantics.  Any
    constant TEMP referenced anywhere inside `XASM` is also retained.  The
    Malta64 kernel gate caught the missing XASM rule when `mtc0 %0,$6` was
    incorrectly emitted as `mtc0 0,$6`; focused `machdep.o` and all final
    kernel builds preserve register operands.
  - Constant phi copies are skipped only for terminal phis with no use by
    another phi.  This conservative rule preserves transitive definitions for
    downstream phis that cannot themselves be removed.  New
    `misc/ssaconst001` covers equal branch constants and a self-referential
    loop constant; `pcclist/asm002` remains a permanent native XASM gate.
  - Cross regression passed `326/329`, with only the three documented expected
    failures, and runtime `289/289`.  Native PCC passed `299` compilations plus
    `30` expected compile failures and runtime `289/289`; unexpected counts
    were zero and `NATIVE_PCC_REGRESS_RC:0`.  C++, F77, split-pass,
    host-cross, and native pass2 objects build successfully.  Unoptimized
    assembly remains byte-identical, SHA-256
    `bff0d1f27f8ab0b61e07de14db8313979e788d03be521282055fb327f6fb08ef`.
  - All six PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `10877/11403`, Malta64 soft `941/952`, Malta hard
    `12949/12291`, Malta soft `1021/1021`, MaltaEL hard `13280/13360`, and
    MaltaEL soft `1058/1047` KFLOPS.  Logs are
    `/private/tmp/pcc-phase5d1-xasm-*.log`.
  - Linpack counts remain Phase 5C-identical: VR4300 `2475/157`
    instructions/nops with `643/244` loads/stores; MIPS32r2 `2549/112` with
    `772/293`.  One constant in `idamax` is rematerialized in `$v0` after a
    call instead of remaining live in `$s0`, so assembly hash changes without
    changing counts.  Stripped host `ccom` changes only `542872 -> 542920`
    bytes.  Branch folding remains a separate Phase 5D2 change.
  - The build-only N64 hard-float a.out ROM is
    `builds/20260710-phase5d1-sccp-lite/pcc-debug.z64`, size `6619136`,
    SHA-256
    `9e3624f40ab31018465ce354d59221c102be71f13806d4fe6d65b038ae7d7f86`.
    It uses the default `-mfix4300` path.  Real N64 hardware and a distinct
    `-mno-fix4300` image have not been run; QEMU cannot reproduce the physical
    multiply erratum.  Earlier preserved ROM hashes remain unchanged.
- [x] Fold integer branches made constant by the SSA lattice (Phase 5D2,
  implementation commit `07a126ba`).
  - The pass accepts only a `CBRANCH` whose comparison is one of
    `EQ/NE/LT/LE/GT/GE` or the unsigned variants and whose two operands are
    unnamed integral `ICON` nodes.  Signed and unsigned relations are
    evaluated separately.  Constant true becomes a direct `GOTO`; constant
    false removes only the branch node.
  - Folding runs after the existing post-SSA `deljumps`, then immediately
    rebuilds and verifies the CFG as `post-constant-branch-fold`.  It does not
    run another dead-code pass, so unreachable-block cleanup remains a
    separate Phase 5D3 change.  FP comparisons, symbolic constants, XASM,
    delay-slot scheduling, and MIPS target code are untouched.
  - New `misc/ssabranch001` covers true and false branches exposed by D1 while
    retaining a nonconstant selector branch.  Cross regression passed
    `327/330` with only the three documented failures and runtime `290/290`.
    Native PCC passed `300` compilations plus `30` expected compile failures
    and runtime `290/290`; unexpected counts were zero and
    `NATIVE_PCC_REGRESS_RC:0`.  C++, F77, split-pass, host-cross, and native
    pass2 objects build successfully.  Unoptimized assembly remains
    byte-identical with SHA-256
    `bff0d1f27f8ab0b61e07de14db8313979e788d03be521282055fb327f6fb08ef`.
  - All six PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `11225/11063`, Malta64 soft `938/957`, Malta hard
    `12649/12720`, Malta soft `1026/1009`, MaltaEL hard `12474/13084`, and
    MaltaEL soft `1057/1034` KFLOPS.  Logs are
    `/private/tmp/pcc-phase5d2-*.log`.
  - Linpack is byte-identical to D1, SHA-256
    `4ded49d82f017ccb2243652f808d36cd767a7248e111ccda142a9248b00c429d`;
    counts stay VR4300 `2475/157` instructions/nops with `643/244`
    loads/stores and MIPS32r2 `2549/112` with `772/293`.  Stripped host
    `ccom` changes only `542920 -> 542952` bytes.
  - The build-only N64 hard-float a.out ROM is
    `builds/20260710-phase5d2-branch-fold/pcc-debug.z64`, size `6619136`,
    SHA-256
    `73487bfee4a26fc1dee5c8ad819c994915e469bd4d4478f783a0c90162c5837f`.
    It uses the default `-mfix4300` path.  Real N64 hardware and a distinct
    `-mno-fix4300` image have not been run; QEMU cannot reproduce the physical
    multiply erratum.  Earlier preserved ROM hashes remain unchanged.
- [x] Remove CFG blocks made unreachable by SSA branch folding (Phase 5D3,
  implementation commit `c19a5ceb`).
  - Reachability is recomputed from the fresh post-D2 CFG instead of using the
    old dominator DFS numbers.  The function entry, epilogue, `IP_DEFNAM`
    blocks, and every label named by the computed-goto table are roots; their
    complete successor closure is retained.
  - Every remaining unreachable block is removed as one interpass extent,
    tree nodes are released, and the CFG is immediately rebuilt and verified
    as `post-unreachable-removal`.  The pass does not perform general DCE,
    expression folding, LVN, scheduling, or target-specific rewriting.
  - New `misc/ssaunreach001` covers constant-true and constant-false paths
    whose dead sides contain volatile stores.  Optimized assembly contains no
    dead store.  Cross regression passed `328/331` with only the three
    documented failures and runtime `291/291`.  Native PCC passed `301`
    compilations plus `30` expected failures and runtime `291/291`; unexpected
    counts were zero and `NATIVE_PCC_REGRESS_RC:0`.
  - All six PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `11670/11229`, Malta64 soft `946/950`, Malta hard
    `12897/12806`, Malta soft `1018/1011`, MaltaEL hard `13075/13216`, and
    MaltaEL soft `1033/1041` KFLOPS.  Logs are
    `/private/tmp/pcc-phase5d3-{malta64,malta,maltael}-{hard,soft}.log`.
  - Linpack counts remain unchanged: VR4300 `2475/157` instructions/nops with
    `643/244` loads/stores and MIPS32r2 `2549/112` with `772/293`.
    Unoptimized assembly remains byte-identical with SHA-256
    `bff0d1f27f8ab0b61e07de14db8313979e788d03be521282055fb327f6fb08ef`.
    Stripped host `ccom` changes only `542952 -> 543000` bytes.
  - The build-only N64 hard-float a.out ROM is
    `builds/20260710-phase5d3-unreachable/pcc-debug.z64`, size `6619136`,
    SHA-256
    `0c831754963a61f611b2cd30eb2264174ace57e54fca905d43190912f2f6b2fd`.
    It uses the default `-mfix4300` path.  A real N64 run reported
    `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and the final
    `N64_PCC_DEBUG_RC_END` marker.  A distinct `-mno-fix4300` image has not
    been run; QEMU cannot reproduce the physical multiply erratum.  Earlier
    preserved ROM hashes remain unchanged.
- [x] Add conservative local value numbering after SSA cleanup (Phase 5D4,
  implementation commit `ed66b00a`).
  - Exact-type, side-effect-free integer `PLUS`, `MINUS`, `MUL`, bitwise,
    shift, unary-minus, and complement expressions are matched structurally
    inside one SSA basic block.  Only SSA TEMPs and unnamed integer constants
    are accepted; memory, pointers, FP, conversions, division, comparisons,
    symbols, and commutative reordering remain outside the pass.
  - Calls, volatile/memory operations, stores, XASM, structure operations, and
    uncertain assignments end the current LVN region.  A duplicate RHS is
    replaced with the first destination TEMP, then the existing Phase 5C copy
    propagation removes the resulting alias.  The pass is machine-independent
    and does not alter MIPS scheduling or the VR4300 erratum policy.
  - New `misc/ssalvn001` reduces the repeated-expression helper from two XORs
    to one while retaining both XORs across a volatile-memory barrier.  Cross
    regression compiled `329/332` with only the three documented failures and
    passed `292/292` runtime cases.  Native PCC passed `302` compilations plus
    `30` expected failures and `292/292` runtimes; unexpected counts were zero.
    C++, F77, split-pass, host-cross, and native pass2 builds are green.
  - All six PCC-kernel/PCC-rootfs QEMU profiles reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`.  Linpack pairs were
    Malta64 hard `11560/11156`, Malta64 soft `940/960`, Malta hard
    `12904/12964`, Malta soft `1018/1025`, MaltaEL hard `13248/13176`, and
    MaltaEL soft `1060/1051` KFLOPS.  Logs are
    `/private/tmp/pcc-phase5d4-{malta64,malta,maltael}-{hard,soft}.log`.
  - Linpack has no matching duplicate expression, so counts remain VR4300
    `2475/157` instructions/nops with `643/244` loads/stores and MIPS32r2
    `2549/112` with `772/293`.  Unoptimized `optim003` remains byte-identical,
    SHA-256
    `bff0d1f27f8ab0b61e07de14db8313979e788d03be521282055fb327f6fb08ef`.
    Stripped host `ccom` changes from `543000` to `559568` bytes.
  - The build-only N64 hard-float a.out ROM is
    `builds/20260710-phase5d4-lvn/pcc-debug.z64`, size `6619136`, SHA-256
    `795878b66c824beaf3c58bb8af52f3e68a331991b827e45006a92ec01ead2956`.
    It uses the default `-mfix4300` path.  Real N64 validation of D4 and a
    distinct `-mno-fix4300` image remain pending; all earlier ROM hashes are
    unchanged.

## Phase 1: Host PCC Configuration

- [x] Add a ReBSD OS configuration to imported PCC.
  - Target aliases: `mips-rebsd`, `mips-unknown-rebsd`.
  - Compatibility aliases: `mips-retrobsd`, `mips-unknown-retrobsd`.
  - ABI: static MIPS o32, big-endian, no PIC/TLS/threads/C++.
- [x] Define ReBSD preprocessor macros.
  - Required compatibility: `__retrobsd__`, `__RETROBSD__`.
  - Required Unix/MIPS: `__unix__`, `unix`, `__mips__`, `mips`,
    `__MIPSEB__`, `_MIPSEB`, `__mips_o32`, `__mips_hard_float`.
  - ReBSD identity: `__rebsd__`, `__REBSD__`.
- [x] Teach PCC configure scripts about the ReBSD target.
- [x] Fix PCC MIPS target endian selection for big-endian output.
- [x] Keep PCC assembly output compatible with ReBSD `as` for the first smoke.
- [x] Build host cross PCC out of tree in `/private/tmp`.
- [x] Compile a trivial C file to assembly with host PCC.
- [x] Assemble and link a trivial ReBSD a.out using ReBSD `as` and `ld`.
  - Smoke binary: `/private/tmp/rebsd-pcc-smoke`.
  - Configure/build dir: `/private/tmp/rebsd-pcc-build`.
  - Install prefix: `/private/tmp/rebsd-pcc-install`.

## Phase 2: Host PCC Smoke Tests

- [x] Add a make target for host PCC smoke testing.
  - Malta target: `make -C sys/mips/malta smoke-host-portablecc`.
  - Shared MIPS dispatcher: `make -C sys/mips smoke-host-portablecc`.
- [x] Verify generated objects with existing ReBSD tools.
  - `mips-rebsd-pcc -S`, `-c`, and full link pass with ReBSD native
    `as`/`ld`.
- [x] Run a minimal linked binary under Malta.
  - Temporary QEMU rootfs smoke ran `/root/rebsd-pcc-smoke`.
  - Expected output observed: `rebsd-pcc-smoke:6`.
- [x] Use the old `src/cmd/ccom/arch-mips` backend as the compatibility oracle
  for imported PCC MIPS fixes.
  - Big-endian `long long` load/store/FUNARG word order.
  - Big-endian `CLASSB` register word selection and overlapping `rmove()`.
  - Direct `SOREG` conversions for scalar and `long long` narrowing/widening.
  - Floating constant conversion and synthetic literal storage.
  - Big-endian `long long`, float, and double static initializer emission.
  - Big-endian offset handling for narrowing conversions.
- [x] Expand PCC smoke tests to cover core scalar and ABI cases.
  - Host `-S`, `-c`, and full link pass for `types-smoke.c`, `ll-smoke.c`,
    `ll-abi-smoke.c`, and `pcc-fpu-smoke.c`.
  - Malta runtime status: `types-status:0`, `ll-status:0`, `llabi-status:0`,
    `fpu-status:0`.
  - Covered areas include scalar initializers, global/static objects, pointer
    initializers, floating literals, stack-passed narrow arguments, `long long`
    arithmetic/conversions, hardware FPU double operations, and C/assembly o32
    ABI handoff.
  - The target native smoke now also runs `/root/libc-abi-smoke.sh`, which
    compiles and runs the same libc/ABI stress source with both `cc` and `pcc`.
    It covers struct return/by-value arguments, stack-passed 64-bit arguments,
    qsort callbacks, varargs, `vsnprintf`, parsing, `setjmp`/`longjmp`, and
    stdio file I/O.
- [x] Link ReBSD PCC programs through `src/dev/pcc/pcc-libs/libpcc`.
  - `libpcc.a` is built as a ReBSD a.out archive by the existing N64 native
    runtime path, using the GCC-to-ReBSD-assembler wrapper for now.
  - Rootfs staging installs `/usr/lib/libpcc.a` and `/lib/libpcc.a`.
  - `os/rebsd/ccconfig.h` now uses `-lpcc -lc -lpcc` with `-L` library paths,
    so this depends on ReBSD `ld` support for `-L` and `-l`.
  - `libpccsoftfloat.a` is still not staged for hardware-FPU Malta/N64 builds.
- [x] Inspect `src/dev/pcc/pcc-libs/csu` for ctor/dtor support.
  - `crtbegin.o`/`crtend.o` are primarily for C++ global constructors and
    destructors, plus C constructor/destructor attributes.
  - They are not enabled in the default ReBSD C link path yet.
  - ReBSD now supports the C constructor/destructor attribute gate without
    changing PCC code generation: `as` preserves `.ctors`/`.dtors` and
    `.init_array`/`.fini_array` as a.out pseudo-sections, `ld` collects them
    after normal data, and libc startup/shutdown walks the linker-defined
    `__CTOR_LIST__`/`__CTOR_END__` and `__DTOR_LIST__`/`__DTOR_END__` ranges.
- [x] Run upstream `src/dev/pcc/pcc-tests/regress` against ReBSD/MIPS PCC.
  - Baseline without `libpcc.a`: 300 checks total, 268 passed, 32 failed;
    Malta runtime 239 linked binaries, 230 passed, 9 failed.
  - With `libpcc.a`: 300 checks total, 271 passed, 29 failed; Malta runtime
    242 linked binaries, 231 passed, 11 failed.
  - ReBSD/PCC run with `libpcc.a` and default `crt0.o`: 311 checks total,
    283 passed, 28 failed; Malta runtime 246 linked binaries, 234 passed,
    12 failed.
  - Current ReBSD/PCC run after `ld` `-L/-l` ordering, hard-float helper
    prototypes, `sqrt(-1.0)`, raw `signbit`, `double -> float` NaN-sign,
    unsigned bitfield assignment, MIPS `ZF` helper ABI fixes, MIPS
    aggregate-return ABI fixes, MIPS stack-argument alignment fixes, MIPS
    memory integer-to-double conversion fixes, MIPS big-endian sub-word
    bitfield fixes, ReBSD a.out text/data/BSS 8-byte alignment fixes,
    ReBSD 16-bit `wchar_t` ABI fixes for PCC wide strings, target-neutral
    `long double` limits in `c99/arith003`, self-contained `assert.h`,
    ReBSD `as` absolute `la` pseudo-op support, libc `strtof`/`strtold`
    wrappers for the ReBSD IEEE64 `long double` ABI, ReBSD `as`
    `.eh_frame`/`neg`/`negu` compatibility, ReBSD `as`
    `symbol+-offset`/UTF-8 identifier compatibility, and ReBSD PCC
    trigraph preprocessing, cast-wrapped string literals in static
    initializers, `stddef.h` `wchar_t`, ABI-neutral wide-string test
    expectations, macro-expanded `#include` line-end consumption, floating
    NaN/Inf static initializer folding, MIPS computed-goto support, and
    ReBSD a.out ctor/dtor collection:
    311 checks total, 308 passed, 3 failed; Malta runtime 271 linked
    binaries, 271 passed, 0 failed.
  - Skipped by policy for this target: PIC/shared-library groups and GNU
    linker-flag-specific cases.
  - Raw artifacts from this run: `/private/tmp/rebsd-pcc-tests/compile-summary.json`,
    `compile.log`, `/private/tmp/rebsd-pcc-runtime.log`, and generated test
    binaries.
- [x] Fix MIPS o32 aggregate-return ABI handling in imported PCC.
  - No-argument struct/union-return calls now still run through target
    `funcode()` so the hidden result pointer is emitted.
  - Struct/union return temporaries now allocate a full stack object via
    `cstknode()` instead of a single temp word.
  - Callee parameter stack offsets are shifted by one o32 slot after the hidden
    `$a0` result pointer.
  - This fixed `c99/func003` and `jira/PCC-138`; it also exposed the separate
    `jira/PCC-85` stack alignment failure fixed below.
- [x] Fix MIPS o32 stack alignment for stack-passed call arguments.
  - `lastcall()` now computes the real `FUNARG` push size after target
    lowering and emits a pre-argument pad when the pushed stack arguments would
    leave `$sp` only 4-byte aligned.
  - This keeps the ABI position of the first stack argument intact while
    preserving the existing rounded post-call cleanup.
  - This fixed `jira/PCC-85`, which was faulting in `_doprnt` at
    `sdc1 $f26,504(sp)` because `$sp` reached GCC-built libc misaligned.
  - The Linpack smoke exposed a second case in the same area: internal MIPS
    FP bit-bounce argument nodes must not be counted as extra stack-pushed
    arguments during post-call cleanup.
  - `lastcall()` now derives the cleanup size from actual `FUNARG` pushes plus
    the ABI home area/pad, fixing an 8-byte stack over-clean after calls such
    as `daxpy_r(int, double, ptr, int, ptr, int)`.
- [x] Fix MIPS o32 stack slot accounting for mixed aggregate/64-bit calls.
  - Hidden struct-return pointers now shift following callee parameter offsets
    while preserving 8-byte alignment for `long long`, `double`, and
    `long double`.
  - Stack-only `long long`/`double` arguments now emit their ABI padding slot at
    the correct call-site position instead of relying on a trailing whole-call
    stack pad.
  - By-value struct/union arguments now pass the stack tail as normal `FUNARG`
    words in callee-visible order instead of writing fixed offsets from the
    caller frame.
  - PCC's ReBSD/MIPS predefined macros now include `__mips_hard_float`, so
    public headers expose the hard-float `jmp_buf` size expected by `setjmp.S`.
    Without this, `setjmp()` saved 47 words into a 14-word buffer and overwrote
    the following `__atexit` global.
  - Added libc `vsnprintf()` as the real `snprintf()` worker so target varargs
    code can link without a private stub.
  - Added `/root/libc-abi-smoke.c` and `/root/libc-abi-smoke.sh`; Malta and
    Malta64/R4000 both pass it through native `cc` and `pcc`.
- [x] Keep MIPS register coalescing inside a single register class.
  - The optimizer now refuses GP/FP cross-class move coalescing instead of
    assigning one temporary to incompatible integer and FPU registers.
  - FP register arguments and varargs bit-copies are lowered through typed
    stack views, so the allocator does not see one temp as both raw integer
    bits and a floating-point value.
  - This fixed invalid optimized MIPS/FPU output seen while bringing up the
    PCC-built Linpack benchmark.
- [x] Fix MIPS memory integer-to-double conversion.
  - `SOREG TWORD -> double/long double` conversion now loads the 32-bit
    integer word into an FPU register with `l.s` before `cvt.d.w`.
  - The old `l.d` form required 8-byte alignment and read past the integer
    slot.
  - This fixed `jira/PCC-236`.
- [x] Fix MIPS big-endian sub-word bitfield reads and padding clears.
  - `rmfldops()` now computes big-endian bit offsets from the actual storage
    unit type (`char`, `short`, `int`, or `long long`) instead of the promoted
    result type.
  - Automatic aggregate initialization now clears sub-byte padding spans with
    byte-sized bitfield stores, avoiding unaligned word RMW sequences.
  - This fixed `jira/PCC-462`; it changed the failure from an unaligned memory
    fault, through a diagnostic return code of 4, to a clean Malta pass.
- [x] Make ReBSD `assert.h` self-contained for active assertions.
  - `assert.h` now has an include guard and includes `stdio.h`/`stdlib.h`
    when `NDEBUG` is not set, so `assert()` exposes `stderr`, `fprintf`, and
    `exit` regardless of include order.
  - The macro now uses a `do { ... } while (0)` statement form and compiles
    PCC `jira/PCC-194` through the frontend.
- [x] Let ReBSD `as` accept absolute `la` pseudo-op operands.
  - GNU-compatible MIPS assembly can use `la $reg,0` for null pointer
    constants; ReBSD `as` now treats absolute `la` operands like `li`.
  - This fixed `c99/basic004`, `c99/func004`, `c99/oper001`, `jira/PCC-178`,
    and `jira/PCC-194` in the PCC compile/link pass.
- [x] Add libc `strtof` and `strtold` wrappers for the current MIPS ABI.
  - ReBSD/MIPS currently uses 8-byte IEEE64 `long double`
    (`__LDBL_MANT_DIG__ == 53`), matching `double`.
  - `strtof()` now casts from `strtod()`, and `strtold()` returns the same
    parsed value as `strtod()` under this ABI.
  - This fixed PCC compile/link and Malta runtime `jira/PCC-23`.
- [x] Extend ReBSD `as` for PCC `.eh_frame` and `neg`/`negu` output.
  - `.eh_frame` section attributes now assemble into the a.out string/rodata
    compatibility section instead of being rejected as an unknown section.
  - `neg`/`negu` assemble as two-operand aliases of `sub`/`subu` with `$zero`.
  - This fixed PCC compile/link `jira/PCC-228` and `jira/PCC-464`; both pass
    on Malta QEMU runtime.
- [x] Extend ReBSD `as` expression and identifier parsing for PCC output.
  - Unary `+`/`-` terms are accepted in expressions, so PCC output such as
    `yycrank+-8` assembles without rewriting compiler output.
  - UTF-8 bytes are accepted in symbol names, so UCN-derived identifiers can
    be assembled as emitted by PCC.
  - This fixed PCC compile/link `misc/array001` and moved `misc/ucn001` from
    an assembler failure to the trigraph preprocessing issue fixed below.
    `misc/array001` passes on Malta QEMU runtime.
- [x] Enable trigraph conversion in the ReBSD PCC target configuration.
  - PCC's `cpp` already has a `-T` trigraph translation pass; ReBSD now enables
    it by default for the target driver instead of adding another preprocessor.
  - This fixed PCC compile/link and Malta runtime `c99/trigraph001`.
  - It also fixed Malta runtime `misc/ucn001`, where `??/u023e` must become
    `\u023e` before UCN and string handling.
- [x] Mark static-initializer string literals through cast wrappers.
  - PCC saves string literal bodies until `strprint()`, and only emits literals
    whose `SSTRING` symbol has been marked referenced.
  - ReBSD now walks the static initializer expression before constant emission,
    so casts like `(struct a *)"FOO"` still mark the underlying string symbol.
  - This fixed PCC compile/link and Malta runtime `pcclist/init013`.
- [x] Expose ReBSD `wchar_t` through `stddef.h` and keep wide-string tests
  ABI-neutral.
  - `include/stddef.h` now exports the same 16-bit `wchar_t` ABI as
    `include/wchar.h` for ReBSD/MIPS.
  - `misc/str001` now declares expected wide-string arrays as `wchar_t`
    instead of assuming `wchar_t == int`, so the test remains architecture
    independent.
  - This fixed PCC compile/link and Malta runtime `misc/str001`.
- [x] Consume macro-expansion residue after macro-expanded `#include` names.
  - PCC `cpp` now eats trailing whitespace and internal macro-service bytes
    after a macro-expanded include filename before returning to directive
    parsing.
  - This fixed PCC compile/link and Malta runtime `pcclist/prep003`.
- [x] Fold floating NaN/Inf constant expressions in static initializers.
  - PCC already uses host-independent softfloat for frontend floating
    constants, so ReBSD no longer punts floating divide-by-zero folds to
    runtime codegen when a constant initializer is required.
  - Integer constant division by zero remains protected in `conval()`.
  - This fixed PCC compile/link and Malta runtime `jira/PCC-180` and
    `jira/PCC-215`.
- [x] Add MIPS computed-goto support for GCC-compatible label values.
  - The MIPS pass2 table now emits `jr` for computed `goto`.
  - MIPS static initializer emission now normalizes negative temporary label
    offsets before printing `LABFMT`, so `&&label` arrays produce `L259`
    instead of an undefined external `L-259`.
  - This fixed PCC compile/link and Malta runtime `gcccompat/extension004`.
- [x] Classify `pcc-tests` compile/link failures for the current C-only gate.
  - Current remaining compile/link failures are outside the first ReBSD/MIPS C
    compiler gate:
    `gcccompat/typeof001` uses x86 inline-asm constraints `=d`/`=a`;
    `misc/shlib3` uses unsupported `-fPIC -shared` output;
    `pcclist/init004` requires TLS, which the ReBSD target currently rejects.
  - `gcccompat/typeof001` is not a portable `typeof` failure: the test embeds
    x86 instructions and register constraints (`eax`/`edx`/`ecx`, `a`, `d`).
    Keep it expected-fail for MIPS, or add a target guard/MIPS-specific
    equivalent if this upstream test is kept in the ReBSD gate.
  - `misc/shlib3` is a shared-library/PIC policy failure: static and combined
    variants of the same source pass, including constructor/destructor
    attributes, but ReBSD a.out does not implement `-shared`, runtime shared
    object loading, `-rpath`, or a shared-library ABI.
  - `pcclist/init004` is a TLS policy failure: PCC reports "thread-local
    storage not supported for this target", matching the current ReBSD/MIPS
    target configuration and runtime ABI.
- [x] Support ReBSD a.out ctor/dtor collection in `as`, `ld`, and libc startup.
  - The root cause was in the a.out toolchain rather than PCC: ReBSD `as`
    accepted `.ctors`/`.dtors` syntax but flattened those sections into normal
    `.data`, and ReBSD `ld` had no way to preserve constructor/destructor list
    boundaries across linked inputs.
  - ReBSD `as` now emits local size metadata for ctor/dtor tails, and ReBSD
    `ld` uses that metadata to split each input's normal data, constructor
    table, and destructor table before writing the final data segment.
  - `pcclist/init006` now passes on Malta; current expected runtime failures
    are empty.
- [x] Make `c99/arith003` respect target `long double` limits.
  - ReBSD/MIPS intentionally uses IEEE64 `long double`, with PCC reporting
    `__LDBL_MAX_EXP__ == 1024` and `__LDBL_MANT_DIG__ == 53`.
  - The upstream test hard-coded extended-range values such as
    `1.1897314953572317650E+4932L`, which fold to infinity on IEEE64
    `long double` targets.
  - The test now uses compiler-provided `__LDBL_MIN__`/`__LDBL_MAX__` when
    available, and keeps the old constants as a fallback.
- [x] Align ReBSD `wchar_t` ABI with PCC wide strings.
  - `include/wchar.h` now keeps ReBSD/MIPS `wchar_t` at 16 bits, matching
    PCC's `WCHAR_TYPE USHORT`/`WCHAR_SIZE 2`, even when libc is built by a
    GCC toolchain whose native wide literals are 32-bit.
  - The temporary `wprintf` implementation no longer uses GCC wide literals,
    so it reads PCC-generated wide strings as 16-bit elements.
  - This fixed Malta runtime `jira/PCC-21` and removed the associated
    bad-address fault.
- [x] Add targeted value regression for mixed `long long` bitfields.
  - A temporary diagnostic for `jira/PCC-462` showed the official test now
    passes, but some layouts involving a following signed `long long`
    bitfield still read `f0` as `0` instead of `2`.
  - Added `jira/PCC-462-1`, which checks exact `f0 == 2` and `f1 == -2`
    values for global initialization, automatic initialization, and both
    bitfield assignment orders.
- [x] Fix native PCC regressions found by the target-side Malta gate.
  - MIPS `STASG`/`STARG` hidden `memcpy` calls now mark all caller-saved
    registers unavailable, so native tools do not keep live values across the
    helper call.
  - Big-endian memory narrowing uses the addressed low-order byte/half/word
    instead of mutating OREG offsets globally.
  - MIPS unsigned 32-bit comparisons now emit real unsigned compare branches,
    including constant cases; this fixed native `as` encoding negative `li`
    operands as raw illegal-instruction words.
  - Bitfield value contexts now apply integer promotions before usual
    arithmetic conversions, and assignment to bitfields narrower than `int`
    yields an `int` value.  This fixed `jira/PCC-462` and `jira/PCC-476-1`.
  - Added strict regressions `misc/stasg001` and `misc/ucmp001`.
  - Clean Malta target-side result after rebuilding generated/rootfs artifacts:
    `NATIVE_PCC_REGRESS_TOTAL:317`,
    `NATIVE_PCC_REGRESS_COMPILE_PASS:287`,
    `NATIVE_PCC_REGRESS_EXPECTED_COMPILE_FAIL:30`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_COMPILE_FAIL:0`,
    `NATIVE_PCC_REGRESS_RUNTIME_TOTAL:277`,
    `NATIVE_PCC_REGRESS_RUNTIME_PASS:277`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_RUNTIME_FAIL:0`,
    `NATIVE_PCC_REGRESS_RC:0`.
- [x] Continue expanding smoke tests to cover libc-heavy programs, varargs,
  structs/unions, pointer arithmetic stress, and basic shell build workloads.
  - `/root/alias-stress-smoke.sh` now covers larger pointer arithmetic,
    nested structs/unions, char-alias access, function pointer dispatch,
    struct-by-value copies, and overlapping struct `memmove()` through both
    `/usr/bin/cc` and `/usr/bin/pcc`.
  - `/root/make-workload-smoke.sh` now covers a second native shell build:
    two static libraries, dependent `-L/-l` link order, `nm`, `size`, archive
    probing, `make clean`, and a clean rebuild through both compiler driver
    names.
- [x] Audit MIPS backend `ZF` soft-float helper calls under the ReBSD
  hard-float o32 ABI.
  - Current ReBSD/MIPS table bypasses hardware `cvt.s.d` for `double -> float`
    and calls `__truncdfsf2` with the hard-float ABI to preserve NaN sign bits.
  - Separate raw `cvt.s.d` assembly check returned mask `2` on both Malta QEMU
    and real N64/VR4300 hardware: `-1.0` keeps its sign after conversion, but
    `-NaN` is canonicalized to a positive single-precision NaN.
  - ReBSD `ZF` conversions now pass FP helper arguments in `$f12` for the
    hard-float o32 ABI, and FP-to-`long long` helper results are modeled as
    `$v0/$v1` returns instead of stale `$a0/$a1` arguments.
  - This fixed `pcclist/basic005` on Malta.
- [x] Turn the temporary `pcc-tests` harness into a tracked test target.
  - `make -C sys/mips pcc-regress-compile` builds the upstream
    `src/dev/pcc/pcc-tests/regress` matrix with the host ReBSD/MIPS PCC.
  - `make -C sys/mips pcc-regress-runtime` stages linked runtime binaries
    through the generated Malta rootfs manifest, rebuilds `unix.elf`, and runs
    them under Malta QEMU.
  - The harness preserves upstream runtime expectations such as `c99/basic006`
    observing `argv[0] == "./basic006.out"`.
- [x] Add a Linpack GCC/PCC smoke benchmark.
  - `make -C sys/mips linpack-smoke-runtime` builds the same Linpack source
    with the existing GCC wrapper and imported PCC, stages `/root/linpack-gcc`
    plus `/root/linpack-pcc`, then runs both on Malta.
  - Runtime defaults are `LINPACK_ARRAY_SIZE=120` and
    `LINPACK_MIN_SECONDS=1`; the make variables `LINPACK_SMOKE_ARRAY_SIZE`
    and `LINPACK_SMOKE_MIN_SECONDS` override them for longer comparisons.
  - `libm.a` now includes `fabs.o`, so Linpack links cleanly with `-lm`.
  - Clean Malta runtime baseline: GCC about 14.7 MFLOPS and PCC about
    12.4-13.0 MFLOPS with `LINPACK_RC:0` at the default smoke size.

## Current Remaining PCC Work

- [x] Package imported PCC as the native C rootfs compiler.
  - Install the driver as `/usr/bin/cc`, `/usr/bin/pcc`, and `/usr/bin/cpp`.
  - Install C-only internal passes under `/usr/libexec/pcc/`.
  - Stage headers, `crt0.o`, `libc.a`, `libm.a`, and `libpcc.a` coherently for
    native use.
- [x] Replace the legacy compiler stack after native C PCC became usable.
  - Remove the old `cpp`, legacy `cc`/`pcc`, `lcc`/`lccom`, and
    `smallc`/`smlrc` from the default rootfs install profile.
  - Imported PCC now compiles, assembles, links, and runs C programs inside
    Malta using the native ReBSD `as`/`ld` and staged `libpcc.a`.
- [x] Run the native C Malta PCC gate from inside ReBSD.
  - Boot Malta with imported C PCC installed.
  - Compile, assemble, link, and run representative C programs on the target.
  - Verified result: `NATIVE_PCC_SMOKE_RC:0`.
- [x] Run the full PCC test gate from the target environment.
  - The native rootfs harness stages `src/dev/pcc/pcc-tests/regress` under
    `/root/pcc-tests` and runs `/root/run-native-pcc-regress.sh` on Malta.
  - Current clean Malta target-side result with native `/usr/bin/cc` on the
    default QEMU Malta 24Kf/MIPS32 profile:
    317 total checks, 287 compile pass, 30 expected compile fail,
    0 unexpected compile fail; 277 runtime candidates, 277 pass,
    0 unexpected runtime fail.
  - Re-run on 2026-07-03 with `make -C sys/mips/malta
    native-pcc-regress-runtime` stayed green with the same summary and
    `NATIVE_PCC_REGRESS_RC:0`.
  - Re-run again on 2026-07-03 after making the PCC softfloat `DEBUGFP`
    oracle FCSR-safe on MIPS stayed green:
    `NATIVE_PCC_REGRESS_TOTAL:317`,
    `NATIVE_PCC_REGRESS_COMPILE_PASS:287`,
    `NATIVE_PCC_REGRESS_EXPECTED_COMPILE_FAIL:30`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_COMPILE_FAIL:0`,
    `NATIVE_PCC_REGRESS_RUNTIME_TOTAL:277`,
    `NATIVE_PCC_REGRESS_RUNTIME_PASS:277`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_RUNTIME_FAIL:0`,
    `NATIVE_PCC_REGRESS_RC:0`.
  - Re-run again on 2026-07-03 after replacing the MIPS/ReBSD
    `DEBUGFP` `double -> float -> double` oracle path with
    `__truncdfsf2`/`__extendsfdf2` stayed green:
    `NATIVE_PCC_REGRESS_TOTAL:317`,
    `NATIVE_PCC_REGRESS_COMPILE_PASS:287`,
    `NATIVE_PCC_REGRESS_EXPECTED_COMPILE_FAIL:30`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_COMPILE_FAIL:0`,
    `NATIVE_PCC_REGRESS_RUNTIME_TOTAL:277`,
    `NATIVE_PCC_REGRESS_RUNTIME_PASS:277`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_RUNTIME_FAIL:0`,
    `NATIVE_PCC_REGRESS_RC:0`.
  - `c99/darray005` now initializes the VLA inputs it passes to `add()`.  The
    old test read uninitialized stack integers, which could trap as signed
    arithmetic overflow on target hardware and was not a valid compiler
    regression.
  - ReBSD/native PCC `DEBUGFP` now decodes big-endian IEEE64 softfloat words
    before comparing against native FPU results, treats NaN payload/sign
    differences as class-equal, and keeps the `strtold()` oracle only for
    host builds where native libc is a valid parser oracle.
- [x] Add a VR4300-like Malta QEMU gate before claiming N64 hardware readiness.
  - The previous green Malta result used `qemu-system-mips -M malta` without
    `-cpu`, which is not a VR4300-class CPU model.
  - Local QEMU exposes no exact VR4300.  The closest automated profile is
    `qemu-system-mips64 -M malta -cpu R4000`; `-cpu VR5432` is available as
    a later NEC VR-family probe.
  - Current `qemu-system-mips64` big-endian machine list is `malta`,
    `magnum`, and `pica61`; there is no `n64`/`vr4300` board model.
  - ReBSD now has a separate `sys/mips/malta64` port for this gate.  It uses
    `qemu-system-mips64 -M malta -cpu R4000 -m 32M -nographic`, keeps the
    normal `sys/mips/malta` port unchanged, and builds the kernel with
    MIPS3/R4000 flags plus the 64-bit Malta stage0 wrapper.
  - Clean R4000/Malta64 native PCC gate on 2026-07-03 after the N64 debug
    isolation work:
    `NATIVE_PCC_REGRESS_TOTAL:317`,
    `NATIVE_PCC_REGRESS_COMPILE_PASS:287`,
    `NATIVE_PCC_REGRESS_EXPECTED_COMPILE_FAIL:30`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_COMPILE_FAIL:0`,
    `NATIVE_PCC_REGRESS_RUNTIME_TOTAL:277`,
    `NATIVE_PCC_REGRESS_RUNTIME_PASS:277`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_RUNTIME_FAIL:0`,
    `NATIVE_PCC_REGRESS_RC:0`.
  - This gate is useful for catching MIPS3/R4000 regressions, but it still
    does not reproduce the real N64 intermittent `ccom` bad-address faults.
    Keep real hardware logs as the deciding evidence for N64 readiness.
  - N64 hardware native PCC output showed real unexpected failures:
    `c99__arith002`, `c99__arith003`, and `jira__PCC-185` link with
    `Undefined: main`; `jira__PCC-152` reports empty compiler output; several
    runtime candidates print shell `syntax error`/`Memory fault` noise that
    the hardware harness must isolate instead of masking.
  - Added a dedicated N64 hardware diagnostic image target:
    `make -C sys/mips/n64 pcc-debug-image`.
    It does a clean N64 rebuild, forces `N64_DEBUG_UART_ONLY=1`, builds the
    rootfs userland with native PCC, disables video/input console paths, and
    packages a 6 MB minimal test rootfs as `sys/mips/n64/pcc-debug.z64`.
  - The debug rootfs auto-runs `/root/n64-pcc-debug-runner` on boot.  This
    runner is a standalone ReBSD a.out binary built with the N64 GCC wrapper
    plus ReBSD `as`/`ld`; it executes `/usr/bin/cc`, `/usr/bin/size`,
    `/usr/bin/aout`, and test binaries directly through `fork`/`exec`, without
    shell conditionals, `$?`, or redirection.  The primary pass isolates the
    first N64-only symptoms: `c99__arith002`, `c99__arith003`, `jira__PCC-70`,
    `jira__PCC-77`, `jira__PCC-152`, and `jira__PCC-185`.
  - The legacy `/root/run-n64-pcc-debug.sh` and
    `/root/run-n64-pcc-debug-all.sh` scripts are still staged for comparison,
    but they are not the boot-time harness until the N64 shell issue is fixed.
  - The first boot of this image reached `/etc/rc`, then the PCC-built
    `/bin/sh` faulted in `stoi()` at PC `0x0040f588` on address `0x00000001`.
    The immediate trigger was shell fd-move redirection (`2>&1`) in the debug
    rc script, before any PCC tests ran.  Keep this as a real N64/PCC shell
    regression to isolate and fix; the debug rc now avoids fd-move redirection
    only so the hardware test can progress.
  - A later debug boot faulted on `/etc/rc` line 4 (`exec </dev/console`) and
    then failed to mount `/var`; the runner could only report
    `N64_PCC_MKDIR_FAIL /var/tmp/n64-pcc-debug 30` because the cartridge
    rootfs was still read-only.  The debug rc now leaves the inherited console
    descriptors alone and prints explicit `mkfs`, `mount`, and runner return
    markers so the next hardware log separates shell redirection faults from
    ramdisk/mount failures.
  - The next debug boot produced no `/etc/rc` marker at all.  `init` opens `/`
    and duplicates that fd to stdout/stderr before `execl("/bin/sh",
    "/bin/sh", "/etc/rc", "autoboot")`; the old shell redirection had been the
    only thing reconnecting output to `/dev/console`, and that redirection is
    part of the broken shell path.  The N64 debug rootfs now stages the C
    runner as `/bin/sh` as well as `/root/n64-pcc-debug-runner`; when invoked
    as `/bin/sh /etc/rc`, it opens `/dev/console`, formats `/dev/ram0`, mounts
    `/var`, creates runtime state, and then runs the same PCC diagnostic path
    without using shell parsing or redirection.
  - A later N64 boot reached `/root/run-n64-pcc-debug.sh` and failed with
    `bad number` before the first test marker; this is another shell runner
    failure, not a compiler-test result.  Interactive `login` then looped
    because it starts `/bin/sh` as the root login shell, so a broken shell
    makes login look like authentication failed.  The debug image now disables
    getty in `/etc/ttys` and relies on the boot-time UART runner until the
    shell/login path is fixed.
  - Earlier 2026-07-03 hardware logs showed `/var` formatting and mounting
    successfully, then repeated `/etc/rc`/single-user re-entry after the test
    runner returned.  The repeated runs produced `/dev/ram0 on /var: Device
    busy` and `/var: bad dir I=28 off 0: mangled entry` noise, so only the
    first `N64_PCC_DEBUG_RC_BEGIN` to `N64_PCC_DEBUG_RC_END` block should be
    used as valid evidence.
  - The debug runner now treats both `argv0="sh"` and `argv0="-"` as shell
    stubs.  Unsupported shell-stub invocation now sleeps forever after
    printing `N64_PCC_SH_STUB`, preventing `init` from repeatedly re-entering
    the PCC tests after the first diagnostic pass.
  - The N64 8 MB `/dev/ram0` allocation for `/var` was increased from 1 MB to
    2 MB for the PCC debug image.  The follow-up hardware log confirmed this
    part is clean: `/dev/ram0` formatted as 2048 kbytes/512 inodes, mounted
    with `N64_PCC_DEBUG_MOUNT_RC 0`, and `manual__min` passed the full direct
    pipeline through `/usr/bin/cc`, direct `ccom`, direct `/usr/bin/as`, and
    `aout -d`.
  - The same first valid log block ended with `N64_PCC_DEBUG_END 6`.  Three
    failures were driver environment/tool lookup bugs: `jira__PCC-70`,
    `jira__PCC-77`, and `jira__PCC-152` generated assembly and assembled
    through direct `/usr/bin/as`, but the PCC driver compile/object path
    reported `Exec of as failed` / `as terminated with status 127`.
  - The `as` lookup failure is caused by the target PCC driver executing bare
    `as` from a boot-time environment without the login shell `PATH`; ReBSD's
    fallback `_PATH_STDPATH` is only `/bin`, while native `as` lives in
    `/usr/bin`.  The native PCC build now defines `ASSEMBLER=/usr/bin/as` and
    `LINKER=/usr/bin/ld`, and the PCC driver `find_file()` now preserves
    absolute tool paths instead of searching `progdirs`.
  - The next supplied hardware log confirmed the absolute-tool fix was real:
    `manual__min` passed and the old `Exec of as failed` path did not return.
    The remaining failures moved to `/usr/libexec/pcc/cpp`/`ccom` signal 8
    on FP-heavy tests such as `c99__arith002` and `c99__arith003`.
  - A follow-up debug boot exposed another pre-test harness failure:
    PCC-built `/sbin/mount /var` crashed with SIGSEGV while the explicit
    `/sbin/mount -o rw /dev/ram0 /var` form succeeded.  The boot runner now
    uses only the explicit form before PCC tests, and the shorthand
    fstab/getfsfile path is tracked as a separate userland regression.
  - A temporary `-DCROSS_COMPILING` target-PCC build proved useful only as a
    diagnostic, because it removed PCC's softfloat native-FPU debug oracle
    from `ccom`; it is not an acceptable final fix and has been removed.
  - The next N64 signal path was narrowed by the latest supplied hardware log:
    `/var` formatting/mounting is clean, `manual__min` passes the direct
    `/usr/bin/cc`, `ccom`, `/usr/bin/as`, and `aout -d` pipeline, and the
    absolute-tool lookup failures did not return.  The remaining compiler
    failures were in PCC FP folding: `fp__builtin_inf` faulted in direct
    `ccom` with signal 11, while `c99__arith002`, `c99__arith003`, and
    `jira__PCC-185` faulted in direct `ccom` with signal 8.
  - The signal 8 cases were traced to the same raw MIPS FPU conversion class
    as the earlier `double -> float` NaN-sign fix.  The N64 FPE trace pointed
    at `soft_fp2fp()` inside the PCC softfloat `DEBUGFP` oracle, where the
    native C cast emitted `cvt.s.d; cvt.d.s`; the faulting instruction was the
    `cvt.d.s` with FCSR `0x00020040` (unimplemented operation).
  - The native FPU probe in that log produced correct IEEE results
    (`inf_ok=1`, `nan_ok=1`) but reported a contradictory child exit status;
    keep the wait-status/reporting path under suspicion, but do not treat the
    whole VR4300 FPU as broken from that probe alone.
  - PCC's softfloat `DEBUGFP` native-FPU oracle is now FCSR-safe on MIPS: it
    saves the incoming FCSR, clears exception flags, exception enables, and
    cause bits while executing oracle expressions, then restores the original
    FCSR.  This keeps `DEBUGFP` enabled for target `ccom` without using the
    rejected `-DCROSS_COMPILING` workaround.
  - The same oracle now uses the ReBSD/MIPS runtime helper path for
    `double -> float` checking: `__truncdfsf2` rounds to single precision and
    `__extendsfdf2` widens back for the `long double` comparison.  This keeps
    the oracle active, but removes the raw `cvt.s.d`/`cvt.d.s` sequence from
    target `ccom`.
  - Binary check after a clean N64 rebuild: `soft_fp2fp` in
    `rootfs.pcc-debug.stage/usr/libexec/pcc/ccom` calls `__truncdfsf2` at
    `0x0043bd48` and `__extendsfdf2` at `0x00438ea8`; no `cvt.s.d` or
    `cvt.d.s` remains in the disassembled `ccom`.
  - The N64 debug runner now starts with a GCC-built native FPU/FCSR child
    probe, then a small PCC FP-folding probe matrix before the full primary
    tests: `fp__div_zero`, `fp__nan_zero`, `fp__builtin_inf`,
    `fp__builtin_nan`, and `fp__isnan_call`.
  - Rebuilt `sys/mips/n64/pcc-debug.z64` on 2026-07-03 after a clean N64
    rebuild.  The image contains the absolute-tool fix, the FP probes, the
    FCSR-safe/helper-based `DEBUGFP` oracle, the deterministic `darray005`
    test, and the `ccom-stress.sh` smoke; `fsutil --check` reports 322 files,
    3222 blocks used, and 2897 blocks free.
  - The latest debug image also isolates the PCC hardware run from unrelated
    N64 video/input/USB activity: `N64_DEBUG_UART_ONLY=1` removes the VI,
    SI/joybus, and N64cart USB Ethernet backend objects from the kernel.
    `ioconf.debug-uart.o` provides a matching service list without USBNet, so
    no no-op `usbnetattach`/`usbnpoll` compatibility symbols are needed.
  - Add the closest usable VR4300-like QEMU/Malta profile if we can make one
    boot, but do not treat the current default Malta 24Kf/MIPS32 pass as N64
    hardware readiness.
- [x] Make the userland compiler selectable.
  - Default remains GCC until explicitly changed.
  - `MIPS_ROOTFS_COMPILER=gcc|pcc` selects the shared Malta/Malta64 rootfs
    compiler mode.
  - `N64_USERLAND_COMPILER=gcc|pcc` selects the direct N64 userland compiler
    mode.
  - These are aliases for the same compiler choice at different make entry
    points.  If only one is set, the other entry point inherits it; if both
    are set and conflict, make aborts.
  - PCC mode keeps the kernel, stage0, and target binary tools on the existing
    ReBSD/GCC/a.out flow.
- [x] Decide long-term runtime policy for PCC support libraries.
  - Keep `libpcc.a` staged as PCC's compiler runtime. In PCC-target builds,
    libc is built without the overlapping compiler helper symbols and the
    canonical implementations live in `libpcc.a`: `__divdi3`, `__moddi3`,
    `__udivdi3`, `__umoddi3`, `__fixdfdi`, `__floatdidf`, and the 64-bit
    shift helpers.
  - Legacy GCC-only rootfs links still use libc as their compiler-runtime
    fallback until that link path is migrated to an explicit runtime library.
  - `libpcc.a` still carries compiler-private helpers such as `__muldi3`,
    compare helpers, stack protector support, and remaining conversion
    helpers.
  - Future work: optimize the hot MIPS helper routines in assembly after the
    C implementations are stable on Malta64 and N64.
  - Public C/POSIX/BSD compatibility functions must live in ReBSD libc or
    libm, not in `libpcc.a`. If PCC-built userland exposes missing standard
    or compatibility symbols, add them to the system library that owns that
    API.
  - When closing libc/libm gaps, add the related function family or interface
    surface together instead of growing the ABI as disconnected one-off
    stubs.
  - Do not stage `libpccsoftfloat.a` for current hard-float Malta/N64 builds.
    Add it only if a future no-FPU target or concrete test failure proves it
    is needed.
  - Verification on 2026-07-04 after the helper ownership cleanup:
    `nm libc.a` has no public `T` definitions for the overlapping compiler
    helpers in PCC-target runtime builds, `nm libpcc.a` owns those symbols,
    and both Malta64/R4000 and Malta pass `/root/pcc-smoke-all.sh` with
    `CCOM_STRESS_COUNT=20`, `LINPACK_MIN_SECONDS=1`, and
    `PCC_SMOKE_ALL_RC:0`.
  - N64 hardware verification on 2026-07-04: after rebuilding the PCC rootfs,
    two full `/root/pcc-smoke-all.sh` runs passed with
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`; `ccom-stress`
    completed 100 iterations.  The earlier one-off
    `as: pcc-smoke.s, 4: bad instruction` failure did not reproduce, and the
    smoke script now prints the assembler input head if that path fails again.
- [x] Defer C++ and `pcc-libs/csu` integration to future work.
  - Current flat ctor/dtor ranges are enough for C constructor/destructor
    attributes.
  - Full C++ needs a startup policy for `crtbegin.o`/`crtend.o`, sentinel
    entries, priorities, destructor ordering, and partial links.
  - Do not install `/usr/bin/p++` or `/usr/libexec/pcc/cxxcom` into any default
    rootfs until a future C++ phase has a real compile/link/runtime gate.
  - Current imported `cxxcom` is not ready: the diagnostic `p++` driver does
    not find the prefixed `mips-rebsd-cxxcom`, successful direct `cxxcom`
    output still emits `.file` directives rejected by ReBSD `as`, and real C++
    features such as `extern "C"`, `bool`, references, overloads, structs,
    classes, constructors, member functions, and templates fail in the
    frontend.  `new`/`delete` can emit calls, but they need a missing C++
    runtime (`_Znwm`/`_ZdlPv`).
- [x] Defer TLS and shared libraries to future work.
  - TLS remains rejected by the current static ReBSD/MIPS target and blocks
    only `pcclist/init004`.
  - Shared libraries/PIC remain out of scope for static a.out and block only
    `misc/shlib3`.
  - Keep both as expected-fail policy cases until ReBSD grows a concrete TLS
    or shared-library ABI/runtime plan.
- [x] Add or keep target-aware handling for architecture-specific upstream
  tests.
  - `gcccompat/typeof001` is x86 inline asm, not a MIPS C frontend failure.
  - `sys/mips/tools/pcc-regress.py` now keeps the expected compile/link
    failures in a target-aware policy table.  For `mips-rebsd`, the policy
    covers only the x86-inline-asm test, static a.out shared-library/PIC
    rejection, and static a.out TLS rejection.
- [x] Close remaining test-coverage gaps.
  - Keep expanding strict PCC runtime coverage as backend issues are fixed.
  - Covered now by `/root/libc-abi-smoke.sh`: libc-heavy varargs,
    structs/unions, qsort callbacks, `setjmp`/`longjmp`, stdio file I/O, and
    key pointer arithmetic cases.
  - Covered now by `/root/libpcc-helper-smoke.sh`: direct `libpcc.a`
    compiler-runtime helper calls for 64-bit shifts, signed and unsigned
    division/modulo, multiplication, and double/unsigned-double conversion
    helpers.  The smoke runs through both `/usr/bin/cc` and `/usr/bin/pcc`
    and is included in `/root/pcc-smoke-all.sh`.
  - Covered now by `/root/utility-workload-smoke.sh`: real ReBSD utility
    rebuilds for `basename`, `sum`, and `size` from staged `src/cmd` sources,
    through both `/usr/bin/cc` and `/usr/bin/pcc`.  This covers normal
    utility source, stdio file I/O, `getopt`, multi-source links, and a.out
    header parsing through `aoutio.c`.
  - Covered now by `/root/wide-policy-smoke.sh`: ReBSD's current 16-bit
    `wchar_t`, ASCII-only byte/wide conversion contract, non-ASCII rejection,
    wide string/memory helpers, and wide numeric parsers through both native
    compiler driver names.
  - Covered now by `/root/alias-stress-smoke.sh`: larger pointer/aliasing
    stress, nested structs/unions, function pointer dispatch, struct-by-value
    copies, and overlapping struct `memmove()`.
  - Covered now by `/root/make-workload-smoke.sh`: an additional shell build
    workload with two static libraries, dependent `-L/-l` ordering, `nm`,
    `size`, archive probing, `make clean`, and rebuild checks.
- [x] Make `fsutil` fail hard on rootfs population errors.
  - The first 4 MB N64 PCC debug rootfs exposed `inode_alloc: out of in-core
    inodes` and missing file/device creation while `fsutil --new` still
    returned success and `fsutil --check` passed the truncated image.
  - `fsutil --new ... --manifest` now propagates file, directory, device,
    hardlink, symlink, and final sync failures through `add_contents()` and
    returns non-zero instead of publishing a silently truncated image.
  - `fsutil --add` now propagates `add_object()` and final sync failures too.
  - Verified with missing-file and inode-exhaustion negative tests
    (`FSUTIL_*_RC:255`), a positive image plus `fsutil --check`, Malta
    `MIPS_ROOTFS_COMPILER=pcc rootfs.img`, and N64
    `N64_USERLAND_COMPILER=pcc kernel.z64 preflight.z64`.
- [x] Replace temporary compatibility stubs with real policy.
  - Core wide-char and wide-stdio APIs now live in libc as a coherent
    ASCII/byte-oriented ReBSD wide ABI surface.
  - C99 floating-point classification and NaN constructor APIs now live in
    libc/libm as a supported ReBSD/PCC math surface.
  - Move stubbed or missing public compatibility functions into libc or libm,
    together with their companion declarations and closely related functions,
    so ReBSD does not accumulate partial API fragments.
  - Decide whether ReBSD will grow locale-aware multibyte/wide semantics, or
    explicitly document the current single-byte execution character policy.
  - `sys/include/cdefs.h` now has an explicit ReBSD/PCC compatibility surface
    and a target-side native smoke gate.  Keep future additions as named policy
    rather than adding silent catch-all macros.
  - `docs/PCC_PORTING_NOTES.md` now records the compatibility ownership
    policy: compiler-runtime helpers stay in `libpcc.a`, public C/POSIX/BSD
    and math APIs live in libc/libm, and locale/C++/TLS/shared-library
    semantics must not be hidden behind placeholder functions.
  - `/root/wide-policy-smoke.sh` is now part of `/root/pcc-smoke-all.sh`, so
    the current ASCII-only wide-char policy is a runtime contract instead of
    documentation only.
- [x] Document the final ReBSD/PCC target details and clean up build artifacts.
  - `docs/PCC_PORTING_NOTES.md` now records the supported C target contract,
    native rootfs layout, runtime library policy, validation gates, and
    intentionally out-of-scope C++/TLS/shared-library work.
  - `.gitignore` coverage now keeps the generated native PCC build trees,
    Malta64 stages, N64 PCC manifests/stamps, command binaries, Python caches,
    and retroforth generated images out of git unless they are tracked source
    inputs.

## Phase 3: Native PCC in Malta Rootfs

- [x] Package PCC native C driver and internal passes into the rootfs.
  - Public commands: `/usr/bin/cc`, `/usr/bin/pcc`, `/usr/bin/cpp`.
  - Internal C stages: `/usr/libexec/pcc/cpp`, `/usr/libexec/pcc/ccom`.
  - Headers: `/usr/include`.
  - Libraries/start files: `/usr/lib`.
- [x] Replace the legacy rootfs compiler stack after native C PCC is packaged.
  - Remove the old `cpp`, legacy `cc`/`pcc` alias, `lcc`/`lccom`, and
    `smallc`/`smlrc` from the default rootfs install profile.
  - Keep ReBSD `as`, `ld`, `ar`, and `ranlib`; imported PCC must continue to
    use the native a.out toolchain.
  - ReBSD rootfs now keeps a native C compiler by installing imported PCC as
    `/usr/bin/cc`, `/usr/bin/pcc`, and `/usr/bin/cpp` before removing the
    legacy compiler commands.
- [x] Decide the first ReBSD policy for `src/dev/pcc/pcc-libs`.
  - Current policy: port and stage `libpcc.a` as the PCC compiler runtime.
  - Do not duplicate `divdi3`/`moddi3`/`udivdi3`/`umoddi3`-class helpers
    between PCC-target libc and target `libpcc.a`; they live in `libpcc.a`.
    Legacy GCC-only links keep libc fallback helpers until that path is
    migrated separately.
  - Public C/POSIX/BSD compatibility routines and missing libc-era helpers go
    into ReBSD libc or libm, not `libpcc.a`.
  - Add related libc/libm functions as coherent API groups where practical,
    rather than leaving isolated compatibility stubs.
  - Future work: add MIPS assembly implementations for selected hot helpers
    once the C helper set is proven correct.
  - Decide only if tests prove it needed: stage `libpccsoftfloat.a`.
  - Verification on 2026-07-04: after rebuilding `libc.a` and `libpcc.a` with
    this split, Malta64/R4000 and ordinary Malta both pass
    `/root/pcc-smoke-all.sh` with zero failures.
  - N64 hardware also passes the same rootfs smoke suite with
    `PCC_SMOKE_ALL_RC:0`.
  - The rootfs smoke suite now includes `/root/libpcc-helper-smoke.sh`, which
    calls the staged `libpcc.a` helper ABI directly instead of relying only on
    compiler-generated calls.
  - `pcc-libs/csu` is still not staged by default. ReBSD now has safe flat
    linker-defined ctor/dtor ranges for C constructor/destructor attributes,
    but `crtbegin.o`/`crtend.o` need a separate C++ startup policy before they
    are enabled.
  - Keep hardware-FPU Malta/N64 builds free of unnecessary soft-float runtime
    dependencies.
- [x] Boot Malta with native C PCC installed.
- [x] Compile, assemble, link, and run C programs inside Malta.
- [x] Run the full PCC Malta test gate.
  - `make -C sys/mips native-pcc-regress-runtime` passed on Malta QEMU.
  - Result: `NATIVE_PCC_REGRESS_TOTAL:317`,
    `NATIVE_PCC_REGRESS_COMPILE_PASS:287`,
    `NATIVE_PCC_REGRESS_EXPECTED_COMPILE_FAIL:30`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_COMPILE_FAIL:0`,
    `NATIVE_PCC_REGRESS_RUNTIME_TOTAL:277`,
    `NATIVE_PCC_REGRESS_RUNTIME_PASS:277`,
    `NATIVE_PCC_REGRESS_UNEXPECTED_RUNTIME_FAIL:0`,
    `NATIVE_PCC_REGRESS_RC:0`.

## Temporary Compatibility Stubs To Close

- [x] Audit and eliminate diagnostic workarounds rather than promoting them
  into normal builds.
  - Real fixes that should stay: absolute native PCC tool paths for
    `/usr/bin/as` and `/usr/bin/ld`, `TMPDIR`-aware compiler temporaries,
    correct compiler-driver child wait status handling, compiler diagnostics
    on stderr, `sbrk()` returning `(void *)-1` with `ENOMEM`, and the
    FCSR-safe ReBSD/MIPS `DEBUGFP` oracle.
  - Diagnostic-only paths that must not become the normal system contract:
    staging the N64 PCC C runner as `/bin/sh`, the shell-stub sleep path,
    mounting `/var` with the explicit `/sbin/mount -o rw /dev/ram0 /var`
    form to bypass the crashing `/sbin/mount /var` path, and the
    `N64_DEBUG_UART_ONLY` no-op `usbnetattach()`/`usbnpoll()` compatibility
    symbols.
  - Closed: the real `/sbin/mount /var` path is fixed in libc fstab parsing;
    an explicit Malta64 runtime check now passes `umount /var`, `mkfs
    /dev/ram0`, and `/sbin/mount /var` with `MOUNT_VAR_RC:0`.
  - Closed: PCC-specific K&R declaration switches were removed from public
    headers. `include/unistd.h` and `sys/include/signal.h` no longer hide
    typed prototypes behind `#ifdef __PCC__`; the PCC-built N64 rootfs
    rebuilds with normal prototypes, so future failures must be fixed in PCC
    or in the exact public type declarations, not by reverting to empty
    argument lists.
  - Verification after the header/fstab cleanup: `make -C sys/mips/n64
    N64_USERLAND_COMPILER=pcc rootfs.stage/.userland.pcc` passes; full native
    PCC regressions with `MIPS_ROOTFS_COMPILER=pcc` pass on both
    `BOARD=malta64` and `BOARD=malta` with 317 total, 287 compile-pass, 30
    expected compile-fail, 0 unexpected compile-fail, and 277/277 runtime
    pass.
  - Closed: the PCC-built shell/login/redirection path was fixed as a real
    MIPS relocation bug in ReBSD `as` (`RHIGH16`/`RHIGH16S` addends), restoring
    normal N64 login and repeated shell startup without shell-runner
    workarounds in the normal PCC rootfs.
  - Closed: `N64_DEBUG_UART_ONLY=1` now switches to a dedicated
    `ioconf.debug-uart.o` and filters out `if_usbn.o`; the old no-op
    `usbnetattach()`/`usbnpoll()` compatibility branch was removed from
    `if_usbn.c`.
  - Verification: `make -C sys/mips/n64 N64_DEBUG_UART_ONLY=1
    N64_USERLAND_COMPILER=pcc pcc-debug.z64` passes, the debug kernel symbol
    table contains no `usbnetattach`/`usbnpoll`, and the normal
    `make -C sys/mips/n64 N64_USERLAND_COMPILER=pcc kernel.z64 preflight.z64`
    path still links the real USBNet symbols.
  - Real N64 hardware validation after this cleanup passed.
- [x] Re-run the N64 native PCC debug image after absolute native tool paths,
  native FPU/FCSR probes, PCC FP-folding probes, and shell-stub sleep fix.
  - The old `manual__min` driver-stack failure is no longer present in the
    latest supplied hardware log: `manual__min` passes `cc -v -S`, direct
    `ccom -v`, direct `ccom`, direct `/usr/bin/as`, and `aout -d`.
  - The first valid log block has exactly six failures: three driver
    environment/tool lookup failures (`jira__PCC-70`, `jira__PCC-77`,
    `jira__PCC-152`) and three direct `ccom` signal-8 failures
    (`c99__arith002`, `c99__arith003`, `jira__PCC-185`).
  - The driver failures are fixed in the new image by baking absolute
    `/usr/bin/as` and `/usr/bin/ld` into the native PCC driver and teaching
    `find_file()` to return absolute tools directly.
  - The boot runner now avoids the crashing `/sbin/mount /var` shorthand and
    mounts `/var` with the explicit device/path form before setting up
    `/var/tmp`; the shorthand mount path remains a separate regression to
    debug.
  - The latest hardware log showed the `ccom` failures are concentrated in
    PCC FP folding rather than the driver, assembler, linker, `/var`, or rootfs
    setup.  `fp__builtin_inf` faults in direct `ccom` with signal 11;
    `c99__arith002`, `c99__arith003`, and `jira__PCC-185` fault in direct
    `ccom` with signal 8.
  - PCC softfloat `DEBUGFP` now guards its native-FPU oracle on MIPS by saving
    FCSR, clearing exception flags/enables/cause bits around oracle
    expressions, and restoring FCSR afterwards.  This must be verified on real
    N64 hardware because default Malta QEMU does not reproduce the VR4300 FCSR
    behavior.
  - The 2026-07-03 N64 UART log after the FCSR-safe oracle confirmed
    `fp__builtin_inf` now passes through `cc -S`, direct `ccom`, direct `as`,
    and `aout -d`.
  - The remaining fresh N64 failures in that log are no longer the same direct
    softfloat/FCSR crashes: `manual__min` and `fp__nan_zero` fail only in the
    full driver path where `/usr/bin/cc -v -S` feeds `ccom` from
    `/tmp/ctm.*`; the saved preprocessed `.i` file under
    `/var/tmp/n64-pcc-debug` compiles with direct `ccom`.
  - The native PCC driver now makes `gettmp()` honor `TMPDIR` instead of
    hardcoding `/tmp/ctm.XXXXXX`.
  - The N64 debug runner now prints raw FPU child wait status, preserves
    driver temp files with `-X` after a failed `cc -S`, dumps the resulting
    `/tmp/ctm.*` files, checks direct `ccom` on a copied `/tmp` input, and
    reruns the same compile with `TMPDIR=/var/tmp/n64-pcc-debug` as an A/B
    diagnostic.  This is intended to distinguish bad driver temp-file content,
    `/tmp` symlink/ramfs behavior, and `ccom` input-path sensitivity on real
    N64 hardware.
  - The separate `sys/mips/malta64` R4000 gate now provides usable QEMU
    evidence: `make -C sys/mips BOARD=malta64 native-pcc-regress-runtime
    MIPS_ROOTFS_COMPILER=pcc` boots with `qemu-system-mips64 -M malta -cpu
    R4000 -m 32M -nographic` and passes 317 total checks, 287 compile-pass,
    30 expected compile-fail, 0 unexpected compile-fail, and 277/277 runtime
    pass.
  - The ordinary Malta native PCC regression also still passes after the FCSR
    and driver-temp changes: 317 total, 287 compile-pass, 30 expected
    compile-fail, 0 unexpected compile-fail, 277/277 runtime pass.
  - The R4000/Malta64 pass does not reproduce the intermittent N64 `ccom`
    bad-address faults, so it is a regression gate, not a substitute for real
    N64 hardware validation.
  - Rebuilt `sys/mips/n64/pcc-debug.z64` on 2026-07-03 from a clean N64
    `pcc-debug-image` build after the Malta64 run.  The debug rootfs remains
    6144 KB with 322 files, 3222 used blocks, and 2897 free blocks.
  - The shell-stub path now sleeps forever for `argv0="sh"` and `argv0="-"`,
    so the next hardware log should not contain repeated test passes after
    `N64_PCC_DEBUG_RC_END`.
  - The next supplied N64 hardware log from `/Users/sash/pcc-debug.log`
    contains three complete full diagnostic passes from this image.  Each pass
    starts with `N64_PCC_DEBUG_RC_BEGIN`, formats and mounts the 2 MB `/var`
    ramdisk, reports clean FPU/FCSR probes, passes all manual PCC pipeline
    probes (`manual__min`, `fp__div_zero`, `fp__nan_zero`,
    `fp__builtin_inf`, `fp__builtin_nan`, `fp__isnan_call`), passes
    `c99__arith002`, `c99__arith003`, `jira__PCC-70`, `jira__PCC-77`,
    `jira__PCC-152`, and `jira__PCC-185`, and ends with
    `N64_PCC_DEBUG_END 0` plus `N64_PCC_DEBUG_RUNNER_RC 0`.
  - The same hardware log has no `bad address`, no `N64_PCC_SIGNAL`, no EMT
    trap, no compile failure, and no runtime failure in any complete
    diagnostic pass.  The previous `usbn0` attach line is gone, confirming the
    UART-only debug kernel is not initializing the N64cart USB Ethernet path.
  - One partial block immediately after a user interrupt (`^C`) reports
    `N64_PCC_DEBUG_MOUNT_DIRECT_RC 1` and skips the runner because `/var` was
    already in an interrupted/re-entry state.  Do not treat that partial block
    as compiler evidence.
- [x] Run the expanded N64 PCC debug image on real hardware.
  - The boot-time C runner now reports `N64_PCC_DEBUG_MODE all` and executes
    the staged extended JIRA regression subset after the already-green primary
    hardware probes.
  - Clean local `make -C sys/mips/n64 pcc-debug-image` completed after this
    runner change.  `fsutil --check` reports the same 6144 KB debug rootfs,
    322 files, 3222 used blocks, and 2897 free blocks; `strings` on
    `sys/mips/n64/pcc-debug.z64` confirms `N64_PCC_DEBUG_MODE` and the
    extended test names are present in the image.
  - Malta64/R4000 target-side native PCC regression was re-run after this
    runner change with `MIPS_ROOTFS_COMPILER=pcc`.  The QEMU command used
    `qemu-system-mips64 -M malta -cpu R4000 -m 32M -nographic`; the summary
    in `/private/tmp/rebsd-malta64-native-pcc-regress-after-n64-debug-all.log`
    is 317 total, 287 compile-pass, 30 expected compile-fail, 0 unexpected
    compile-fail, 277/277 runtime pass, 0 unexpected runtime failures, and
    `NATIVE_PCC_REGRESS_RC:0`.
  - The next supplied N64 hardware log from `/Users/sash/pcc-debug.log`
    contains two complete expanded runs.  Each run reports
    `N64_PCC_DEBUG_MODE all`, executes 45 compile/run tests, and ends with
    `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and
    `N64_PCC_DEBUG_RC_END`.
  - The same log has no non-zero `N64_PCC_*_RC` status, no
    `N64_PCC_SIGNAL`, no `FAIL`, no `bad address`, no EMT trap, and no panic.
    The last test in each expanded run is `jira__PCC-204`, which compiles and
    runs successfully.
  - Keep the generated shell scripts staged only for comparison; the hardware
    gate must continue using direct `fork`/`exec` from the C runner until the
    PCC-built shell regressions are separately fixed.
- [x] Restore the normal N64 PCC rootfs image path after the debug gate.
  - The tracked normal N64 `/etc/rc` now mounts `/var` through `/etc/fstab`
    with `mount /var`; the explicit `/sbin/mount -o rw /dev/ram0 /var` path
    remains diagnostic-only for the PCC debug image.
  - The normal PCC rootfs now stages `/root/pcc-smoke-all.sh`, a single
    UART-friendly runner for the manual hardware pass. It runs the native PCC,
    cc/pcc driver, type, `long long`, ABI, libc string, shell, repeated
    `ccom`, Linpack, and quick runtime stress smokes without network tests.
  - Normal N64 PCC images now stage prebuilt `/root/linpack-gcc` and
    `/root/linpack-pcc` before rootfs packaging, so `/root/linpack-smoke.sh`
    no longer fails with a missing benchmark binary.
  - The 2026-07-04 N64 hardware `/root/pcc-smoke-all.sh` log showed the
    restored normal PCC rootfs booting through login and `/var` mount.  All
    smokes passed except `cc-pcc`: `/usr/bin/cc --sysroot / -v ...` handed
    `--sysroot=/` to ReBSD `ld`, and the target linker rejected it as
    `unknown flag`.
  - The active PCC driver in `src/dev/pcc/pcc/cc/cc/cc.c` already accepts
    both `--sysroot DIR` and `--sysroot=DIR`; it now treats an unset target
    sysroot as `/` and joins `=/usr/lib` style paths without producing
    `//usr/lib`.  ReBSD `ld` now accepts `--sysroot[=DIR]`, defaults to `/`,
    and applies it only to standard library directories, leaving explicit
    `-L` paths unchanged.
  - Clean local build completed on 2026-07-03:
    `make -C sys/mips/n64 clean` followed by
    `make -C sys/mips/n64 N64_USERLAND_COMPILER=pcc kernel.z64 preflight.z64`.
  - Clean local build repeated on 2026-07-04 after the target sysroot fix:
    `rootfs.img`, `kernel.z64`, and `preflight.z64` all rebuilt successfully.
  - The normal PCC rootfs is not the shell-stub/debug-runner image: staged
    `/bin/sh`, `/sbin/init`, `/libexec/getty`, and `/bin/login` are real
    userland binaries, `.userland.pcc` is present, and no `N64_PCC`/debug
    marker strings are present in the normal staged rootfs.
  - Latest `fsutil --check` for the generated normal `rootfs.img` reports
    628 files, 12766 used blocks, and 3553 free blocks in the 16 MB image.
  - Hardware re-validation after the target sysroot fix passed on real N64.
    The supplied 2026-07-04 UART log contains two complete
    `/root/pcc-smoke-all.sh` runs from the normal PCC rootfs; both end with
    `PCC_SMOKE_ALL_FAILURES 0`, `PCC_SMOKE_ALL_OK`, and
    `PCC_SMOKE_ALL_RC:0`.
  - The same N64 log confirms the target PCC driver now passes `--sysroot=/`
    to ReBSD `ld`, the target linker accepts it, and the library search path
    is normalized as `-L/usr/lib/` rather than `-L//usr/lib`.
  - N64 Linpack smoke in that log passes for both compilers at array size 120:
    GCC reports about 4.0 MFLOPS and PCC about 2.5 MFLOPS on the real
    hardware smoke configuration.
- [x] Fix PCC MIPS unsigned narrow memory loads exposed by DHCP.
  - PCC-generated `dhclient` rejected valid DHCP replies because the options
    cookie byte `130` was loaded as a signed `char` value.  The MIPS backend
    now emits `lbu` for `unsigned char` and `lhu` for `unsigned short`, while
    keeping `lb`/`lh` for signed types.
  - Added strict regression `misc/ucharload001` for volatile
    `unsigned char` and `unsigned short` loads.
  - Added `/root/dhcp-recv-smoke.sh` plus UDP input/output counters in
    `netstat -s` so DHCP receive path failures are visible on Malta and N64.
  - Verified with host-cross PCC assembly (`lbu`/`lhu` only for unsigned
    loads), Malta/PCC `PCC_SMOKE_ALL_RC:0`, Malta/PCC
    `DHCP_RECV_RC:0` and `DHCP_DNS_RC:0`, and Malta64/R4000/PCC
    `NATIVE_PCC_SMOKE_RC:0` plus `DHCP_DNS_RC:0`.
  - Clean N64 PCC image build after the fix completed with
    `make -C sys/mips/n64 clean` followed by
    `make -C sys/mips/n64 N64_USERLAND_COMPILER=pcc kernel.z64
    preflight.z64`.  The generated rootfs is marked `.userland.pcc`, contains
    `/root/dhcp-recv-smoke.sh`, `/root/linpack-gcc`, and `/root/linpack-pcc`,
    and the freshly installed N64 host PCC also emits `lbu`/`lhu` for
    `misc/ucharload001`.
  - Real N64 hardware validation after this build passed
    `/root/usbn-dhcp-smoke.sh`: `dhclient` received OFFER and ACK from
    `192.168.2.1`, configured `usbn0` as `192.168.2.3/24`, installed the
    default route and resolver, and pinged both the DHCP router and external
    hosts (`ya.ru`) with 0% packet loss.
- [x] Add a libc regression for `printf(stdout)` followed by
  `freopen(stdout, ...)` and later `malloc()` traffic.
  - The `ccom -v` crash path strongly suggested this stream transition can
    corrupt or expose corruption in the old malloc/stdio interaction on N64.
  - Even though PCC should not print compiler diagnostics to the assembly
    output stream, libc should still survive this valid stdio sequence.
  - Added to `/root/native-pcc-smoke.sh` as step 7: compile and run a target
    PCC binary that prints to stdout, redirects stdout with `freopen()`, then
    stresses `malloc()`/`free()` traffic and verifies both output streams.
  - Verified on PCC-built rootfs with Malta and Malta64/R4000:
    `NATIVE_PCC_SMOKE_RC:0`.
- [ ] Future: implement the full C++ ctor/dtor startup model.
  - Current ReBSD startup walks flat linker-defined ctor/dtor ranges and is
    enough for PCC C constructor/destructor attributes.
  - It does not yet enable PCC `crtbegin.o`/`crtend.o`, sentinel entries,
    constructor priorities, or a C++ global initialization/destruction policy.
  - Partial links with `ld -r` must either preserve ReBSD ctor/dtor metadata or
    reject unsupported ctor/dtor inputs before this is treated as a complete
    C++ runtime model.
- [x] Keep the PCC C++ frontend disabled before publishing `p++`.
  - The native build does not compile or publish `cxxcom` by default. A
    diagnostic `--with-cxx` build can still be used while bringing C++ up.
  - Re-enable `/usr/bin/p++` only in a future C++ phase, together with a
    target-side compile/link/run gate and the C++ ctor/dtor runtime policy
    above.
- [x] Formalize the N64 UART-only debug video policy.
  - `N64_DEBUG_UART_ONLY=1` filters out `video.o`, `video_console.o`, `si.o`,
    `joybus.o`, `if_usbn.o`, `n64cart_usbnet.o`, `n64cart_usbecm.o`, and
    `n64usbnet_proto.o`; it removes `VIDEO_ENABLED`, `INPUT_ENABLED`, and
    `USBNET_ENABLED` from kernel flags so the VI/framebuffer path and N64cart
    USB Ethernet backend are not initialized during PCC hardware diagnosis.
  - `N64_DEBUG_UART_ONLY=1` uses `ioconf.debug-uart.o`, whose service list has
    no USBNet attach entry.  `if_usbn.c` no longer carries no-op debug
    `usbnetattach()`/`usbnpoll()` symbols.
  - Policy: this remains a diagnostic build mode for PCC hardware work, not a
    separate supported N64 board profile.
  - Verified locally with `make -C sys/mips/n64 N64_DEBUG_UART_ONLY=1
    N64_USERLAND_COMPILER=pcc pcc-debug.z64` and normal
    `make -C sys/mips/n64 N64_USERLAND_COMPILER=pcc kernel.z64 preflight.z64`;
    the operator also reported that the updated image passes on real N64
    hardware.
  - Basic C++ smoke attempts fail in the frontend before reaching ReBSD `as` or
    `ld`: constructor syntax is rejected, and a class/member-function compile
    fails with `compiler error: cdope missing op 93`.
  - Re-enable `/usr/bin/p++` only together with a target-side compile/link/run
    gate and the C++ ctor/dtor runtime policy above.
- [x] Expand `include/wchar.h` beyond the minimal ReBSD wide-char ABI.
  - `wchar_t` remains the ReBSD/PCC 16-bit ABI.
  - `wchar.h` now declares a coherent libc-owned core: wide stdio entry
    points, `wcs*`, `wmem*`, `wcsto*`, and byte-oriented `mbr*`/`wcrtomb`
    conversion helpers.
  - `src/libc/string/wchar.c` implements the wide string, memory, conversion,
    and numeric parsing helpers in libc rather than `libpcc.a`.
- [x] Replace the one-off `src/libc/stdio/wprintf.c` stub with a libc
  wide-stdio family.
  - Implemented `fwide`, `fputwc`, `putwc`, `putwchar`, `fputws`,
    `fwprintf`, `vfwprintf`, `vwprintf`, `wprintf`, `swprintf`, and
    `vswprintf`.
  - The formatter handles narrow and wide `%s`/`%c`, width/precision/flags,
    integer formats, pointer formats, ReBSD `%b`, and float formats by
    routing scalar formatting through the existing libc printf machinery.
  - Current policy remains single-byte/ASCII-oriented. Full locale-aware
    multibyte conversion and non-ASCII wide I/O are still future policy work,
    not `libpcc` work.
- [x] Decide and document the full locale/multibyte policy for wide-char APIs.
  - Current `mbr*`/`wcrtomb`/`mbstowcs`/`wcstombs` helpers intentionally
    implement the ReBSD single-byte ASCII execution character model.
  - If ReBSD later adds locale or UTF-8 semantics, update the whole conversion
    family and error policy together instead of changing isolated functions.
  - `docs/PCC_PORTING_NOTES.md` now states this as the current C milestone
    contract, and `/root/wide-policy-smoke.sh` verifies ASCII conversion plus
    non-ASCII rejection with both `cc` and `pcc`.
- [x] Audit and complete `sys/include/cdefs.h`.
  - Current supported ReBSD/PCC surface includes declaration wrappers, `__P`,
    BSD string/concatenation helpers, compiler attribute shims used by the tree,
    branch prediction helpers, `__arraycount`, version predicates, and
    `__RCSID`/`__COPYRIGHT`/`__KERNEL_RCSID`/`__IDSTRING`.
  - Unsupported semantics are still left out deliberately: C++ startup,
    TLS-specific behavior, shared-library/PIC policy, and locale/multibyte
    behavior remain separate TODO items instead of being hidden behind header
    compatibility macros.
  - `/root/native-pcc-smoke.sh` now builds and runs a `sys/cdefs.h`
    compatibility program with target `cc`.
- [x] Turn the C99 math additions in `include/math.h` into a supported
  libc/libm contract.
  - `include/math.h` now exposes the C99 `FP_*` classes, `fpclassify`,
    `isfinite`, `isnormal`, `isnan`, `isinf`, `HUGE_VAL*`, `INFINITY`, `NAN`,
    `nan*`, and `signbit` as one coherent ReBSD/PCC surface.
  - Classification, finite/normal tests, signed infinity tests, fallback
    signbit helpers, and `nan*` constructors live in libc
    (`src/libc/gen/mathclass.c`, `src/libc/gen/nan.c`) because normal userland
    can need those APIs without explicitly linking `-lm`.
  - `signbit` still uses the raw compiler builtin for GCC/PCC in `math.h`;
    wrapping the builtin in `!= 0` previously exposed the PCC comparison
    inversion tracked in `jira/PCC-382`.  The libc helper names deliberately
    avoid the `libpcc.a` `__signbit*` compiler-runtime ABI.
  - `/root/math-class-smoke.sh` is part of `/root/pcc-smoke-all.sh` and
    builds/runs the classification contract with both `cc` and `pcc`.
  - Verified after cleaning generated port build stages with N64 PCC image
    build, Malta PCC rootfs/full smoke, and Malta64/R4000 PCC rootfs/full
    smoke.

## Phase 4: Selectable Compiler Mode

- [x] Make userland compiler selection explicit after Malta is green.
  - Default remains GCC until intentionally changed.
  - Supported values are `gcc` and `pcc`.
  - Shared MIPS ports use `MIPS_ROOTFS_COMPILER`.
  - Direct N64 builds use `N64_USERLAND_COMPILER`.
  - The two names are aliases for the same selected compiler mode and are
    validated for conflicts.
- [x] Add documentation for the compiler selector.
  - `docs/TOOLCHAIN.md` documents the policy, supported values, and common
    Malta/Malta64/N64 command forms.
  - `docs/PCC_PORTING_NOTES.md` records PCC as a supported opt-in C userland
    compiler while leaving C++/TLS/shared-library policy out of the C gate.
- [x] Build full userland with PCC on Malta.
- [x] Re-run Malta smoke and regression tests with PCC userland.
  - Native PCC regressions pass with 317 total checks, 287 compile-pass,
    30 expected compile-fail, 0 unexpected compile-fail, and 277/277 runtime
    pass.
  - Linpack smoke passes with `LINPACK_RC:0`; latest default-size Malta
    numbers are GCC about 14.7 MFLOPS and PCC about 13.4 MFLOPS.
- [x] Re-run Malta64/R4000 smoke and regression tests with PCC userland.
  - Native PCC regressions pass with the same 317/287/30 and 277/277 runtime
    result under `qemu-system-mips64 -M malta -cpu R4000 -m 32M -nographic`.
  - Linpack smoke passes with `LINPACK_RC:0`; latest default-size Malta64
    numbers are GCC about 14.4 MFLOPS and PCC about 12.2 MFLOPS.
- [x] Build N64 artifacts with the selected compiler mode.
  - `make -C sys/mips/n64 N64_USERLAND_COMPILER=pcc kernel.z64 preflight.z64`
    builds the normal PCC rootfs image.
- [x] Leave N64 hardware validation to the operator.
  - Real N64 hardware validation passed `/root/pcc-smoke-all.sh` with
    `PCC_SMOKE_ALL_RC:0`.
  - N64 Linpack smoke passes for both compilers at array size 120; latest
    hardware numbers are GCC about 4.0 MFLOPS and PCC about 2.6 MFLOPS.

## Phase 5: MIPS/VR4300 PCC Performance Plan

Goal: raise PCC-generated MIPS userland performance, with N64/VR4300 as the
primary target, without turning the MIPS backend into a large pile of
one-off templates.  Keep the backend small by using a target cost model,
compact late scheduling/peephole rules, ABI cleanup, and target-independent
middle-end improvements where the optimization is not inherently MIPS-specific.

Baseline evidence:

- [x] Keep a reproducible GCC/PCC benchmark record for Malta, Malta64/R4000,
  and real N64 hardware.
  - Track Linpack MFLOPS, wall time, assembly line count, `nop` count,
    load/store count, `mult`/`div` count, generated text size, and regression
    status for every performance patch.
  - Current N64 hardware Linpack baseline is about GCC 4.0 MFLOPS versus PCC
    2.6 MFLOPS at array size 120.
  - Added `pcc-perf-baseline` and `sys/mips/tools/mips-asm-counters.py` on
    2026-07-08; this compiles the Linpack smoke source to GCC and PCC assembly
    and records instruction, `nop`, memory, branch/delay-slot, multiply/divide,
    FPU, and size counters as JSON before any codegen change.
  - Malta64/VR4300 baseline:
    - JSON:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - GCC: 1138 instructions, 42 `nop`; PCC: 3687 instructions, 1233 `nop`.
    - PCC/GCC instruction ratio: 3.240; `nop` ratio: 29.357; PCC has 229
      branch/jump delay-slot `nop` instructions.
  - Malta/mips32r2 big-endian baseline:
    - JSON:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
    - GCC: 1082 instructions, 5 `nop`; PCC: 4319 instructions, 1553 `nop`.
    - PCC/GCC instruction ratio: 3.992; `nop` ratio: 310.600; PCC has 229
      branch/jump delay-slot `nop` instructions.
  - MaltaEL/mips32r2 little-endian baseline:
    - JSON:
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
    - GCC: 1082 instructions, 5 `nop`; PCC: 4319 instructions, 1553 `nop`.
    - PCC/GCC instruction ratio: 3.992; `nop` ratio: 310.600; PCC has 229
      branch/jump delay-slot `nop` instructions.
  - Added `pcc-smoke-all-runtime` as the correctness gate for QEMU Malta
    targets.  On 2026-07-08, all three PCC rootfs smoke gates passed with
    `PCC_SMOKE_ALL_RC:0`:
    - Malta64/VR4300:
      `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`.
    - Malta/mips32r2 big-endian:
      `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`.
    - MaltaEL/mips32r2 little-endian:
      `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - The mips32r2 baseline has the same PCC delay-slot `nop` count as VR4300
    and a worse total `nop` ratio, so the first `nop` cleanup and delay-slot
    scheduling work should be shared across VR4300 and mips32r2 rather than
    hidden behind a VR4300-only path.
- [x] Record the VR4300 scheduling facts used by the compiler work.
  - Local reference: `docs/VR4300-Users-Manual.pdf`.  The local
    `docs/JZ4780_pm.pdf` copy is a peripheral module manual and was not used
    for CPU pipeline timing.
  - Branch and jump instructions have one architectural delay slot.
  - Integer load-use hazards normally cost one pipeline cycle when the next
    instruction consumes the loaded value; the hardware interlock makes this a
    performance hazard, not a correctness reason to keep blanket `nop`s.
  - Stores can keep the data cache busy for a following data-cache access; the
    VR4300 documents this as a store delay slot with hardware interlocks.  A
    trailing store `nop` is therefore only useful when it would otherwise hide a
    real following memory access stall.
  - FP compare/computational and `LWC1`/`LDC1` paths have FP bypass limits, so
    FP load/compute latency must be handled by scheduling rather than blanket
    deletion.
  - Integer `mult`/`multu` are about 5 cycles; `div`/`divu` are about
    37 cycles, so strength reduction is high-value on this CPU.
  - Double FP add/sub are about 3 cycles, double multiply about 8 cycles, and
    double divide about 58 cycles; dependent FP use also needs spacing.
  - VR4300 I-cache is 16 KB and D-cache is 8 KB, so code size and spill
    traffic matter on real N64 hardware more than they appear to on Malta.

Backend work, kept small:

- [ ] Add a small MIPS/VR4300 cost and latency description.
  - Put target costs in one place rather than spreading cycle knowledge across
    instruction templates.
  - Keep the first version conservative: load-use, branch delay, store/cache
    hazard awareness, integer multiply/divide costs, and core FP latencies.
  - Make non-VR4300 MIPS profiles keep existing behavior unless explicitly
    selected by `-march`/target defaults.
  - Milestone C3 now centralizes exact FPR read/write sets and the VR4300
    binary add/sub/multiply/divide latencies in the late scheduler.  Integer,
    cache, unary FP, and conversion costs still keep this broader item open.
- [ ] Remove obviously over-conservative unconditional `nop` emission.
  - Start with jump templates that currently emit more than the single
    architectural delay slot.
  - First control-transfer cleanup landed on 2026-07-08: direct `j LL`,
    computed `jr AL`, and hard-float compare branches now keep exactly one
    required delay-slot `nop` instead of emitting an extra unconditional `nop`.
    This deliberately does not touch load-use, multiply/divide, or FPU memory
    padding yet.
  - Post-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from 3687 instructions and 1233
      `nop` to 3619 instructions and 1165 `nop`; PCC/GCC instruction ratio
      improved from 3.240 to 3.180, and `nop` ratio from 29.357 to 27.738.
    - Malta/mips32r2 big-endian PCC Linpack changed from 4319 instructions
      and 1553 `nop` to 4251 instructions and 1485 `nop`; PCC/GCC
      instruction ratio improved from 3.992 to 3.929, and `nop` ratio from
      310.600 to 297.000.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
      The branch/jump delay-slot `nop` counter remains 229 because this patch
      removes only the redundant second `nop`, not the architectural delay
      slot.
  - Post-cleanup QEMU correctness gates passed with `PCC_SMOKE_ALL_RC:0` on
    Malta64/VR4300, Malta/mips32r2 big-endian, and MaltaEL/mips32r2
    little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Store-side cleanup landed on 2026-07-08: scalar stores, soft-float stores,
    hard-float single/double stores, split double stores, and `long long`
    stores no longer append unconditional trailing `nop`s.  This removes only
    store padding backed by the VR4300 store/cache interlock; branch/call delay
    slots, load-use padding, `mfc1`/`mtc1`, and multiply/divide spacing stay
    unchanged until a scheduler can fill or preserve them intentionally.
  - Post-store-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3619 instructions
      and 1165 `nop` to 3461 instructions and 1007 `nop`; total improvement
      from the original baseline is -226 instructions and -226 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 3.041,
      and `nop` ratio from 29.357 to 23.976.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 4251
      instructions and 1485 `nop` to 3984 instructions and 1218 `nop`; total
      improvement from the original baseline is -335 instructions and -335
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.682, and `nop` ratio from 310.600 to 243.600.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
      The branch/jump delay-slot `nop` counter still remains 229 because this
      patch does not fill architectural delay slots.
  - Post-store-cleanup QEMU correctness gates passed with `PCC_SMOKE_ALL_RC:0`
    on Malta64/VR4300, Malta/mips32r2 big-endian, and MaltaEL/mips32r2
    little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Paired-load cleanup landed on 2026-07-08: `long long` loads and split
    hard-float double loads no longer emit an internal `nop` between the two
    independent word loads.  The trailing load-use `nop` after the second load
    is still kept; branch/call delay slots, FP transfer/compute padding, and
    multiply/divide spacing are unchanged.
  - Post-paired-load-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3461 instructions
      and 1007 `nop` to 3448 instructions and 994 `nop`; total improvement
      from the original baseline is -239 instructions and -239 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 3.030,
      and `nop` ratio from 29.357 to 23.667.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3984
      instructions and 1218 `nop` to 3760 instructions and 994 `nop`; total
      improvement from the original baseline is -559 instructions and -559
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.475, and `nop` ratio from 310.600 to 198.800.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
      The branch/jump delay-slot `nop` counter still remains 229 because this
      patch does not fill architectural delay slots.
  - Post-paired-load-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Omit-frame epilogue cleanup landed on 2026-07-08: functions that omit
    `$fp` and have a small positive local frame adjustment now reload `$ra`
    through the old `$sp`, use the local-frame pop as the `$ra` load-use
    spacer, and put the final 16-byte ABI stack pop in the `jr $ra` delay
    slot.  Zero-frame, large-frame, and `$fp` epilogues keep the old explicit
    `nop`; an attempted `$fp` epilogue delay-slot fill was rejected by the
    native assembler during `utility-workload`, so that case stays for the
    compact late scheduler.
  - Post-omit-frame-epilogue-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack stayed at 3448 instructions and 994 `nop`;
      PCC/GCC instruction ratio stayed 3.030, and `nop` ratio stayed 23.667.
    - Malta/mips32r2 big-endian PCC Linpack stayed at 3760 instructions and
      994 `nop`; PCC/GCC instruction ratio stayed 3.475, and `nop` ratio
      stayed 198.800.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
      The Linpack branch/jump delay-slot `nop` counter still remains 229; this
      cleanup affects only small omit-frame return epilogues, not the hot
      Linpack loops covered by the baseline counter.
  - Post-omit-frame-epilogue-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Call delay-slot cleanup landed on 2026-07-08: call templates with an
    existing 16-byte outgoing argument home-area reservation now place that
    `subu $sp,$sp,16` in the `jal`/`jal $25` delay slot.  No-argument
    `UCALL` and no-reserve `USTCALL` templates stay unchanged; this is a
    local template cleanup, not a general scheduler.
  - Post-call-delay-slot-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3448 instructions
      and 994 `nop` to 3403 instructions and 949 `nop`; total improvement
      from the original baseline is -284 instructions and -284 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.990,
      and `nop` ratio from 29.357 to 22.595.  The branch/jump delay-slot
      `nop` counter changed from 229 to 184 for this step.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3760
      instructions and 994 `nop` to 3715 instructions and 949 `nop`; total
      improvement from the original baseline is -604 instructions and -604
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.433, and `nop` ratio from 310.600 to 189.800.  The branch/jump
      delay-slot `nop` counter changed from 229 to 184 for this step.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
  - Post-call-delay-slot-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Callee-saved restore cleanup landed on 2026-07-08: `eoftn()` no longer
    prints a `nop` after each restored permanent register load because the
    epilogue does not consume restored `$sN` values before returning.  `$ra`,
    `$fp`, and stack handling stay unchanged.
  - Post-callee-saved-restore-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3403 instructions
      and 949 `nop` to 3388 instructions and 934 `nop`; total improvement
      from the original baseline is -299 instructions and -299 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.977,
      and `nop` ratio from 29.357 to 22.238.  The branch/jump delay-slot
      `nop` counter remains 184.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3715
      instructions and 949 `nop` to 3700 instructions and 934 `nop`; total
      improvement from the original baseline is -619 instructions and -619
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.420, and `nop` ratio from 310.600 to 186.800.  The branch/jump
      delay-slot `nop` counter remains 184.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
  - Post-callee-saved-restore-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Helper-call delay-slot cleanup landed on 2026-07-08: `starg()`,
    `stasg()`, soft-float/integer helper calls, and the hard-float
    `__truncdfsf2` helper template now place their existing 16-byte outgoing
    argument home-area reservation in the `jal` delay slot.  Ordinary no-arg
    `UCALL` sites stay unchanged; this is still a local emitter/template
    cleanup rather than a general scheduler.
  - Post-helper-call-delay-slot-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack stayed at 3388 instructions and 934 `nop`;
      PCC/GCC instruction ratio stayed 2.977, `nop` ratio stayed 22.238, and
      the branch/jump delay-slot `nop` counter stayed 184.
    - Malta/mips32r2 big-endian PCC Linpack stayed at 3700 instructions and
      934 `nop`; PCC/GCC instruction ratio stayed 3.420, `nop` ratio stayed
      186.800, and the branch/jump delay-slot `nop` counter stayed 184.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
      The hot Linpack loop does not use the touched helper emitters, so this
      step is a correctness/coverage improvement for less common helper paths,
      not a Linpack counter win.
  - Post-helper-call-delay-slot-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Stack-argument memory `FUNARG` cleanup landed on 2026-07-08: direct
    word/pointer, `long long`, and soft-float double argument templates now
    load stack-source operands into scratch registers before adjusting `$sp`,
    then use the stack adjustment itself as the load-use spacer before the
    final argument stores.  This preserves the existing o32 argument layout
    and avoids a broader call-argument scheduler in the backend.
  - Post-stack-argument-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3388 instructions
      and 934 `nop` to 3380 instructions and 926 `nop`; total improvement
      from the original baseline is -307 instructions and -307 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.970,
      and `nop` ratio from 29.357 to 22.048.  The branch/jump delay-slot
      `nop` counter remains 184.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3700
      instructions and 934 `nop` to 3692 instructions and 926 `nop`; total
      improvement from the original baseline is -627 instructions and -627
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.412, and `nop` ratio from 310.600 to 185.200.  The branch/jump
      delay-slot `nop` counter remains 184.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
  - Post-stack-argument-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - A broader direct call-argument load experiment was rejected: marking
    `movearg_32bit()` loads as call-argument producers improved Linpack by
    another 18 instructions and 18 `nop` on mips32r2, but full
    Malta64/VR4300 `pcc-smoke-all-runtime` failed in the self-hosted `types`
    smoke with a native assembler error.  Keep that shape for the future
    compact late scheduler, where final instruction order can be validated
    with explicit dependency checks instead of ad hoc node markers.
  - Narrow unsigned conversion cleanup landed on 2026-07-08: the
    memory-source `short -> unsigned long long` conversion now uses its
    existing independent `move U1,$zero` as the `lhu` load-use spacer instead
    of emitting an explicit `nop`.  This is a one-rule table cleanup and does
    not affect the Linpack hot path: Malta64/VR4300 stayed at 3380
    instructions and 926 `nop`; Malta/mips32r2 big-endian and
    MaltaEL/mips32r2 little-endian stayed at 3692 instructions and 926 `nop`.
  - Post-narrow-conversion-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Long-long compare delay-slot cleanup landed on 2026-07-08: ordered
    64-bit comparisons (`<`, `<=`, `>`, `>=` and unsigned variants) now place
    the next independent `slt`/`sltu` in the previous `bnez` delay slot.
    `==` and `!=` stay unchanged because their next instruction is another
    branch, not a safe delay-slot filler.  This is a local emitter cleanup in
    `twollcomp()`, not a general scheduler.
  - Post-long-long-compare-cleanup `pcc-perf-baseline` assembly counters stayed
    at Malta64/VR4300 3380 instructions and 926 `nop`, and Malta/mips32r2
    big-endian plus MaltaEL/mips32r2 little-endian 3692 instructions and 926
    `nop`.  Linpack does not exercise these ordered 64-bit compare paths, but
    the `long-long`, `long-long-abi`, and workload smoke tests cover the
    transformed code shape.
  - Post-long-long-compare-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Narrow constant arithmetic cleanup landed on 2026-07-08: the MIPS table
    now matches positive power-of-two constants for multiply, unsigned divide
    by power-of-two, and signed divide by positive power-of-two.  This lowers
    those forms to `sll`, `srl`, or a signed trunc-toward-zero bias plus
    `sra` sequence, avoiding `mult`/`div` and HI/LO latency without adding a
    general strength-reduction pass to the backend.
  - Post-constant-arithmetic-cleanup `pcc-perf-baseline` assembly counters:
    Malta64/VR4300 moved from 3380 instructions, 926 `nop`, and 65
    multiply/divide instructions to 3376 instructions, 922 `nop`, and 63
    multiply/divide instructions; the instruction ratio moved from 2.970 to
    2.967 and the `nop` ratio from 22.048 to 21.952.
  - Malta/mips32r2 big-endian and MaltaEL/mips32r2 little-endian moved from
    3692 instructions, 926 `nop`, and 65 multiply/divide instructions to 3688
    instructions, 922 `nop`, and 63 multiply/divide instructions; the
    instruction ratio moved from 3.412 to 3.409 and the `nop` ratio from
    185.200 to 184.400.
  - Post-constant-arithmetic-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
    The in-QEMU Linpack smoke remained green at roughly 13.5-13.7 MFLOPS
    across the three boards.
  - Conservative integer load-delay postpass landed on 2026-07-08: the PCC
    driver now trims selected final-assembly `nop`s after integer loads without
    adding a general scheduler to the MIPS backend.  The pass runs only after
    `ccom` has produced temporary assembly, so user-provided `.s` inputs are
    not rewritten.  It handles only `lw`, `lh`, `lhu`, `lb`, `lbu`, `lwl`, and
    `lwr`; FPU loads stay untouched.  A load-delay `nop` is removed only when
    the following instruction is not another `nop` or integer load, and both
    the following instruction and one more physical instruction do not mention
    the loaded GPR.  The pass never reorders instructions.
  - A broader load-gap trim was rejected: it removed more `nop`s, but full
    target-native smoke exposed unsafe assembler/runtime shapes in `types` and
    `libc-abi`.  Keep load-to-load and FPU-dependent cleanup for the future
    compact late scheduler with explicit dependency modelling.
  - Post-integer-load-postpass `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3376 instructions
      and 922 `nop` to 3241 instructions and 787 `nop`; total improvement from
      the original baseline is -446 instructions and -446 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.848,
      and `nop` ratio from 29.357 to 18.738.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3688
      instructions and 922 `nop` to 3553 instructions and 787 `nop`; total
      improvement from the original baseline is -766 instructions and -766
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.284, and `nop` ratio from 310.600 to 157.400.  Multiply/divide count
      stayed 63.
    - MaltaEL/mips32r2 little-endian matched the big-endian mips32r2 result.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-integer-load-postpass QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Conservative FPU load-delay postpass landed on 2026-07-08: the same
    final-assembly driver pass now also trims selected `nop`s after
    `l.s`, `l.d`, `lwc1`, and `ldc1`.  The VR4300 manual documents CP1 load
    interlocks as performance hazards, not correctness requirements for
    blanket padding, but the rule stays conservative: no reordering, no
    user-provided `.s` rewriting, paired FPR masks for double loads, and
    removal only when the following instruction and one more physical
    instruction do not mention the loaded FPR.  `mtc1`/`mfc1` and dependent
    FPU compute latency are still left for a real late scheduler.
  - Post-FPU-load-postpass `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3241 instructions
      and 787 `nop` to 3160 instructions and 706 `nop`; total improvement
      from the original baseline is -527 instructions and -527 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.777,
      and `nop` ratio from 29.357 to 16.810.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3553
      instructions and 787 `nop` to 3472 instructions and 706 `nop`; total
      improvement from the original baseline is -847 instructions and -847
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.209, and `nop` ratio from 310.600 to 141.200.  Multiply/divide count
      stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3553 instructions and 787 `nop` to 3385 instructions and 619 `nop`;
      total improvement from the original baseline is -934 instructions and
      -934 `nop`.  PCC/GCC instruction ratio improved from the original 3.992
      to 3.128, and `nop` ratio from 310.600 to 123.800.  Multiply/divide
      count stayed 63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-FPU-load-postpass QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Conservative adjacent `move` plus shift fold landed on 2026-07-08: the
    final-assembly driver postpass now folds only physical adjacent
    `move dst,src; sll/srl/sra dst,dst,imm` into `sll/srl/sra dst,src,imm`.
    This removes a dead copy without adding MIPS backend table growth or a
    general scheduler.  The pass never rewrites user-provided `.s` inputs,
    never reorders instructions, and refuses the fold when the previous
    physical instruction is a branch, jump, or call so delay slots are not
    rewritten.
  - Post-move-shift-fold `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3160 instructions
      and 706 `nop` to 3086 instructions and 706 `nop`; total improvement
      from the original baseline is -601 instructions and -527 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.712,
      and `nop` ratio stayed 16.810.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3472
      instructions and 706 `nop` to 3398 instructions and 706 `nop`; total
      improvement from the original baseline is -921 instructions and -847
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.140, and `nop` ratio stayed 141.200.  Multiply/divide count stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3385 instructions and 619 `nop` to 3311 instructions and 619 `nop`;
      total improvement from the original baseline is -1008 instructions and
      -934 `nop`.  PCC/GCC instruction ratio improved from the original 3.992
      to 3.060, and `nop` ratio stayed 123.800.  Multiply/divide count stayed
      63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-move-shift-fold QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Conservative adjacent `move` plus `addiu` fold landed on 2026-07-08: the
    final-assembly driver postpass now also folds only physical adjacent
    `move dst,src; addiu dst,dst,imm` into `addiu dst,src,imm`.  This keeps
    the no-reordering/no-user-`.s` boundary from the shift fold and refuses
    folds immediately after a branch, jump, or call so delay-slot contents are
    never rewritten.
  - Post-move-addiu-fold `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3086 instructions
      and 706 `nop` to 3046 instructions and 706 `nop`; total improvement
      from the original baseline is -641 instructions and -527 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.677,
      and `nop` ratio stayed 16.810.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3398
      instructions and 706 `nop` to 3358 instructions and 706 `nop`; total
      improvement from the original baseline is -961 instructions and -847
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.104, and `nop` ratio stayed 141.200.  Multiply/divide count stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3311 instructions and 619 `nop` to 3271 instructions and 619 `nop`;
      total improvement from the original baseline is -1048 instructions and
      -934 `nop`.  PCC/GCC instruction ratio improved from the original 3.992
      to 3.023, and `nop` ratio stayed 123.800.  Multiply/divide count stayed
      63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-move-addiu-fold QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - `$fp` GPR parser fix landed on 2026-07-08: the MIPS assembly postpass
    parser now treats only `$f` followed by a digit as an FPU register, so
    `$fp` is visible as the normal GPR alias for register 30.  This is not a
    new backend template or scheduler; it lets the existing conservative
    postpasses see `$fp` operands they were previously refusing.
  - Post-fp-parser-fix `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3046 instructions
      and 706 `nop` to 3042 instructions and 706 `nop`; total improvement
      from the original baseline is -645 instructions and -527 `nop`.
      PCC/GCC instruction ratio improved from the original 3.240 to 2.673,
      and `nop` ratio stayed 16.810.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3358
      instructions and 706 `nop` to 3354 instructions and 706 `nop`; total
      improvement from the original baseline is -965 instructions and -847
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.100, and `nop` ratio stayed 141.200.  Multiply/divide count stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3271 instructions and 619 `nop` to 3267 instructions and 619 `nop`;
      total improvement from the original baseline is -1052 instructions and
      -934 `nop`.  PCC/GCC instruction ratio improved from the original 3.992
      to 3.019, and `nop` ratio stayed 123.800.  Multiply/divide count stayed
      63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-fp-parser-fix QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Move-into-jump-delay fold landed on 2026-07-08: the final-assembly
    driver postpass now folds only the exact generated pattern
    `move dst,src; j label; nop` into `j label; move dst,src`, and only when
    the `move` is not already in a control-transfer delay slot.  This remains
    a narrow text peephole rather than a scheduler: it does not rewrite
    conditional branches, calls, load-delay sequences, or user-supplied `.s`.
  - Post-move-jump-delay-fold `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3042 instructions
      and 706 `nop` to 3023 instructions and 687 `nop`; branch/jump delay
      `nop` count fell from 184 to 165.  Total improvement from the original
      baseline is -664 instructions and -546 `nop`.  PCC/GCC instruction
      ratio improved from the original 3.240 to 2.656, and `nop` ratio
      improved to 16.357.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3354
      instructions and 706 `nop` to 3335 instructions and 687 `nop`;
      branch/jump delay `nop` count fell from 184 to 165.  Total improvement
      from the original baseline is -984 instructions and -866 `nop`.
      PCC/GCC instruction ratio improved from the original 3.992 to 3.082,
      and `nop` ratio improved to 137.400.  Multiply/divide count stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3267 instructions and 619 `nop` to 3248 instructions and 600 `nop`;
      branch/jump delay `nop` count fell from 184 to 165.  Total improvement
      from the original baseline is -1071 instructions and -953 `nop`.
      PCC/GCC instruction ratio improved from the original 3.992 to 3.002,
      and `nop` ratio improved to 120.000.  Multiply/divide count stayed 63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-move-jump-delay-fold QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - GPR-ALU-into-jump-delay fold landed on 2026-07-08: the same final
    postpass now also moves only real non-trapping GPR ALU instructions
    `addiu` and `addu` from immediately before a plain `j` into that jump's
    `nop` delay slot.  It still refuses pseudo `li`, stores, loads, FPU loads,
    conditional branches, calls, and user-supplied `.s`, so this remains a
    narrow peephole instead of a scheduler.
  - Post-gpr-alu-jump-delay-fold `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3023 instructions
      and 687 `nop` to 3003 instructions and 667 `nop`; branch/jump delay
      `nop` count fell from 165 to 145.  Total improvement from the original
      baseline is -684 instructions and -566 `nop`.  PCC/GCC instruction
      ratio improved from the original 3.240 to 2.639, and `nop` ratio
      improved to 15.881.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3335
      instructions and 687 `nop` to 3315 instructions and 667 `nop`;
      branch/jump delay `nop` count fell from 165 to 145.  Total improvement
      from the original baseline is -1004 instructions and -886 `nop`.
      PCC/GCC instruction ratio improved from the original 3.992 to 3.064,
      and `nop` ratio improved to 133.400.  Multiply/divide count stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3248 instructions and 600 `nop` to 3228 instructions and 580 `nop`;
      branch/jump delay `nop` count fell from 165 to 145.  Total improvement
      from the original baseline is -1091 instructions and -973 `nop`.
      PCC/GCC instruction ratio improved from the original 3.992 to 2.983,
      and `nop` ratio improved to 116.000.  Multiply/divide count stayed 63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-gpr-alu-jump-delay-fold QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Non-RA-GPR-into-call-delay fold landed on 2026-07-08: the final
    postpass now uses the same real `move`/`addiu`/`addu` whitelist before a
    plain `jal` with a `nop` delay slot, but refuses any candidate touching
    `$ra` because `jal` writes register 31.  This still does not rewrite
    `jalr`, conditional branches, loads, stores, FPU operations, pseudo
    instructions, or user-supplied `.s`.
  - Post-non-ra-gpr-call-delay-fold `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 3003 instructions
      and 667 `nop` to 2997 instructions and 661 `nop`; branch/jump/call
      delay `nop` count fell from 145 to 139.  Total improvement from the
      original baseline is -690 instructions and -572 `nop`.  PCC/GCC
      instruction ratio improved from the original 3.240 to 2.634, and `nop`
      ratio improved to 15.738.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3315
      instructions and 667 `nop` to 3309 instructions and 661 `nop`;
      branch/jump/call delay `nop` count fell from 145 to 139.  Total
      improvement from the original baseline is -1010 instructions and -892
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.058, and `nop` ratio improved to 132.200.  Multiply/divide count
      stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3228 instructions and 580 `nop` to 3222 instructions and 574 `nop`;
      branch/jump/call delay `nop` count fell from 145 to 139.  Total
      improvement from the original baseline is -1097 instructions and -979
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      2.978, and `nop` ratio improved to 114.800.  Multiply/divide count
      stayed 63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-non-ra-gpr-call-delay-fold QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Signed-imm16 `li`-into-control-delay fold landed on 2026-07-08: the
    final postpass now recognizes exact `li dst,imm16` before a plain direct
    `j`/`jal` with a `nop` delay slot, lowers it to the real single
    instruction `addiu dst,$zero,imm`, and then reuses the existing
    control-delay safety predicate.  It does not emit pseudo-instructions in
    delay slots; it rejects wide immediates, symbols, loads, stores, FPU
    operations, conditional branches, indirect jumps/calls, and user-supplied
    `.s`.  For `jal`, the existing `$ra` guard still applies after lowering.
  - Post-signed-imm16-li-control-delay-fold `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2997 instructions
      and 661 `nop` to 2994 instructions and 658 `nop`; branch/jump/call
      delay `nop` count fell from 139 to 136.  Total improvement from the
      original baseline is -693 instructions and -575 `nop`.  PCC/GCC
      instruction ratio improved from the original 3.240 to 2.631, and `nop`
      ratio improved to 15.667.  Multiply/divide count stayed 63.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3309
      instructions and 661 `nop` to 3306 instructions and 658 `nop`;
      branch/jump/call delay `nop` count fell from 139 to 136.  Total
      improvement from the original baseline is -1013 instructions and -895
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      3.055, and `nop` ratio improved to 131.600.  Multiply/divide count
      stayed 63.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3222 instructions and 574 `nop` to 3219 instructions and 571 `nop`;
      branch/jump/call delay `nop` count fell from 139 to 136.  Total
      improvement from the original baseline is -1100 instructions and -982
      `nop`.  PCC/GCC instruction ratio improved from the original 3.992 to
      2.975, and `nop` ratio improved to 114.200.  Multiply/divide count
      stayed 63.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-signed-imm16-li-control-delay-fold QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Power-of-two modulo strength reduction landed on 2026-07-08: the MIPS
    table now matches unsigned and signed `% pow2` constants.  Unsigned
    modulo lowers to a mask; signed modulo lowers to the C-correct
    trunc-toward-zero bias/mask/subtract sequence, so negative dividends still
    produce negative remainders.  This removes `div`/`mfhi` latency without a
    general magic-number division pass and applies to VR4300 and MIPS32R2.
  - Post-power-of-two-modulo-strength-reduction `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack kept the previous 2994 instructions but
      changed from 658 `nop` to 654 `nop`; multiply/divide count fell from 63
      to 61.  Total improvement from the original baseline is -693
      instructions and -579 `nop`.  PCC/GCC instruction ratio stayed 2.631,
      and `nop` ratio improved to 15.571.
    - Malta/mips32r2 big-endian PCC Linpack kept the previous 3306
      instructions but changed from 658 `nop` to 654 `nop`;
      multiply/divide count fell from 63 to 61.  Total improvement from the
      original baseline is -1013 instructions and -899 `nop`.  PCC/GCC
      instruction ratio stayed 3.055, and `nop` ratio improved to 130.800.
    - MaltaEL/mips32r2 little-endian PCC Linpack kept the previous 3219
      instructions but changed from 571 `nop` to 567 `nop`;
      multiply/divide count fell from 63 to 61.  Total improvement from the
      original baseline is -1100 instructions and -986 `nop`.  PCC/GCC
      instruction ratio stayed 2.975, and `nop` ratio improved to 113.400.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-power-of-two-modulo-strength-reduction QEMU correctness gates passed
    with `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian,
    and MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - MIPS32R2 one-instruction multiply landed on 2026-07-08: `features()` now
    has a CPU-gated `FEATURE_MIPS32R2`, and 32-bit integer `MUL` uses real
    `mul rd,rs,rt` only when `-march=mips32r2` is active.  VR4300 keeps the
    old `mult`/delay/`mflo` sequence; this avoids relying on assembler macro
    expansion for an instruction that is not in the VR4300 ISA.  Keep
    assembly-template comments lowercase because PCC's template expander
    interprets some capital letters as commands.
  - Post-MIPS32R2-mul `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack stayed at 2994 instructions, 654 `nop`,
      and 61 multiply/divide instructions; PCC/GCC instruction ratio stayed
      2.631, and `nop` ratio stayed 15.571.  This confirms the CPU feature
      gate leaves VR4300 code generation unchanged.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3306
      instructions, 654 `nop`, and 61 multiply/divide instructions to 3129
      instructions, 536 `nop`, and 2 multiply/divide instructions.  Total
      improvement from the original baseline is -1190 instructions and -1017
      `nop`; PCC/GCC instruction ratio improved to 2.892, and `nop` ratio
      improved to 107.200.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3219 instructions, 567 `nop`, and 61 multiply/divide instructions to
      3042 instructions, 449 `nop`, and 2 multiply/divide instructions.
      Total improvement from the original baseline is -1277 instructions and
      -1104 `nop`; PCC/GCC instruction ratio improved to 2.811, and `nop`
      ratio improved to 89.800.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-MIPS32R2-mul QEMU correctness gates passed with `PCC_SMOKE_ALL_RC:0`
    on Malta64/VR4300, Malta/mips32r2 big-endian, and MaltaEL/mips32r2
    little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - VR4300 errata flag plumbing landed on 2026-07-08: ReBSD/MIPS PCC accepts
    `-mfix4300` and `-mno-fix4300`.  `-march=vr4300` defaults fix mode on
    unless explicitly overridden; `-march=mips32r2` defaults it off unless
    explicitly overridden.  `features()` exposes `FEATURE_FIX4300` and
    `FEATURE_NOFIX4300` so future table rules can pair errata-safe fallback
    code with faster `-mno-fix4300` forms.  This patch deliberately does not
    change existing errata-sensitive code sequences until the exact workaround
    is selected and tested.
  - Signed power-of-two division cleanup landed on 2026-07-08: the old
    exactly-2 signed division rule now covers all positive powers of two with
    C-correct trunc-toward-zero bias.  `/2` keeps the old short
    `srl/addu/sra` form; larger divisors use sign-mask bias plus `sra`,
    avoiding `div`/`mflo` without adding a general magic-number division pass.
  - Direct host checks passed after the `fix4300` and signed-division patch:
    `-mfix4300` and `-mno-fix4300` are accepted for VR4300 in both option
    orders and with MIPS32R2, and temporary `/2`, `/8`, and `/1024` assembly
    contains no `div`/`mflo`.
  - Post-signed-power-of-two-division and `fix4300` plumbing
    `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack stayed at 2994 instructions, 654 `nop`, and
      61 multiply/divide instructions; PCC/GCC instruction ratio stayed
      2.631, and `nop` ratio stayed 15.571.
    - Malta/mips32r2 big-endian PCC Linpack stayed at 3129 instructions,
      536 `nop`, and 2 multiply/divide instructions; PCC/GCC instruction
      ratio stayed 2.892, and `nop` ratio stayed 107.200.
    - MaltaEL/mips32r2 little-endian PCC Linpack stayed at 3042
      instructions, 449 `nop`, and 2 multiply/divide instructions; PCC/GCC
      instruction ratio stayed 2.811, and `nop` ratio stayed 89.800.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-signed-power-of-two-division and `fix4300` plumbing QEMU correctness
    gates passed with `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2
    big-endian, and MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - VR4300/R4300i multiply erratum workaround landed on 2026-07-08:
    the final MIPS assembly postprocess inserts a real `nop` between
    `mul.s`/`mul.d` and any immediately following `mul.s`/`mul.d`, `mult`,
    `multu`, `dmult`, or `dmultu` when `MIPS_FIX4300_ACTIVE` is true.
    `-march=vr4300` enables the workaround by default; `-mno-fix4300`
    disables it; MIPS32R2 remains unaffected even if the option is forwarded
    to `ccom`, because the repair itself is gated on selected CPU == VR4300.
    The same driver path repairs simple adjacent handwritten
    `.s` inputs on a temporary copy and warns when `mul.s`/`mul.d` appears in
    a branch or jump delay slot.  Full CFG repair of handwritten assembly
    stays out of scope for the compiler driver.
  - VR4300 erratum documentation added in `docs/vr4300_errata.md`.  QEMU is
    kept as an architecture/regression gate only; it is not treated as proof
    that the revision-specific VR4300 pipeline bug is absent.
  - Direct host assembly checks passed for the erratum workaround:
    `-march=vr4300` default and explicit `-mfix4300` insert the separator
    `nop` for adjacent `mul.s`/`mul.d` followed by FP or integer multiply;
    `-mno-fix4300`, MIPS32R2, and non-FP-multiply pairs leave the input
    unchanged; branch-delay-slot `mul.s`/`mul.d` emits the intended warning.
  - Post-VR4300-erratum-workaround `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack stayed at 2994 instructions, 654 `nop`, and
      61 multiply/divide instructions; PCC/GCC instruction ratio stayed
      2.631, and `nop` ratio stayed 15.571.
    - Malta/mips32r2 big-endian PCC Linpack stayed at 3129 instructions,
      536 `nop`, and 2 multiply/divide instructions; PCC/GCC instruction
      ratio stayed 2.892, and `nop` ratio stayed 107.200.
    - MaltaEL/mips32r2 little-endian PCC Linpack stayed at 3042
      instructions, 449 `nop`, and 2 multiply/divide instructions; PCC/GCC
      instruction ratio stayed 2.811, and `nop` ratio stayed 89.800.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-VR4300-erratum-workaround QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Single-gap GPR load-delay cleanup landed on 2026-07-08: the final MIPS
    postprocess now removes an integer-load delay `nop` when the immediately
    following real instruction is independent, including another independent
    integer load.  This is a narrow change in the existing load-delay trim
    pass; FPU loads still keep the older two-instruction lookahead.  The fresh
    VR4300 Linpack assembly had `fpu_load_one_gap_candidates=0`, so no
    FPU-specific expansion was added for this step.
  - Post-single-gap-GPR-load-cleanup `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2994 instructions
      and 654 `nop` to 2963 instructions and 623 `nop`; total improvement
      from the original baseline is -724 instructions and -610 `nop`.
      PCC/GCC instruction ratio improved to 2.604, `nop` ratio improved to
      14.833, and multiply/divide count stayed 61.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3129
      instructions and 536 `nop` to 3098 instructions and 505 `nop`; total
      improvement from the original baseline is -1221 instructions and -1048
      `nop`.  PCC/GCC instruction ratio improved to 2.863, `nop` ratio
      improved to 101.000, and multiply/divide count stayed 2.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      3042 instructions and 449 `nop` to 3011 instructions and 418 `nop`;
      total improvement from the original baseline is -1308 instructions and
      -1135 `nop`.  PCC/GCC instruction ratio improved to 2.783, `nop` ratio
      improved to 83.600, and multiply/divide count stayed 2.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-single-gap-GPR-load-cleanup QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`, and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`.
  - Shift-before-load delay fill landed on 2026-07-08 for VR4300: the MIPS
    final postprocess rewrites only the conservative `sll`/`srl`/`sra`; `lw`;
    `nop`; immediate load-use pattern into `lw`; shift; load-use.  The shift
    must not already be in a branch delay slot, must not affect the load
    address or destination, and the pass is limited to ordinary GPR `lw`.
    After a PCC-kernel MIPS32R2 soft-float control run exposed unstable kernel
    execution, the pass was scoped to `-march=vr4300` until a safe MIPS32R2
    scheduling rule is validated.
  - Post-shift-load-delay-fill `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2963 instructions
      and 623 `nop` to 2919 instructions and 579 `nop`; total improvement
      from the original baseline is -768 instructions and -654 `nop`.
      PCC/GCC instruction ratio improved to 2.565, `nop` ratio improved to
      13.786, and multiply/divide count stayed 61.
    - Malta/mips32r2 big-endian is not using this pass after the guard:
      current PCC Linpack remains at 3098 instructions and 505 `nop`; total
      improvement from the original baseline is -1221 instructions and -1048
      `nop`.  PCC/GCC instruction ratio is 2.863, `nop` ratio is 101.000,
      and multiply/divide count stays 2.
    - MaltaEL/mips32r2 little-endian is not using this pass after the guard:
      current PCC Linpack remains at 3011 instructions and 418 `nop`; total
      improvement from the original baseline is -1308 instructions and -1135
      `nop`.  PCC/GCC instruction ratio is 2.783, `nop` ratio is 83.600, and
      multiply/divide count stays 2.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-shift-load-delay-fill QEMU correctness gates passed with
    `PCC_SMOKE_ALL_RC:0` on Malta64/VR4300, Malta/mips32r2 big-endian, and
    MaltaEL/mips32r2 little-endian.  After scoping the fill to VR4300, the
    full PCC-kernel/PCC-rootfs hard-float and soft-float gate matrix also
    passed on all three boards:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - Move-before-load delay fill landed on 2026-07-08 for VR4300: the same
    final postprocess now also rewrites only the conservative `move`; `lw`;
    `nop`; immediate load-use pattern into `lw`; `move`; load-use.  The
    `move` must not already be in a branch delay slot, must not affect the
    load address or destination, and the rule remains limited to ordinary GPR
    `lw`.  PCC kernel builds do pass `-fomit-frame-pointer`, but `$fp`/`$s8`
    is still a real allocatable GPR in non-trivial frames, so the postpass
    treats it like any other GPR and rejects transforms that touch the load
    address or loaded value.
  - Post-move-load-delay-fill `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2919 instructions
      and 579 `nop` to 2910 instructions and 570 `nop`; total improvement
      from the original baseline is -777 instructions and -663 `nop`.
      PCC/GCC instruction ratio improved to 2.557, `nop` ratio improved to
      13.571, branch/jump/call delay `nop` count stayed 136, and
      multiply/divide count stayed 61.
    - Malta/mips32r2 big-endian is unchanged because this fill is VR4300-only:
      PCC Linpack remains at 3098 instructions and 505 `nop`.  PCC/GCC
      instruction ratio remains 2.863, `nop` ratio remains 101.000, and
      multiply/divide count stays 2.
    - MaltaEL/mips32r2 little-endian is unchanged for the same reason:
      PCC Linpack remains at 3011 instructions and 418 `nop`.  PCC/GCC
      instruction ratio remains 2.783, `nop` ratio remains 83.600, and
      multiply/divide count stays 2.
      Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`,
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`,
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-move-load-delay-fill full PCC-kernel/PCC-rootfs QEMU correctness
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    The kernel C compile path still uses `-msoft-float -fomit-frame-pointer`;
    rootfs float mode is varied independently by the hard/soft gates.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - A broader independent integer-load gap fill was tested but not landed: it
    saved about 20 Linpack instructions/`nop` on the counters, but failed the
    Malta native `libc-abi` assembler smoke with
    `as: libc-abi-cc.s, 712: bad instruction arguments`.  Keep this as a
    scheduler TODO until the exact assembler/codegen interaction is understood.
  - VR4300/MIPS3 assembler support for integer `ld` and `sd` landed on
    2026-07-08.  The in-tree assembler now accepts them only in
    VR4300/MIPS3 mode and rejects them under `-mips32r2 -march=mips32r2`
    with `requires MIPS3/VR4300`; `smoke-as-vr4300` and
    `matrix-as-vr4300` cover the accept/reject matrix.
  - A VR4300-only `l.d; nop; s.d` double-copy compiler fold was tested after
    assembler support but not landed.  It rewrote about 40 same-FPR FPU copy
    patterns and reduced the static Malta64/VR4300 Linpack counter from
    2910 instructions and 570 `nop` to 2870 instructions and 530 `nop`, but
    the full Malta64 hard-float PCC-kernel/PCC-rootfs QEMU gate caught an
    illegal-instruction failure in current o32 userland when target PCC emitted
    integer `ld`/`sd` sequences such as `ld $at,L487; sd $at,-80($fp)`.
    The compiler fold was removed; the post-revert Malta64 baseline is back
    to 2910 instructions and 570 `nop`.
  - Future check before any compiler use of integer `ld`/`sd`: verify whether
    the real N64/VR4300 ReBSD user mode enables 64-bit integer instructions
    and addressing through CP0 status bits such as UX/SX/KX, or an equivalent
    kernel policy.  If it is safe, add an explicit opt-in compiler switch and
    validate o32 userland, interrupt handling, Malta64 smoke, and real N64
    hardware.  Keep it VR4300/MIPS3-only; it must not be enabled for MIPS32R2.
  - Post-assembler-`ld`/`sd` full PCC-kernel/PCC-rootfs QEMU correctness
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - GPR-ALU-before-FPU-load delay fill landed on 2026-07-08 for VR4300:
    the existing late postprocess now also rewrites a conservative
    `addiu`/`addu`/`subu`; FPU load; `nop`; immediate FPU-load-use pattern
    into FPU load; GPR ALU; FPU-load-use.  The ALU instruction must not touch
    the FPU load address register or loaded FPR, and the FPU load must be one
    of the already tracked CP1 loads (`l.s`, `l.d`, `lwc1`, `ldc1`).  This is
    still a narrow VR4300-only load-delay repair, not a general scheduler, and
    it does not place FP multiplies into delay slots; the separate
    `-mfix4300`/`-mno-fix4300` multiply-erratum repair remains responsible for
    the VR4300/R4300i multiply pipeline hazard.
  - Post-GPR-ALU-FPU-load-delay-fill `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2910 instructions
      and 570 `nop` to 2902 instructions and 562 `nop`; total improvement
      from the original baseline is -785 instructions and -671 `nop`.
      PCC/GCC instruction ratio improved to 2.550, `nop` ratio improved to
      13.381, branch/jump/call delay `nop` count stayed 136, and
      multiply/divide count stayed 61.  The generic load-followed-by-`nop`
      count dropped from 290 to 282.
      Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 and MaltaEL/mips32r2 code generation are intentionally
      unchanged by this pass because the pass only runs for selected CPU
      `VR4300`.  Their correctness coverage is the full hard/soft QEMU matrix
      below; keep any mips32r2 scheduling improvement separate unless the
      latency rule is valid for that ISA profile too.
  - Post-GPR-ALU-FPU-load-delay-fill full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard about 9537-9572 KFLOPS, Malta64 soft about 593-603
    KFLOPS, Malta hard about 5446-5588 KFLOPS, Malta soft about 549-560
    KFLOPS, MaltaEL hard about 8901-8932 KFLOPS, and MaltaEL soft about
    580-586 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - Zero-left compare branch support landed on 2026-07-08: the MIPS backend now
    has a real table/codegen rule for `0 OP reg` in conditional context instead
    of materializing zero into a temporary and subtracting before branching.
    Signed cases use the direct inverted zero-branch forms (`bgtz`, `bgez`,
    `bltz`, `blez`), equality uses `beqz`/`bnez`, and unsigned constant-zero
    cases fold to either the equivalent zero test or an unconditional/no branch
    result.  This is instruction selection, not a post-assembly rewrite.
  - Post-zero-left-compare-branch `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2902 instructions
      and 562 `nop` to 2896 instructions and 568 `nop`.  The direct branch
      removes the old temp/subtract sequence but exposes some load-use slots
      that had previously been hidden by the materialized zero move:
      load-followed-by-`nop` is now 288, branch/jump/call delay `nop` count is
      still 136, and multiply/divide count is still 61.  PCC/GCC instruction
      ratio is 2.545 and `nop` ratio is 13.524.
      Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3098
      instructions and 505 `nop` to 3086 instructions and 505 `nop`;
      PCC/GCC instruction ratio is 2.852 and `nop` ratio is 101.000.
      Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous 3011
      instructions and 418 `nop` to 2999 instructions and 418 `nop`;
      PCC/GCC instruction ratio is 2.772 and `nop` ratio is 83.600.
      Counter artifact:
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-zero-left-compare-branch full PCC-kernel/PCC-rootfs QEMU correctness
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    The kernel C compile path still uses `-msoft-float -fomit-frame-pointer`;
    rootfs float mode is varied independently by the hard/soft gates.  Linpack
    smoke rates observed in QEMU: Malta64 hard about 12922-13132 KFLOPS,
    Malta64 soft about 884-887 KFLOPS, Malta hard about 8399-8475 KFLOPS,
    Malta soft about 854-861 KFLOPS, MaltaEL hard about 12544-13060 KFLOPS,
    and MaltaEL soft about 878-881 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - MIPS32R2 interlocked-load cleanup landed on 2026-07-08: the final MIPS
    postprocess now removes the explicit `load; nop; use` gap for selected CPU
    `MIPS32R2`, because that ISA profile has hardware load interlocks.  This
    is deliberately not enabled for VR4300: VR4300 still keeps dependent GPR
    and FPU load-use spacing unless a separate VR4300-specific scheduler moves
    an independent instruction into the gap.  The rule is a small extension of
    the existing load-delay trim, not a general instruction reorder pass.
  - Post-MIPS32R2-interlocked-load-cleanup `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack stayed at 2896 instructions and 568 `nop`;
      PCC/GCC instruction ratio stayed 2.545, `nop` ratio stayed 13.524,
      load-followed-by-`nop` stayed 288, branch/jump/call delay `nop` count
      stayed 136, and multiply/divide count stayed 61.  This confirms the
      MIPS32R2 gate leaves VR4300 code generation unchanged.
      Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian PCC Linpack changed from the previous 3086
      instructions and 505 `nop` to 2746 instructions and 165 `nop`;
      PCC/GCC instruction ratio improved from 2.852 to 2.538, and `nop` ratio
      improved from 101.000 to 33.000.
      Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
    - MaltaEL/mips32r2 little-endian PCC Linpack changed from the previous
      2999 instructions and 418 `nop` to 2746 instructions and 165 `nop`;
      PCC/GCC instruction ratio improved from 2.772 to 2.538, and `nop` ratio
      improved from 83.600 to 33.000.
      Counter artifact:
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-MIPS32R2-interlocked-load-cleanup full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard about 12884-13123 KFLOPS, Malta64 soft about 888-889
    KFLOPS, Malta hard about 7178 KFLOPS, Malta soft about 902-907 KFLOPS,
    MaltaEL hard about 8627-9160 KFLOPS, and MaltaEL soft about 917-924
    KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - Signed-imm16 `li` before integer-load delay fill landed on 2026-07-08
    for VR4300: the same narrow postprocess now rewrites an exact
    `li dst,imm16`; `lw`; `nop`; immediate load-use window into `lw`;
    real `addiu dst,$zero,imm`; load-use.  This keeps the backend small by
    reusing the existing final assembly cleanup instead of adding a general
    scheduler.  The rule only accepts the already supported signed-imm16
    `li` fold, rejects `$sp`, rejects control-transfer load users, rejects
    loads that touch the `li` registers, and rejects cases where the folded
    `addiu` touches the loaded GPR.  It stays VR4300-only because the
    containing pass only runs for selected CPU `VR4300`.
  - A broader `li` load-delay variant was tested but not landed: allowing
    control-transfer consumers reduced the static Malta64/VR4300 Linpack
    counter to 2886 instructions and 558 `nop`, but the Malta64 hard-float
    PCC-kernel/PCC-rootfs gate failed while building/running the native target
    `types` test with `as: types-cc.s, 3750: bad instruction arguments`.
    The landed rule keeps control transfers outside this local movement until
    the exact assembler/codegen interaction is understood.
  - Post-signed-imm16-`li`-load-delay-fill `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2895 instructions
      and 567 `nop` to 2888 instructions and 560 `nop`; PCC/GCC instruction
      ratio is 2.538, `nop` ratio is 13.333, branch/jump/call delay `nop`
      count is 135, multiply/divide count is 61, and load-followed-by-`nop`
      is 281.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 and MaltaEL/mips32r2 code generation are intentionally
      unchanged by this pass because the containing load-delay fill remains
      VR4300-only.  Their correctness coverage is the full hard/soft QEMU
      matrix below.
  - Post-signed-imm16-`li`-load-delay-fill full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard about 12669-12841 KFLOPS, Malta64 soft about 775-785
    KFLOPS, Malta hard about 7159 KFLOPS, Malta soft about 890-891 KFLOPS,
    MaltaEL hard about 8086-8656 KFLOPS, and MaltaEL soft about 909-919
    KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - VR4300 FPU-store jump/call delay-slot parsing fix landed on 2026-07-08:
    the existing simple-store delay-slot helper already allowed `s.s`, `s.d`,
    `swc1`, and `sdc1`, but its FPU branch did not skip whitespace after the
    opcode before parsing the FPR operand.  The fix makes those already
    intended FPU stores recognizable without adding a scheduler or broadening
    the movement rules; it remains VR4300-only through the existing
    `mips_can_move_to_plain_jump_delay()` store gate.
  - Post-FPU-store-delay-slot-parse-fix `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2888 instructions
      and 560 `nop` to 2883 instructions and 555 `nop`; PCC/GCC instruction
      ratio is 2.533, `nop` ratio is 13.214, branch/jump/call delay `nop`
      count is 130, multiply/divide count is 61, and load-followed-by-`nop`
      remains 281.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 and MaltaEL/mips32r2 scheduling remain intentionally
      unchanged because the simple store delay-slot fill still only moves
      stores when `mips_cpu == MIPS_CPU_VR4300`; their coverage is the full
      hard/soft QEMU correctness matrix below.
  - Post-FPU-store-delay-slot-parse-fix full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13020.844-13087.788 KFLOPS, Malta64 soft
    796.688-805.878 KFLOPS, Malta hard 7342.231-7429.915 KFLOPS, Malta soft
    913.901-915.341 KFLOPS, MaltaEL hard 8978.098-9045.244 KFLOPS, and
    MaltaEL soft 931.741-938.098 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - HI/LO-to-FPU convert delay fill landed on 2026-07-08: the final MIPS
    postprocess now recognizes the narrow sequence
    `mult`/`multu`/`dmult`/`dmultu`; `nop`; `nop`; `mflo`/`mfhi`; `mtc1`;
    `nop`; `cvt.s.w`/`cvt.d.w` and rewrites it as multiply, independent
    `mtc1`, one required spacer, `mflo`/`mfhi`, then `cvt`.  The rule rejects
    `div`, rejects an `mtc1` that reads the `mflo`/`mfhi` destination GPR,
    requires the `cvt` source and destination FPRs to match the `mtc1`
    destination FPR, and refuses to run from a control-transfer delay slot.
    This is a targeted final-assembly fold, not a general scheduler.
  - Post-HI/LO-to-FPU-convert-delay-fill `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2883 instructions
      and 555 `nop` to 2881 instructions and 553 `nop`; PCC/GCC instruction
      ratio is 2.532, `nop` ratio is 13.167, branch/jump/call delay `nop`
      count is 130, multiply/divide count is 61, and load-followed-by-`nop`
      remains 281.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 and MaltaEL/mips32r2 counters remained unchanged at 2746
      PCC instructions and 165 `nop`, PCC/GCC instruction ratio 2.538 and
      `nop` ratio 33.000, because mips32r2 uses the real `mul` path for the
      measured Linpack case instead of the VR4300-style `mult`/`mflo`
      sequence.  Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-HI/LO-to-FPU-convert-delay-fill full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13463.405-13555.997 KFLOPS, Malta64 soft
    795.914-802.799 KFLOPS, Malta hard 7502.114-7703.634 KFLOPS, Malta soft
    920.306-929.966 KFLOPS, MaltaEL hard 8951.105-9089.640 KFLOPS, and
    MaltaEL soft 939.889-947.661 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - `mtc1`/convert/FPU-load delay fill landed on 2026-07-08: the final MIPS
    postprocess now recognizes the exact sequence `mtc1`; `nop`;
    `cvt.s.w`/`cvt.d.w`; `l.s`/`l.d`/`lwc1`/`ldc1`; `nop`;
    `add.s`/`add.d`/`sub.s`/`sub.d`/`mul.s`/`mul.d`/`div.s`/`div.d` and
    rewrites it as `mtc1`, the independent FPU load, `cvt`, then the FP
    operation.  The rule requires the `cvt` source and destination FPRs to
    match the `mtc1` destination FPR, rejects FPU loads whose destination
    overlaps the `cvt` FPR or paired double FPRs, and requires the FP
    operation to read the loaded FPR.  It does not move a multiply immediately
    after a previous floating-point multiply and does not weaken the
    `-mfix4300` multiply-erratum repair.
  - Post-`mtc1`/convert/FPU-load-delay-fill `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2881 instructions
      and 553 `nop` to 2877 instructions and 549 `nop`; PCC/GCC instruction
      ratio is 2.528, `nop` ratio is 13.071, branch/jump/call delay `nop`
      count is 130, multiply/divide count is 61, and load-followed-by-`nop`
      dropped to 279.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 and MaltaEL/mips32r2 counters remained unchanged at 2746
      PCC instructions and 165 `nop`, PCC/GCC instruction ratio 2.538 and
      `nop` ratio 33.000.  Counter artifacts:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`
      and
      `/private/tmp/rebsd-pcc-perf-maltael-little.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-`mtc1`/convert/FPU-load-delay-fill full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13628.629-13659.495 KFLOPS, Malta64 soft
    801.915-803.055 KFLOPS, Malta hard 7349.815-7521.371 KFLOPS, Malta soft
    923.106-925.847 KFLOPS, MaltaEL hard 8995.864-9023.562 KFLOPS, and
    MaltaEL soft 940.345-941.248 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - HI/LO-to-independent-`lw` delay fill landed on 2026-07-08: the final MIPS
    postprocess now recognizes the exact sequence `mult`/`multu`/`dmult`/
    `dmultu`; `nop`; `nop`; `mflo`/`mfhi`; simple dependent GPR
    ALU/shift/move use; independent `lw`, and rewrites it as multiply,
    independent `lw`, one required spacer, `mflo`/`mfhi`, then the use.
    Safety rules keep two executed instructions before `mflo`/`mfhi`, require
    the following GPR use to read the `mflo`/`mfhi` result, reject stores and
    reload-like consumers, reject loads that touch multiply operands or the
    `mflo`/`mfhi` result, and reject loads whose destination is touched by the
    consumer.  An earlier broader shape was caught by Malta64 hard-float QEMU
    because moving a reload ahead of a store fed a stale allocation size; the
    committed rule is intentionally narrower.  This is a final-assembly fold,
    not a general scheduler, and it does not move FP multiplies or weaken the
    `-mfix4300` multiply-erratum repair.
  - Post-HI/LO-to-independent-`lw`-delay-fill `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2877 instructions
      and 549 `nop` to 2847 instructions and 519 `nop`; PCC/GCC instruction
      ratio is 2.502, `nop` ratio is 12.357, branch/jump/call delay `nop`
      count is 130, multiply/divide count is 61, and load-followed-by-`nop`
      is 309 because the independent `lw` now occupies one HI/LO spacer and is
      still followed by the remaining required `nop`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - The mips32r2 measured Linpack paths remain covered by the full QEMU
      matrix; their previous hard-float counter run stayed at 2746 PCC
      instructions and 165 `nop` on both big-endian and little-endian Malta,
      because that measured case uses the real `mul` path instead of the
      VR4300-style `mult`/`mflo` sequence.
  - Post-HI/LO-to-independent-`lw`-delay-fill full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13523.433-13585.113 KFLOPS, Malta64 soft
    875.441-882.004 KFLOPS, Malta hard 7304.929-7560.492 KFLOPS, Malta soft
    927.326-932.657 KFLOPS, MaltaEL hard 8959.613-9054.008 KFLOPS, and
    MaltaEL soft 937.860-946.490 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - Two-use HI/LO-to-independent-`lw` delay fill landed on 2026-07-08: the
    same final MIPS postprocess now also recognizes the exact sequence
    `mult`/`multu`/`dmult`/`dmultu`; `nop`; `nop`; `mflo`/`mfhi`; simple GPR
    use; simple GPR use; independent `lw`, and rewrites it as multiply,
    independent `lw`, one required spacer, `mflo`/`mfhi`, then both GPR uses.
    Safety rules keep two executed instructions before `mflo`/`mfhi`, require
    both simple GPR uses to touch the `mflo`/`mfhi` result register, reject
    loads that touch either use, the multiply operands, or the `mflo`/`mfhi`
    result, and reject either use when it touches the load destination/base.
    This remains a narrow final-assembly fold, not a scheduler; it does not
    move FP multiplies, put FP multiplies into delay slots, or weaken the
    `-mfix4300` multiply-erratum repair.
  - Post-two-use-HI/LO-`lw`-delay-fill `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2847 instructions
      and 519 `nop` to 2837 instructions and 509 `nop`; non-`nop`
      instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 130, multiply/divide stayed 61, and
      bytes changed to 81826.  PCC/GCC instruction ratio is 2.493 and `nop`
      ratio is 12.119.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - MIPS32R2 correctness remains covered by the full QEMU matrix below.  No
      MIPS32R2-specific transform was added in this patch; the measured
      hard-float Linpack path usually selects the real `mul` instruction
      rather than the VR4300-style `mult`/`mflo` sequence.
  - Post-two-use-HI/LO-`lw`-delay-fill full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13770.143-13857.808 KFLOPS, Malta64 soft
    831.415-832.141 KFLOPS, Malta hard 7422.324-7488.546 KFLOPS, Malta soft
    924.347-926.147 KFLOPS, MaltaEL hard 9076.971-9128.844 KFLOPS, and
    MaltaEL soft 939.979-944.133 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - VR4300 post-delay-slot load-delay trim landed on 2026-07-08: the final
    MIPS postprocess now repeats the existing load-delay trim once after late
    delay-slot peepholes, so `nop` padding left behind by moving a store into a
    `j`/`jal`/branch delay slot can be removed.  For VR4300 FPU loads, the
    lookahead permits a loaded-FPR use in that already-existing control-transfer
    delay slot because the control transfer itself is the executed gap
    instruction.  The pass deletes only redundant `nop`; it does not move FP
    multiplies, does not put FP multiplies in delay slots, and does not weaken
    the `-mfix4300` multiply-erratum repair.
  - Post-delay-slot-load-trim `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2837 instructions
      and 509 `nop` to 2792 instructions and 464 `nop`; non-`nop`
      instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 130, multiply/divide stayed 61, and
      bytes changed to 81601.  PCC/GCC instruction ratio is 2.453 and `nop`
      ratio is 11.048.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float stayed unchanged at 2730 PCC
      instructions and 149 `nop`; the second load-delay trim pass is gated to
      VR4300.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-delay-slot-load-trim full PCC-kernel/PCC-rootfs QEMU correctness
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    The kernel C compile path still uses `-msoft-float -fomit-frame-pointer`;
    rootfs float mode is varied independently by the hard/soft gates.  Linpack
    smoke rates observed in QEMU: Malta64 hard 11195.183-11468.473 KFLOPS,
    Malta64 soft 824.786-836.386 KFLOPS, Malta hard 7440.568-7598.507 KFLOPS,
    Malta soft 918.686-929.180 KFLOPS, MaltaEL hard 7599.199-7846.095 KFLOPS,
    and MaltaEL soft 904.580-907.619 KFLOPS.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - VR4300 integer load-use interlock cleanup landed on 2026-07-08: the final
    load-delay trim now removes `load`; `nop`; immediate dependent GPR-use
    padding for selected CPU `VR4300`, matching the VR4300 manual's hardware
    interlock behavior for integer loads.  This is intentionally only a
    deletion of compiler-inserted `nop`s; it does not move instructions, does
    not put FP multiplies in delay slots, and does not weaken the
    `-mfix4300` multiply-erratum repair.  Immediate FPU load-use stays
    conservative in this integer-only patch and is handled by the following
    VR4300 FPU load-use cleanup.
  - The same patch canonicalizes backend stack-push zero-offset operands from
    `($sp)` to `0($sp)` in MIPS FUNARG/prologue emission.  The VR4300 integer
    load cleanup exposed a real native guest assembler rejection in the
    soft-float `types` smoke (`sw $v1,($sp)`), while host cross assemblers had
    accepted it.  Emitting the canonical immediate-offset form is backend
    support, not a test workaround.
  - Post-integer-load-use-trim `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2792 instructions
      and 464 `nop` to 2638 instructions and 310 `nop`; non-`nop`
      instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 130, multiply/divide stayed 61,
      load-followed-by-nop fell to 120, and bytes changed to 80831.  PCC/GCC
      instruction ratio is 2.318 and `nop` ratio is 7.381.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float stayed unchanged at 2730 PCC
      instructions and 149 `nop`; this trim only newly changes VR4300 GPR
      load-use because the MIPS32R2 dependent-load cleanup was already
      interlock-based and green.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-integer-load-use-trim full PCC-kernel/PCC-rootfs QEMU correctness
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    The kernel C compile path still uses `-msoft-float -fomit-frame-pointer`;
    rootfs float mode is varied independently by the hard/soft gates.  Linpack
    smoke rates observed in QEMU: Malta64 hard 12935.016-13131.437 KFLOPS,
    Malta64 soft 784.753-885.351 KFLOPS, Malta hard 7443.138-7577.500 KFLOPS,
    Malta soft 881.624-887.288 KFLOPS, MaltaEL hard 7638.942-7641.148 KFLOPS,
    and MaltaEL soft 908.037-911.634 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - VR4300 FPU load-use interlock cleanup landed on 2026-07-08: the final
    load-delay trim now removes `load`; `nop`; immediate dependent FPU-use
    padding for selected CPU `VR4300`, matching the VR4300 CP1 load interlock
    behavior.  This only deletes compiler-inserted `nop`s; it does not
    reorder instructions, does not put FP multiplies in delay slots, and does
    not weaken the final `-mfix4300` multiply-erratum repair.
  - The MIPS32R2 path is unchanged by this patch.  It was already allowed to
    trim dependent load-use `nop`s through the interlocked MIPS32R2 path.
  - Post-FPU-load-use-trim `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2638 instructions
      and 310 `nop` to 2523 instructions and 195 `nop`; non-`nop`
      instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 130, multiply/divide stayed 61,
      load-followed-by-nop fell to 3, and bytes changed to 80317.  PCC/GCC
      instruction ratio is 2.217 and `nop` ratio is 4.643.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float stayed unchanged at 2730 PCC
      instructions and 149 `nop`; the current MIPS32R2 PCC/GCC instruction
      ratio remains 2.523 and `nop` ratio remains 29.800.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-FPU-load-use-trim full PCC-kernel/PCC-rootfs QEMU correctness matrix
    passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08: Malta64/VR4300 hard-float
    and soft-float, Malta/mips32r2 big-endian hard-float and soft-float, and
    MaltaEL/mips32r2 little-endian hard-float and soft-float.  The kernel C
    compile path still uses `-msoft-float -fomit-frame-pointer`; rootfs float
    mode is varied independently by the hard/soft gates.  Linpack smoke rates
    observed in QEMU: Malta64 hard 14039.914-13724.137 KFLOPS, Malta64 soft
    901.726-886.335 KFLOPS, Malta hard 7850.223-7822.841 KFLOPS, Malta soft
    923.245-925.258 KFLOPS, MaltaEL hard 7896.586-7836.592 KFLOPS, and
    MaltaEL soft 940.406-938.065 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - HI/LO post-`mflo` load-gap fill landed on 2026-07-08: the final MIPS
    peephole now moves an independent `lw` immediately after `mflo` into the
    preceding `mult`/`mflo` delay window for the exact
    `mult`; `nop`; `nop`; `mflo`; `lw`; simple-GPR-use shape.  The result
    keeps one spacer before `mflo`, does not move across labels or control
    transfers, does not put FP multiplies in delay slots, and leaves the final
    `-mfix4300` multiply-erratum repair unchanged.  This schedules useful
    integer load work into an existing VR4300 MCI gap; it does not rely on
    removing all MCI wait time.
  - Post-HI/LO-post-`mflo`-load-gap-fill `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2523 instructions
      and 195 `nop` to 2515 instructions and 187 `nop`; non-`nop`
      instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 130, multiply/divide stayed 61,
      load-followed-by-nop stayed 3, and bytes changed to 80277.  PCC/GCC
      instruction ratio is 2.210 and `nop` ratio is 4.452.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float stayed unchanged at 2730 PCC
      instructions and 149 `nop`; the current MIPS32R2 PCC/GCC instruction
      ratio remains 2.523 and `nop` ratio remains 29.800.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-HI/LO-post-`mflo`-load-gap-fill full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13935.358-13684.713 KFLOPS, Malta64 soft
    888.386-892.149 KFLOPS, Malta hard 7854.966-7846.653 KFLOPS, Malta soft
    930.383-933.912 KFLOPS, MaltaEL hard 7722.013-7857.359 KFLOPS, and
    MaltaEL soft 942.707-947.944 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - HI/LO post-`mfhi`/`mflo` nop trim landed on 2026-07-08: the final MIPS
    peephole now removes the conservative exact `mfhi`/`mflo`; `nop`; `nop`
    padding only on VR4300 and MIPS32R2 when the next two real lines are
    instructions and neither writes HI/LO.  The rule rejects nearby
    `mult`/`multu`/`dmult`/`dmultu`, `div`/`divu`/`ddiv`/`ddivu`,
    `mthi`/`mtlo`, and DSP-style `mul`/`madd`/`msub` writer opcodes.  It does
    not move instructions, does not cross labels or control transfers, does
    not put FP multiplies in delay slots, and leaves the final `-mfix4300`
    multiply-erratum repair unchanged.  For VR4300 this relies only on the
    manual-documented MCI interlock for the HI/LO read itself, while preserving
    explicit separation before an immediately following HI/LO writer.
  - Post-HI/LO-post-`mfhi`/`mflo`-nop-trim `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack changed from 2515 instructions and 187 `nop`
      to 2511 instructions and 183 `nop`; non-`nop` instructions stayed 2328,
      loads stayed 652, stores stayed 251, branch/jump/call delay `nop`
      stayed 130, multiply/divide stayed 61, load-followed-by-nop stayed 3,
      and bytes changed to 80257.  PCC/GCC instruction ratio is 2.207 and
      `nop` ratio is 4.357.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float changed from 2730 instructions and
      149 `nop` to 2726 instructions and 145 `nop`; non-`nop` instructions
      stayed 2581, loads stayed 863, stores stayed 352, branch/jump/call
      delay `nop` stayed 136, multiply/divide stayed 2,
      load-followed-by-nop stayed 3, and bytes changed to 81787.  PCC/GCC
      instruction ratio is 2.519 and `nop` ratio is 29.000.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-HI/LO-post-`mfhi`/`mflo`-nop-trim full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13669.211-13669.080 KFLOPS, Malta64 soft
    804.166-809.816 KFLOPS, Malta hard 9757.281-9686.442 KFLOPS, Malta soft
    1040.220-1033.837 KFLOPS, MaltaEL hard 9782.624-9433.580 KFLOPS, and
    MaltaEL soft 1056.429-1055.428 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - MIPS32R2 `mtc1`-to-convert spacer trim landed on 2026-07-08: the final
    MIPS peephole now removes the exact `mtc1`; `nop`;
    `cvt.s.w`/`cvt.d.w` gap only for selected CPU `MIPS32R2` when the
    conversion source FPR is the same FPR loaded by `mtc1`.  VR4300 is
    deliberately excluded: it keeps its FPU transfer/use spacer, the VR4300
    FPU compare-to-branch spacer, and the final `-mfix4300` multiply-erratum
    repair path unchanged.  The rule does not move instructions, does not
    cross labels or control transfers, and does not place FP multiplies in
    delay slots.
  - Post-MIPS32R2-`mtc1`-to-convert-spacer-trim `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack stayed at 2511 instructions and 183 `nop`;
      non-`nop` instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 130, multiply/divide stayed 61,
      load-followed-by-nop stayed 3, and bytes stayed 80257.  PCC/GCC
      instruction ratio stayed 2.207 and `nop` ratio stayed 4.357, confirming
      the MIPS32R2-only gate leaves VR4300 code generation unchanged.  Counter
      artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float changed from 2726 instructions and
      145 `nop` to 2720 instructions and 139 `nop`; non-`nop` instructions
      stayed 2581, loads stayed 863, stores stayed 352, branch/jump/call
      delay `nop` stayed 136, multiply/divide stayed 2,
      load-followed-by-nop stayed 3, and bytes changed to 81757.  PCC/GCC
      instruction ratio is 2.514 and `nop` ratio is 27.800.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-MIPS32R2-`mtc1`-to-convert-spacer-trim full PCC-kernel/PCC-rootfs
    QEMU correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13751.793-13632.969 KFLOPS, Malta64 soft
    808.957-802.896 KFLOPS, Malta hard 10535.652-10841.792 KFLOPS, Malta soft
    1044.301-1043.033 KFLOPS, MaltaEL hard 10904.605-11122.650 KFLOPS, and
    MaltaEL soft 1055.256-1057.483 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - Load-gap label-only skip trim landed on 2026-07-08: the final MIPS
    load-delay peephole can now decide a `load`; `nop` gap by looking past
    preserved blank, comment, and label-only lines to the next real
    instruction.  This removes return-value load spacers before epilogue
    labels without moving instructions, crossing directives, or crossing
    `label: instruction` lines.  It does not place FP multiplies in delay
    slots and leaves the final `-mfix4300` multiply-erratum repair unchanged.
  - Post-load-gap-label-only-skip-trim `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 PCC Linpack changed from 2511 instructions and 183 `nop`
      to 2508 instructions and 180 `nop`; non-`nop` instructions stayed 2328,
      loads stayed 652, stores stayed 251, branch/jump/call delay `nop`
      stayed 130, multiply/divide stayed 61, load-followed-by-nop fell from 3
      to 0, and bytes changed to 80242.  PCC/GCC instruction ratio is 2.204
      and `nop` ratio is 4.286.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float changed from 2720 instructions and
      139 `nop` to 2717 instructions and 136 `nop`; non-`nop` instructions
      stayed 2581, loads stayed 863, stores stayed 352, branch/jump/call
      delay `nop` stayed 136, multiply/divide stayed 2,
      load-followed-by-nop fell from 3 to 0, and bytes changed to 81742.
      PCC/GCC instruction ratio is 2.511 and `nop` ratio is 27.200.  Counter
      artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-load-gap-label-only-skip-trim full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13547.300-13682.168 KFLOPS, Malta64 soft
    810.029-808.229 KFLOPS, Malta hard 12069.746-12166.196 KFLOPS, Malta soft
    951.227-936.110 KFLOPS, MaltaEL hard 12133.423-12127.285 KFLOPS, and
    MaltaEL soft 989.520-985.235 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - MIPS32R2 `jr $ra` frame-load delay fill landed on 2026-07-08: the final
    control-delay peephole now special-cases exact `jr $ra` returns and can
    move an exact preceding `lw $fp,imm16($sp)` frame-pointer restore into the
    already-present return delay-slot `nop`.  The rule is MIPS32R2-only because
    that profile has load interlocks; VR4300 is intentionally excluded because
    the frame-pointer restore currently separates `lw $ra` from `jr $ra` for
    load-delay safety.  The rule does not schedule FP multiplies, does not
    affect the VR4300 `-mfix4300` multiply-erratum repair, and does not enable
    the still-open MIPS32R2 store-in-delay-slot policy.
  - Post-MIPS32R2-`jr $ra`-frame-load-delay-fill `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack stayed at 2508 instructions and 180 `nop`;
      non-`nop` instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 130, multiply/divide stayed 61,
      load-followed-by-nop stayed 0, and bytes stayed 80242.  PCC/GCC
      instruction ratio is 2.204 and `nop` ratio is 4.286.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float changed from 2717 instructions and
      136 `nop` to 2704 instructions and 123 `nop`; non-`nop` instructions
      stayed 2581, loads stayed 863, stores stayed 352, branch/jump/call
      delay `nop` fell from 136 to 123, multiply/divide stayed 2,
      load-followed-by-nop stayed 0, and bytes changed to 81677.  PCC/GCC
      instruction ratio is 2.499 and `nop` ratio is 24.600.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-MIPS32R2-`jr $ra`-frame-load-delay-fill full PCC-kernel/PCC-rootfs
    QEMU correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-08:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13742.076-13897.396 KFLOPS, Malta64 soft
    804.240-812.731 KFLOPS, Malta hard 13290.916-13446.358 KFLOPS, Malta soft
    944.339-952.109 KFLOPS, MaltaEL hard 13493.201-13597.383 KFLOPS, and
    MaltaEL soft 974.199-945.726 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - VR4300 `jr $ra` frame-load delay fill landed on 2026-07-09: the same
    final control-delay peephole now allows selected CPU `VR4300` to use the
    exact `lw $fp,imm16($sp)` frame-pointer restore in the return delay slot.
    The accepted form remains narrow: opcode `lw`, destination `$fp`, base
    `$sp`, and a numeric base-offset operand.  It does not enable general
    return-delay loads/stores, does not schedule FP multiplies, and leaves the
    `-mfix4300`/`-mno-fix4300` erratum repair unchanged.  The VR4300 safety
    argument is the already landed integer load-use interlock cleanup; the
    `$fp` restore is no longer needed as a software spacer for the preceding
    `$ra` load.
  - Post-VR4300-`jr $ra`-frame-load-delay-fill `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack changed from the previous 2503 instructions
      and 175 `nop` to 2490 instructions and 162 `nop`; non-`nop`
      instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` fell from 125 to 112, multiply/divide
      stayed 61, load-followed-by-nop stayed 0, and bytes changed to 80152.
      PCC/GCC instruction ratio is 2.188 and `nop` ratio is 3.857.  Counter
      artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float stayed at 2692 instructions and
      111 `nop`; non-`nop` instructions stayed 2581, loads stayed 863,
      stores stayed 352, branch/jump/call delay `nop` stayed 111,
      multiply/divide stayed 2, load-followed-by-nop stayed 0, and bytes
      stayed 81617.  PCC/GCC instruction ratio is 2.488 and `nop` ratio is
      22.200.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-VR4300-`jr $ra`-frame-load-delay-fill full PCC-kernel/PCC-rootfs
    QEMU correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-09:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 13123.716-12896.576 KFLOPS, Malta64 soft
    793.697-796.642 KFLOPS, Malta hard 12444.482-12667.766 KFLOPS,
    Malta soft 970.488-973.769 KFLOPS, MaltaEL hard
    12929.610-13041.958 KFLOPS, and MaltaEL soft 984.778-938.112 KFLOPS.
    Malta64 `runtime-quick` still reports the known QEMU memory-fault
    diagnostic but exits with `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - MIPS32R2 aligned frame/stack hard-float double memory operations landed
    on 2026-07-09: when the hard-float 64-bit memory ABI path is split because
    `MIPS_DATA_ALIGN64 <= SZINT`, codegen now uses `ldc1`/`sdc1` only for OREG
    operands that are proven backend-controlled stack or frame references.
    The accepted form is intentionally narrow: after `mips_adjust_frame_ref()`
    the base must be `$fp` or `$sp`, and the offset must be 8-byte aligned.
    Unknown object and pointer-based addressing still uses the existing
    endian-correct `lwc1`/`swc1` pair, preserving the o32 4-byte-aligned
    double ABI case.  VR4300 is unchanged because its non-split path already
    uses `l.d`/`s.d`; soft-float is unchanged; and the VR4300
    `-mfix4300`/`-mno-fix4300` multiply-erratum repair is unaffected because
    this is memory-operation selection, not FP multiply scheduling.
  - Post-MIPS32R2-aligned-frame-double-memory `pcc-perf-baseline` assembly
    counters:
    - Malta64/VR4300 PCC Linpack stayed at 2490 instructions and 162 `nop`;
      non-`nop` instructions stayed 2328, loads stayed 652, stores stayed 251,
      branch/jump/call delay `nop` stayed 112, multiply/divide stayed 61,
      load-followed-by-nop stayed 0, and bytes stayed 80152.  PCC/GCC
      instruction ratio is 2.188 and `nop` ratio is 3.857.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float changed from 2692 instructions and
      111 `nop` to 2575 instructions and 113 `nop`; non-`nop` instructions
      fell from 2581 to 2462, loads fell from 863 to 800, stores fell from
      352 to 296, branch/jump/call delay `nop` changed from 111 to 113,
      multiply/divide stayed 2, load-followed-by-nop stayed 0, and bytes fell
      from 81617 to 79662.  PCC/GCC instruction ratio is 2.380 and `nop`
      ratio is 22.600.  The perf assembly contains 63 `ldc1` and 56 `sdc1`
      uses, all through `$fp`, with no pointer/object-addressed
      `ldc1`/`sdc1`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-MIPS32R2-aligned-frame-double-memory full PCC-kernel/PCC-rootfs QEMU
    correctness matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-09:
    Malta64/VR4300 hard-float and soft-float, Malta/mips32r2 big-endian
    hard-float and soft-float, and MaltaEL/mips32r2 little-endian hard-float
    and soft-float.  The kernel C compile path still uses
    `-msoft-float -fomit-frame-pointer`; rootfs float mode is varied
    independently by the hard/soft gates.  Linpack smoke rates observed in
    QEMU: Malta64 hard 9359.083-9388.515 KFLOPS, Malta64 soft
    575.837-578.959 KFLOPS, Malta hard 9548.831-9111.277 KFLOPS, Malta soft
    695.452-700.837 KFLOPS, MaltaEL hard 9468.185-9646.561 KFLOPS, and
    MaltaEL soft 701.301-695.853 KFLOPS.  Malta64 `runtime-quick` still
    reports the known QEMU memory-fault diagnostic but exits with
    `PCC_SMOKE_RC runtime-quick 0`.  Logs:
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta64-big.vr4300.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.hard.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-malta-big.mips32r2.soft.elf.log`,
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.hard.elf.log`,
    and
    `/private/tmp/rebsd-pcc-smoke-all-maltael-little.mips32r2.soft.elf.log`.
  - MIPS call-argument liveness and conservative leaf `$ra` handling landed on
    2026-07-09.  `livecall()` now reports the actual register arguments that
    have already been lowered into the call tree, including paired MIPS
    argument pseudos.  This fixed a real target-side `w/prtat()` failure where
    a live `$a1` call argument was reused before `jal prttime`, corrupting the
    string tail pointer.
  - Leaf functions now omit the prologue/epilogue `$ra` save/restore whenever
    pass2 sees a leaf, including `-fomit-frame-pointer` kernel-style builds.
    The omit-frame epilogue skips the `$ra` reload for those leaf functions and
    keeps the final 16-byte ABI stack pop in the `jr $ra` delay slot.
  - Hidden backend helper calls that can appear in functions pass2 still marks
    as leaf are wrapped with a leaf-only `$ra` spill/reload: structure-copy
    `memcpy`, soft-float helpers, integer emulation helpers, and the ReBSD
    hard-float `double -> float` `__truncdfsf2` table template.  This fixed the
    `types-smoke` `close_float()` crash caused by a hidden helper `jal`
    clobbering an omitted `$ra`.
  - No VR4300 FP multiply scheduling changed here.  The
    `-mfix4300`/`-mno-fix4300` erratum workaround still owns FP multiply
    adjacency and delay-slot policy.
  - Post-livecall/leaf-RA `pcc-perf-baseline` assembly counters:
    - Malta64/VR4300 hard-float Linpack is GCC `1138` instructions / `42`
      `nop` versus PCC `2490` instructions / `162` `nop`; PCC loads/stores
      are `645/244`, branch/jump/call delay `nop` is `112`, bytes are `80243`,
      and PCC/GCC instruction ratio is `2.188`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float Linpack is GCC `1082`
      instructions / `5` `nop` versus PCC `2575` instructions / `113` `nop`;
      PCC loads/stores are `793/289`, branch/jump/call delay `nop` is `113`,
      bytes are `79753`, and PCC/GCC instruction ratio is `2.380`.  Counter
      artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-livecall/leaf-RA full PCC-kernel/PCC-rootfs QEMU correctness matrix
    passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-09: Malta64/VR4300 hard-float
    and soft-float, Malta/mips32r2 big-endian hard-float and soft-float, and
    MaltaEL/mips32r2 little-endian hard-float and soft-float.  The kernel C
    compile path still covers `-msoft-float -fomit-frame-pointer`; rootfs float
    mode is varied independently by the hard/soft gates.  Wrapper logs:
    `/private/tmp/rebsd-livecall-helperra-malta64-hard-clean.log`,
    `/private/tmp/rebsd-livecall-helperra-malta64-soft.log`,
    `/private/tmp/rebsd-livecall-helperra-malta-hard.log`,
    `/private/tmp/rebsd-livecall-helperra-malta-soft.log`,
    `/private/tmp/rebsd-livecall-helperra-maltael-hard.log`, and
    `/private/tmp/rebsd-livecall-helperra-maltael-soft.log`.
  - Omit-FP leaf `$ra` enablement landed on 2026-07-09.  A Malta64 kernel
    assembly audit after the soft-float gate found 1056 generated kernel
    functions: 287 no-`jal` leaf-like functions had no `sw/lw $ra`, and the
    remaining 769 call-path functions still saved and restored `$ra`.  The full
    PCC-kernel/PCC-rootfs matrix passed again with `PCC_SMOKE_ALL_RC:0`;
    wrapper logs:
    `/private/tmp/rebsd-omitfp-leaf-malta64-hard.log`,
    `/private/tmp/rebsd-omitfp-leaf-malta64-soft.log`,
    `/private/tmp/rebsd-omitfp-leaf-malta-hard.log`,
    `/private/tmp/rebsd-omitfp-leaf-malta-soft.log`,
    `/private/tmp/rebsd-omitfp-leaf-maltael-hard.log`, and
    `/private/tmp/rebsd-omitfp-leaf-maltael-soft.log`.
  - Omit-FP callee-save slot trimming landed on 2026-07-09.  Frame sizing now
    uses pass2's `p2env.p_regs[0]` in omit-frame mode instead of reserving
    slots for all `$s0..$s7`.  The full PCC-kernel/PCC-rootfs matrix passed
    again with `PCC_SMOKE_ALL_RC:0`; wrapper logs:
    `/private/tmp/rebsd-used-sregs-malta64-hard.log`,
    `/private/tmp/rebsd-used-sregs-malta64-soft.log`,
    `/private/tmp/rebsd-used-sregs-malta-hard.log`,
    `/private/tmp/rebsd-used-sregs-malta-soft.log`,
    `/private/tmp/rebsd-used-sregs-maltael-hard.log`, and
    `/private/tmp/rebsd-used-sregs-maltael-soft.log`.
  - HI/LO post-`mflo` stack-load fill relaxation landed on 2026-07-09.  The
    final MIPS peephole still handles only the exact
    `mult`; `nop`; `nop`; `mflo`; `lw imm16(base)`; simple-use shape, but now
    recognizes that the original `mult` source GPRs may be overwritten after
    the multiply has issued.  It still rejects any moved `lw` whose destination
    or base register is the `mflo`/`mfhi` result register, keeps one executed
    spacer before the HI/LO read, does not move across labels or control
    transfers, does not put FP multiplies into delay slots, and leaves the
    `-mfix4300`/`-mno-fix4300` FP multiply erratum repair unchanged.
  - Post-relaxed-HI/LO-post-`mflo`-load-fill `pcc-perf-baseline` counters:
    - Malta64/VR4300 hard-float Linpack changed from the previous PCC `2490`
      instructions / `162` `nop` to `2486` instructions / `158` `nop`; GCC
      remains `1138` instructions / `42` `nop`, PCC loads/stores are `645/244`,
      branch/jump/call delay `nop` stayed `112`, bytes are `80223`, and
      PCC/GCC instruction ratio is `2.185`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float Linpack stayed at PCC `2575`
      instructions / `113` `nop` versus GCC `1082` instructions / `5` `nop`;
      PCC loads/stores stayed `793/289`, branch/jump/call delay `nop` stayed
      `113`, bytes stayed `79753`, and PCC/GCC instruction ratio stayed
      `2.380`.  This change therefore only affects the VR4300-style
      `mult`/`mflo` path measured here.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-relaxed-HI/LO-post-`mflo`-load-fill full PCC-kernel/PCC-rootfs QEMU
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-09: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    The kernel C compile path still covers `-fomit-frame-pointer`, and the
    soft gates cover `-msoft-float`.  Wrapper logs:
    `/private/tmp/rebsd-hilo-lw-malta64-hard.log`,
    `/private/tmp/rebsd-hilo-lw-malta64-soft.log`,
    `/private/tmp/rebsd-hilo-lw-malta-hard.log`,
    `/private/tmp/rebsd-hilo-lw-malta-soft.log`,
    `/private/tmp/rebsd-hilo-lw-maltael-hard.log`, and
    `/private/tmp/rebsd-hilo-lw-maltael-soft.log`.
  - Zero-move chain fold landed on 2026-07-09.  The final MIPS peephole now
    folds only the exact adjacent `move tmp,$zero`; `move dst,tmp` shape into
    `move dst,$zero`, and only when the following real instruction, optionally
    after one local label, writes `tmp` before reading it.  The rule does not
    cross control transfers, does not guess general liveness, does not move
    memory operations, does not alter FP multiply scheduling, and leaves the
    `-mfix4300`/`-mno-fix4300` VR4300 FP multiply erratum repair unchanged.
  - Post-zero-move-chain-fold `pcc-perf-baseline` counters:
    - Malta64/VR4300 hard-float Linpack changed from the previous PCC `2486`
      instructions / `158` `nop` to `2480` instructions / `158` `nop`; GCC
      remains `1138` instructions / `42` `nop`, PCC loads/stores are `645/244`,
      branch/jump/call delay `nop` stayed `112`, bytes are `80037`, and
      PCC/GCC instruction ratio is `2.179`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float Linpack changed from the previous
      PCC `2575` instructions / `113` `nop` to `2569` instructions / `113`
      `nop`; GCC remains `1082` instructions / `5` `nop`, PCC loads/stores
      are `793/289`, branch/jump/call delay `nop` stayed `113`, bytes are
      `79567`, and PCC/GCC instruction ratio is `2.374`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-zero-move-chain-fold full PCC-kernel/PCC-rootfs QEMU matrix passed
    with `PCC_SMOKE_ALL_RC:0` on 2026-07-09: Malta64/VR4300 hard-float and
    soft-float, Malta/mips32r2 big-endian hard-float and soft-float, and
    MaltaEL/mips32r2 little-endian hard-float and soft-float.  The kernel C
    compile path still covers `-fomit-frame-pointer`, and the soft gates cover
    `-msoft-float`.  Wrapper logs:
    `/private/tmp/rebsd-zero-move-malta64-hard.log`,
    `/private/tmp/rebsd-zero-move-malta64-soft.log`,
    `/private/tmp/rebsd-zero-move-malta-hard.log`,
    `/private/tmp/rebsd-zero-move-malta-soft.log`,
    `/private/tmp/rebsd-zero-move-maltael-hard.log`, and
    `/private/tmp/rebsd-zero-move-maltael-soft.log`.
  - Move-`addiu` temporary fold landed on 2026-07-09.  The final MIPS
    peephole now folds only `move tmp,src`; `addiu dst,tmp,imm` into
    `addiu dst,src,imm` when `dst != tmp` and `tmp` is explicitly killed
    before any read, either by the following instruction or after one
    independent non-control instruction.  It does not cross labels or control
    transfers, does not infer general liveness, does not move memory
    operations, and leaves the post-pass `-mfix4300`/`-mno-fix4300` VR4300 FP
    multiply erratum repair unchanged.
  - Post-move-`addiu`-temporary-fold `pcc-perf-baseline` counters:
    - Malta64/VR4300 hard-float Linpack changed from the previous PCC `2480`
      instructions / `158` `nop` to `2478` instructions / `158` `nop`; GCC
      remains `1138` instructions / `42` `nop`, PCC loads/stores are `645/244`,
      branch/jump/call delay `nop` stayed `112`, bytes are `80009`, and
      PCC/GCC instruction ratio is `2.178`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta64-big.vr4300.hard.elf/linpack-asm-summary.json`.
    - Malta/mips32r2 big-endian hard-float Linpack changed from the previous
      PCC `2569` instructions / `113` `nop` to `2567` instructions / `113`
      `nop`; GCC remains `1082` instructions / `5` `nop`, PCC loads/stores
      are `793/289`, branch/jump/call delay `nop` stayed `113`, bytes are
      `79539`, and PCC/GCC instruction ratio is `2.372`.  Counter artifact:
      `/private/tmp/rebsd-pcc-perf-malta-big.mips32r2.hard.elf/linpack-asm-summary.json`.
  - Post-move-`addiu`-temporary-fold full PCC-kernel/PCC-rootfs QEMU matrix
    passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-09: Malta64/VR4300 hard-float
    and soft-float, Malta/mips32r2 big-endian hard-float and soft-float, and
    MaltaEL/mips32r2 little-endian hard-float and soft-float.  The kernel C
    compile path still covers `-fomit-frame-pointer`, and the soft gates cover
    `-msoft-float`.  Wrapper logs:
    `/private/tmp/rebsd-addiu-tmp-malta64-hard.log`,
    `/private/tmp/rebsd-addiu-tmp-malta64-soft.log`,
    `/private/tmp/rebsd-addiu-tmp-malta-hard.log`,
    `/private/tmp/rebsd-addiu-tmp-malta-soft.log`,
    `/private/tmp/rebsd-addiu-tmp-maltael-hard.log`, and
    `/private/tmp/rebsd-addiu-tmp-maltael-soft.log`.
  - Do not land text-only adjacent stack store/reload folds for `sw`/`lw` or
    `s.d`/`l.d` unless the transformation can prove the load is not a
    volatile access.  A trial postprocess fold found small Linpack wins, but a
    direct check with `volatile int x; x=y; return x;` and
    `volatile double x; x=y; return x;` shows PCC emits adjacent `$fp`
    store/reload pairs without a volatile marker in the final assembly.  Such
    folds must move into pass2 or another IR-aware location where volatile
    object accesses are still distinguishable from compiler spills.
  - The unsafe postprocess FP store/reload fold was reverted on 2026-07-09.
    A direct `volatile double` compile after the revert keeps `s.d` followed
    by a real `l.d`, restoring the volatile read.  The safe perf baseline is
    again Malta64/VR4300 PCC `2478` instructions / `158` `nop`, `645/244`
    loads/stores, bytes `80009`, PCC/GCC ratio `2.178`; Malta/mips32r2 stays
    at PCC `2567` instructions / `113` `nop`, `793/289` loads/stores, bytes
    `79539`, PCC/GCC ratio `2.372`.
  - Post-revert full PCC-kernel/PCC-rootfs QEMU matrix passed with
    `PCC_SMOKE_ALL_RC:0` on 2026-07-09: Malta64/VR4300 hard-float and
    soft-float, Malta/mips32r2 big-endian hard-float and soft-float, and
    MaltaEL/mips32r2 little-endian hard-float and soft-float.  Wrapper logs:
    `/private/tmp/rebsd-revert-fp-store-load-malta64-hard.log`,
    `/private/tmp/rebsd-revert-fp-store-load-malta64-soft.log`,
    `/private/tmp/rebsd-revert-fp-store-load-malta-hard.log`,
    `/private/tmp/rebsd-revert-fp-store-load-malta-soft.log`,
    `/private/tmp/rebsd-revert-fp-store-load-maltael-hard.log`, and
    `/private/tmp/rebsd-revert-fp-store-load-maltael-soft.log`.
  - An IR-aware hard-float stack reload fold is being reintroduced in pass2
    instead of the final assembly postprocessor.  It rewrites only adjacent
    non-volatile hard-float frame reloads before register allocation, keeps the
    original store in place, and uses PCC qualifiers to reject volatile objects
    before assembly has lost that information.  Probe result: non-volatile
    `double x; x=y; return x;` changes the reload into `mov.d`, while
    `volatile double x; x=y; return x;` still emits a real `l.d`; soft-float is
    unaffected.  Current Linpack perf counters are unchanged: Malta64/VR4300
    hard-float remains PCC `2478` instructions / `158` `nop`, `645/244`
    loads/stores, bytes `80009`, PCC/GCC ratio `2.178`; Malta/mips32r2
    hard-float remains PCC `2567` instructions / `113` `nop`, `793/289`
    loads/stores, bytes `79539`, PCC/GCC ratio `2.372`.  A first Malta64 perf
    attempt was invalidated by a parallel build race; the sequential retry
    passed.
  - Post-pass2 hard-float stack reload fold full PCC-kernel/PCC-rootfs QEMU
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-09: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    The kernel C compile path still covers `-fomit-frame-pointer`, and the
    soft gates cover `-msoft-float`.  Wrapper logs:
    `/private/tmp/rebsd-pass2-fp-reload-malta64-hard.log`,
    `/private/tmp/rebsd-pass2-fp-reload-malta64-soft.log`,
    `/private/tmp/rebsd-pass2-fp-reload-malta-hard.log`,
    `/private/tmp/rebsd-pass2-fp-reload-malta-soft.log`,
    `/private/tmp/rebsd-pass2-fp-reload-maltael-hard.log`, and
    `/private/tmp/rebsd-pass2-fp-reload-maltael-soft.log`.
  - Extend the same pass2 fold to proven 32-bit word/pointer frame reloads.
    Adjacent self-reloads can now be deleted, non-self adjacent reloads can be
    rewritten to moves, and an immediate `CBRANCH` can branch on the stored
    `TEMP`/`REG` instead of reloading the same frame slot.  The fold remains
    intentionally narrow: no labels between the store and user, no
    byte/short/long-long slots, no volatile trees, and no branch condition
    with more than one matching frame reload.  Probe result: the common
    `getenv()` pattern changes from `sw; lw; beqz` to `sw; beqz`, while
    volatile integer and floating local reload probes still emit real loads.
    Current Linpack perf counters: Malta64/VR4300 hard-float PCC `2476`
    instructions / `158` `nop`, `643/244` loads/stores, bytes `79919`,
    PCC/GCC ratio `2.176`; Malta/mips32r2 hard-float PCC `2565`
    instructions / `113` `nop`, `791/289` loads/stores, bytes `79449`,
    PCC/GCC ratio `2.371`.
  - Post-pass2 word/pointer branch reload fold full PCC-kernel/PCC-rootfs QEMU
    matrix passed with `PCC_SMOKE_ALL_RC:0` on 2026-07-09: Malta64/VR4300
    hard-float and soft-float, Malta/mips32r2 big-endian hard-float and
    soft-float, and MaltaEL/mips32r2 little-endian hard-float and soft-float.
    The kernel C compile path still covers `-fomit-frame-pointer`, and the
    soft gates cover `-msoft-float`.  Wrapper logs:
    `/private/tmp/rebsd-branch-reload-malta64-hard.log`,
    `/private/tmp/rebsd-branch-reload-malta64-soft.log`,
    `/private/tmp/rebsd-branch-reload-malta-hard.log`,
    `/private/tmp/rebsd-branch-reload-malta-soft.log`,
    `/private/tmp/rebsd-branch-reload-maltael-hard.log`, and
    `/private/tmp/rebsd-branch-reload-maltael-soft.log`.
  - Clean Phase 0 follow-up correctness work completed on 2026-07-10 before
    further optimization.  F0-F18 are now modeled as caller-clobbered
    temporaries while F20 is unavailable to the allocator, so nested FP calls
    cannot retain live values in an unsaved callee-saved register.  Hidden FP
    conversion helper calls reserve caller-clobbered GPRs, unsigned 32-bit to
    FP conversions use `__floatunsi*`, and variable 64-bit shifts are emitted
    inline instead of exposing unmodeled helper calls to register allocation.
  - The same correction set emits aligned `.comm` declarations, retains
    `.eh_frame` in user links, and separates MIPS data, integer-pair argument,
    and FP64 argument alignment.  MIPS32R2 keeps its existing 4-byte data and
    `long long` slot layout, while `double`/`long double` use even o32 argument
    slots and matching formal/vararg offsets.  VR4300 remains 8-byte aligned.
  - Added `misc/nestedcall001`, `misc/intfp001`, and `misc/llshift001` for the
    nested FP call, unsigned/64-bit FP conversion, and multiple variable-shift
    cases.  Host compile regression passed `318/321` with only the three
    documented expected failures; cross runtime passed `281/281`; native PCC
    passed `291` compilations plus `30` expected compile failures and `281/281`
    runtimes with `NATIVE_PCC_REGRESS_RC:0`.
  - Clean sequential PCC-kernel/PCC-rootfs QEMU gates all reported
    `PCC_SMOKE_ALL_FAILURES 0` and `PCC_SMOKE_ALL_RC:0`: Malta64/VR4300 hard
    and soft, Malta/MIPS32R2 big-endian hard and soft, and MaltaEL/MIPS32R2
    little-endian hard and soft.  The kernel path used `-msoft-float` and
    `-fomit-frame-pointer` in every profile.  Logs are
    `/private/tmp/pcc-phase0-postfix3-malta64-{hard,soft}-build.log`,
    `/private/tmp/pcc-phase0-postfix3-malta-{hard,soft}-build.log`, and
    `/private/tmp/pcc-phase0-postfix3-maltael-{hard,soft}-build.log`.
  - QEMU Linpack PCC ranges from those runs were Malta64 hard
    `13544-13651`, Malta64 soft `965-992`, Malta hard `13618-13729`, Malta
    soft `1023-1024`, MaltaEL hard `13674-13701`, and MaltaEL soft
    `1047-1061` KFLOPS.  QEMU does not reproduce the VR4300 multiply erratum;
    real N64 validation of this source state remains required.
  - A direct post-matrix assembly probe confirmed that VR4300 default/explicit
    `-mfix4300` inserts the erratum `nop`, `-mno-fix4300` removes it, and an
    explicit `-mfix4300` does not apply the repair to `-march=mips32r2`.
  - Continue landing remaining memory/FPU scheduling work in small patches,
    each followed by PCC regressions plus Linpack smoke on Malta/Malta64 and,
    when relevant, N64 hardware validation.
- [ ] Add a compact late MIPS peephole/scheduling pass.
  - Use a short instruction window, initially 3-5 instructions, after final
    instruction selection.
  - Fill branch delay slots with safe independent work such as address updates,
    loop counter updates, invariant moves, or stores whose ordering is known
    safe.
  - Separate load-use and dependent FP operations when an independent
    instruction is available; otherwise leave a real `nop`.
  - Prefer refusing uncertain transformations over creating backend-specific
    correctness risk.
  - [x] First Milestone C window: enable the existing dependency-checked
    `candidate; load; nop; dependent-use` reorder for MIPS32R2 (commit
    `11f66467`).  Candidates remain limited to parsed shifts, moves,
    signed-imm16 `li` lowered to `addiu`, and simple GPR ALU operations before
    FPU loads.  Unknown instructions, control transfers, memory candidates,
    register conflicts, and code already in a delay slot are rejected.
  - MIPS32R2 Linpack contains 47 changed windows.  Two exposed moves are then
    placed in integer branch delay slots by the existing late peephole, moving
    static counts from `2549/112` to `2547/110` instructions/nops.  Loads and
    stores stay `772/293`.  VR4300 remains byte-identical at `2475/157` with
    `643/244` loads/stores.
  - Cross and native regressions pass `292/292`.  All six PCC-kernel/
    PCC-rootfs QEMU profiles pass with one `PCC_SMOKE_ALL_OK`,
    `PCC_SMOKE_ALL_FAILURES 0`, and `PCC_SMOKE_ALL_RC:0` each.  Full details
    are in `docs/PCC_MIPS_SCHEDULER_REPORT.md`.
  - [x] Second Milestone C window: move one parsed pure GPR operation across
    one independent load into an integer branch delay slot (commit
    `668a7d88`).  The rule rejects `$sp`/`$ra`, register conflicts, unknown
    loads or branches, labels, non-GPR candidates, and every CPU profile other
    than VR4300 or MIPS32R2.  It never reorders two memory operations.
  - C2 fills two additional Linpack branch slots on each CPU.  VR4300 moves
    from `2475/157` to `2473/155` instructions/nops; MIPS32R2 moves from the
    C1 `2547/110` to `2545/108`.  Load/store counts remain `643/244` and
    `772/293`, respectively.  Cross/native runtime stays `292/292`, and all
    six PCC-kernel/PCC-rootfs QEMU profiles pass with zero failures.
  - Real N64 hardware validation of the C2 hard-float a.out image passed on
    2026-07-11 with `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and
    the final `N64_PCC_DEBUG_RC_END` marker.  The supplied final marker had no
    numeric value; a distinct `-mno-fix4300` hardware run remains pending.
  - [x] Third Milestone C window: parse exact binary FPU read/write sets and
    move one following `lw` between an immediately dependent FPU producer and
    consumer (commit `0331d3cf`).  The consumer supplies the load delay gap,
    so the old load `nop` is removed.  The rule never reorders two memory
    operations, rejects `$zero`/`$sp`/`$ra` load destinations, is limited to
    VR4300 and MIPS32R2, and leaves generic MIPS3/R4000 unchanged.
  - C3 schedules 11 Linpack dependency windows on each CPU while keeping
    static instruction/nop/load/store totals at VR4300 `2473/155/643/244` and
    MIPS32R2 `2545/108/772/293`.  Cross and native runtime pass `292/292`; all
    six PCC-kernel/PCC-rootfs QEMU profiles pass with zero failures.  The N64
    hard-float default-`-mfix4300` image is
    `sys/mips/n64/builds/20260711-milestone-c3-fpu-latency-schedule/pcc-debug.z64`;
    real hardware validation passed on 2026-07-11 with
    `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and the final
    `N64_PCC_DEBUG_RC_END` marker.  The supplied final marker had no numeric
    value, so none is inferred.
  - [x] Fourth Milestone C window: move one exactly parsed pure GPR operation
    from after `mflo`/`mfhi` into a two-nop integer multiply gap (commit
    `3c2092b7`).  Read/write checks reject the HI/LO result, `$sp`, `$ra`,
    `$zero` destinations, memory, control transfers, and unknown syntax.
    Generic MIPS3/R4000 remains unchanged.
  - C4 fills two VR4300 Linpack gaps, reducing instructions/nops from
    `2473/155` to `2471/153` while loads/stores remain `643/244`.  MIPS32R2 is
    byte-identical to C3 at `2545/108/772/293` because its real `mul` already
    has equivalent scheduling.  Cross and native runtime pass `292/292`; all
    six PCC-kernel/PCC-rootfs QEMU profiles pass with zero failures.
  - The C4 N64 hard-float default-`-mfix4300` image is
    `sys/mips/n64/builds/20260711-milestone-c4-hilo-gap-schedule/pcc-debug.z64`;
    real hardware validation passed on 2026-07-11 with
    `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and the final
    `N64_PCC_DEBUG_RC_END` marker.  The supplied final marker had no numeric
    value, so none is inferred.
  - [x] C5 uses the VR4300 hardware Multicycle Interlock for compiler-generated
    32-bit `mult`/`multu` (commit `af5be45d`).  VR4300 User's Manual Table 3-12
    and section 4.6.4 specify a five-cycle whole-pipeline stall, so the generic
    two software nops before `mflo` execute after the hardware wait and are
    redundant.  Generic MIPS3/R4000 retains both nops; MIPS32R2 retains `mul`.
  - Current VR4300 Linpack changes from `2465/2314/151/639/244/79719` to
    `2443/2314/129/639/244/79609`
    instructions/non-nops/nops/loads/stores/assembly-bytes.  MIPS32R2 remains
    `2473/2367/106/558/276/71207`.  Cross/native runtime pass 292/292 and all
    six PCC-kernel/PCC-rootfs QEMU profiles pass.
  - The clean C5 GCC-kernel/PCC-hard-float-a.out-userland N64 image is
    `sys/mips/n64/builds/20260711-milestone-c5-vr4300-mult-interlock-gcc-kernel/pcc-debug.z64`,
    SHA-256
    `ef2577312ea293f24aea77bcce2ea4f1c6d257cdc6076b64187f8d0ddc4b66fb`.
    It uses build stamp `.build-mode.gcc.1.0.0.1` and default `-mfix4300`;
    physical VR4300 validation passed on 2026-07-11 with
    `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and the final
    `N64_PCC_DEBUG_RC_END` marker.
  - Clean-build audit correction, 2026-07-11: the N64 build-mode stamp did not
    include the selected kernel compiler.  The archived C2 through D1 images
    could reuse GCC kernel objects while `vers.o` reported `with pcc`; the D1
    `main` prologue exactly matches a fresh GCC `-O2` compile.  Their hardware
    results validate PCC userland/debug execution, not a PCC-built kernel.
    Commit `776e41af` adds `N64_KERNEL_COMPILER` to the stamp.  Future N64 PCC
    kernel gates must be clean builds or carry a `.build-mode.pcc.*` stamp.
- [x] Reduce avoidable frame-pointer and outgoing-argument stack traffic.
  - [x] First Milestone D substep: reserve a fixed 16-byte o32 home area for
    `-fomit-frame-pointer` non-leaf functions whose calls use register
    arguments only (commit `34c0b9d8`).  The area is placed below every
    local, spill, and callee-save slot; offsets `0..15($sp)` are never used
    for caller-private data.
  - `FUNARG`, `STARG`, `alloca`, raw frame addresses, explicit `$fp`
    assignment, varargs, and address-taken locals keep the old stable `$fp`
    and dynamic call lowering.  Permanent VR4300/MIPS32R2 probes compile and
    assemble all five boundary cases.
  - Normal Linpack remains byte-identical to C4.  With
    `-fomit-frame-pointer`, VR4300 changes from `2453/153/636/237` to
    `2425/175/634/235`, and MIPS32R2 changes from `2527/108/765/286` to
    `2493/124/763/284` instructions/nops/loads/stores.  Cross/native runtime
    pass `292/292`; all six PCC-kernel/PCC-rootfs QEMU profiles pass.
  - The D1 N64 hard-float default-`-mfix4300` image is
    `sys/mips/n64/builds/20260711-milestone-d1-fixed-call-area/pcc-debug.z64`;
    its userland/debug hardware validation passed on 2026-07-11 with
    `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and the final
    `N64_PCC_DEBUG_RC_END` marker.  The supplied final marker had no numeric
    value, so none is inferred.  The clean-build audit found stale GCC kernel
    objects, so this is not PCC-kernel validation.
  - [x] D2 extends the fixed outgoing area to ordinary scalar stack arguments
    (commits `194d264e` and `5aa462ed`).  Pass2 scans ABI-lowered `FUNARG`
    nodes and reserves the maximum 16-byte o32 home area plus stack slots and
    alignment padding.  `lastcall` and the existing argument emitters compute
    ABI offsets beginning at `16($sp)` without writing target data into the C
    qualifier field `NODE.n_qual`.  Eligible calls store directly into that
    area without per-call `$sp` decrement/restore pairs.
  - `STARG`, raw frame addresses, `alloca`, explicit `$fp`, aggregate calls,
    and variadic callees retain the old stable `$fp` and dynamic lowering.
    Nested scalar calls and variadic callers use the fixed area.  Permanent
    VR4300/MIPS32R2 probes cover register-only, scalar, 64-bit, variadic,
    nested, aggregate, address-taken, and `alloca` cases.
  - Normal Linpack remains byte-identical.  Omit-FP VR4300 changes from D1
    `2425/2250/175/634/235` to `2367/2182/185/632/233`, and MIPS32R2 changes
    from `2493/2369/124/763/284` to `2435/2301/134/761/282`
    instructions/non-nops/nops/loads/stores.  Both remove 68 non-nops, two
    loads, and two stores.  Only `linpack` and `second` still require `$fp`.
  - Cross and native runtime pass 292/292 with no unexpected failures.  All
    six final PCC-kernel/PCC-rootfs QEMU profiles pass with exactly one
    `PCC_SMOKE_ALL_OK`, zero failures, and `PCC_SMOKE_ALL_RC:0`; kernels use
    `-msoft-float -fomit-frame-pointer`, and hard/soft userland was tested.
  - The first clean D2 PCC-kernel image reached the stage0 jump on N64 but
    emitted no kernel banner.  ReBSD ELF `ld` selected the later weak
    `console_null` definition instead of N64cart UART.  Commit `776e41af`
    preserves the first weak definition, adds a big/little-endian permanent
    link probe, and includes the kernel compiler in the N64 build stamp.
  - The corrected clean PCC-kernel/PCC-userland hard-float default-`-mfix4300`
    image is
    `sys/mips/n64/builds/20260711-milestone-d2-clean-pcc-fix/pcc-debug.z64`
    with SHA-256
    `97939aea1d4c3048472974b001fefa11fb3872b3538264db545c2d4d743f17a9`.
    Cross/native runtime pass 292/292, and all six repeated full QEMU profiles
    pass.  Real hardware validation of this corrected clean-PCC image passed
    on 2026-07-11 with `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and
    the final `N64_PCC_DEBUG_RC_END` marker.
  - [x] D3 enables the existing `param_32bit` compiler-`TEMP` path for
    optimized, non-variadic MIPS32R2 functions.  Ordinary 32-bit integer and
    pointer register parameters no longer require incoming frame stores and
    repeated loads.  Address-taken parameters still materialize private local
    slots; structures, 64-bit values, floating-point values, and varargs keep
    their established paths.
  - MIPS32R2 Linpack changes from `2541/2433/108/768/293/79194` to
    `2473/2367/106/558/276/71207`
    instructions/non-nops/nops/loads/stores/assembly-bytes.  Cross and native
    runtime pass 292/292, and all six PCC-kernel/PCC-rootfs QEMU profiles pass.
    Permanent probes cover promoted, assigned, and address-taken parameters.
  - At D3, VR4300 remained byte-identical at
    `2465/2314/151/639/244/79719`.  Enabling the same path experimentally
    raised nops from 151 to 239 despite fewer loads, so it remained disabled
    until C5 removed the generic software padding before interlocked `mflo`.
  - [x] D4 enables the existing scalar/pointer TEMP path for optimized,
    non-variadic VR4300 functions (commit `711a9669`).  Explicit generic
    MIPS3/R4000 retains frame slots; address-taken, assigned, stack-passed,
    aggregate, 64-bit, floating-point, and variadic cases keep their proven
    behavior.
  - VR4300 C5 changes from `2443/2314/129/639/244/79609` to D4
    `2375/2248/127/429/227/71622`
    instructions/non-nops/nops/loads/stores/assembly-bytes.  Cross/native
    runtime pass 292/292, and all six PCC-kernel/PCC-rootfs QEMU profiles pass.
    The clean GCC-kernel/PCC-hard-float-a.out-userland N64 image is
    `sys/mips/n64/builds/20260711-milestone-d4-vr4300-param-promotion-gcc-kernel/pcc-debug.z64`,
    SHA-256
    `359f80f70fdf31bee92c8276e28e8c556da7a069a775b9157d8d06718f5ccabc`;
    physical VR4300 validation passed on 2026-07-11 with
    `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and
    `N64_PCC_DEBUG_RC_END`.
  - Preserve the existing o32 ABI fixes for struct returns, stack-passed
    64-bit arguments, varargs, and aggregate arguments.
  - Leaf `$ra` omission is now landed for both normal and
    `-fomit-frame-pointer` compilations.  Remaining work is broader
    outgoing-argument frame simplification and reducing cases that still force
    a real `$fp`.
  - Measure spills and load/store counts before and after; do not mix this
    with scheduler patches.
- [ ] Tune hard-float codegen for VR4300.
  - Replace unconditional FPU memory-operation `nop` padding with dependency
    scheduling.
  - [x] First FPU tuning substep (commit `1e37ef12`): run the existing exact
    `mtc1`; `nop`; `cvt.s.w`/`cvt.d.w`; FPU-load; `nop`; dependent binary-FPU
    peephole before the interlocked load-nop trim on VR4300 and MIPS32R2.  It
    emits `mtc1`; FPU-load; conversion; binary-FPU after proving that the load
    and conversion register sets do not overlap and that the binary operation
    reads the loaded register.
  - VR4300 User's Manual Table 7-14 gives integer-to-FP conversion five
    execution cycles plus one interlock cycle for an immediately dependent
    consumer.  The independent FPU load fills the `mtc1` transfer gap and one
    conversion cycle while preserving hardware dependency interlocks.
  - The rule excludes `mtc1` immediately after `mfhi`/`mflo`, preserving the
    specialized HI/LO gap fill.  Permanent VR4300/MIPS32R2 probes cover both
    forms, and generic MIPS3 retains its pre-existing late-peephole behavior.
  - VR4300 Linpack improves from `2471` instructions / `153` `nop` to `2469`
    instructions / `151` `nop`; non-`nop` instructions stay `2318`,
    loads/stores stay `643/244`, and bytes fall from `79929` to `79919`.
    MIPS32R2 Linpack stays `2545/108/772/293` because its current double
    constants use split `lwc1` loads rather than the exact single-load window.
  - Cross and native runtime regressions pass `292/292` with zero unexpected
    failures.  All six PCC-kernel/PCC-rootfs QEMU profiles pass with exactly
    one `PCC_SMOKE_ALL_OK`, zero failures, and `PCC_SMOKE_ALL_RC:0`, covering
    VR4300/MIPS32R2, big/little endian, and hard/soft userland.  Kernels use
    `-msoft-float -fomit-frame-pointer`.
  - N64 hardware/performance gates use a GCC kernel and PCC hard-float
    userland because a PCC kernel is currently too slow on real hardware.
    Keep PCC-kernel images only as separate correctness controls on Malta/QEMU
    unless PCC kernel testing is explicitly requested.
  - The clean GCC-kernel/PCC-userland hard-float default-`-mfix4300` N64 image
    used for hardware validation is
    `sys/mips/n64/builds/20260711-milestone-e1-fpu-conversion-schedule-gcc-kernel/pcc-debug.z64`,
    SHA-256
    `f452245340f0d0f4e4c5698f1103f306052449e756ce81c4c18299838d4b90f9`.
    Its build stamp is `.build-mode.gcc.1.0.0.1`.  The earlier PCC/PCC image
    remains preserved as a control artifact.  Real N64 hardware validation
    passed with `N64_PCC_DEBUG_END 0`, `N64_PCC_DEBUG_RUNNER_RC 0`, and the
    final `N64_PCC_DEBUG_RC_END` marker.  The supplied final marker had no
    numeric value, so none is inferred.  The E1 hardware gate is closed.
  - No FP multiply scheduling changed in E1; the `-mfix4300` /
    `-mno-fix4300` erratum switch remains unchanged.
  - [x] G2 keeps optimized non-variadic hard-float `double`/`long double`
    parameters passed through GPR pairs in compiler `TEMP`s on VR4300 and
    MIPS32R2.  The ABI register-pair store still happens first; generic
    MIPS3/R4000, soft-float, variadic functions, and leading F12/F14
    hard-float arguments retain their previous paths.
  - [x] G2 also recognizes the exact independent `mov.s`/`mov.d` between an
    FPU load and a binary FPU consumer.  It moves that register-only operation
    before the load, filling the load-use gap without reordering memory.  Two
    bounded late-scheduler passes let this compose with the existing C3 FPU
    window; the final VR4300 erratum repair still runs afterwards.
  - VR4300 Linpack changes from F1
    `2289/2172/117/431/226/70371` to G2
    `2291/2174/117/416/226/70437`
    instructions/non-nops/nops/loads/stores/assembly-bytes.  MIPS32R2
    hard-float is `2414/2311/103/534/272/70039`; soft-float remains
    byte-identical to F1 at `3695/3601/94/1016/554/99839` for each endian.
  - Alternating clean Malta64 GCC-kernel/PCC-userland A/B runs average
    12819.139 KFLOPS for F1 and 12942.396 for G2, a 0.96% G2 gain.  Cross
    regression and native VR4300 regression pass 294/294 runtime cases.  All
    six PCC-kernel/PCC-rootfs QEMU profiles pass the full
    `/root/pcc-smoke-all.sh`; kernels retain
    `-msoft-float -fomit-frame-pointer`.
  - G2 does not alter `-mfix4300` policy.  The workaround remains enabled by
    default and selectable with `-mfix4300` / `-mno-fix4300`.  Real VR4300
    hardware validation passes at 3188.274 PCC versus 4411.854 GCC KFLOPS in
    the stable 16-rep row, or 72.27% of GCC.  PCC improves 0.32% over F1 while
    the GCC control changes -0.11%, closing the G2 substep.
  - [x] G3 makes F1 constant specialization effective when selected scalar
    parameters are stack-passed.  Only the selected hard-float VR4300 or
    MIPS32R2 parameters are promoted to compiler `TEMP`s; the cloned body can
    then replace their initializers with the selected constants.  Other
    parameters keep their existing storage.
  - [x] Calls to that clone carry a machine-independent marker after prototype
    conversion.  MIPS omits marked o32 argument-register assignments and
    stack stores while retaining every ABI slot.  Generic MIPS3/R4000,
    soft-float, varargs, `-Os`, and `-ffreestanding` retain the G2 path.
  - VR4300 hard-float Linpack changes from G2
    `2291/2174/117/416/226/106/116/70437` to G3
    `2005/1910/95/362/202/82/108/62458`
    instructions/non-nops/nops/loads/stores/branches/jumps/bytes.  MIPS32R2
    hard-float is `2124/2043/81/470/244/82/108/62030`.  MIPS32R2 soft-float
    BE/LE and VR4300 soft-float are byte-identical to G2.
  - Alternating Malta64 GCC-kernel/PCC-userland A/B runs average 12923.604
    KFLOPS for G2 and 13368.590 for G3, a 3.44% gain; both alternating pairs
    favor G3.  Cross regression produces 294 runtime candidates and native
    VR4300 passes 294/294.  All six PCC-kernel/PCC-rootfs QEMU profiles pass
    the full smoke suite with kernels retaining
    `-msoft-float -fomit-frame-pointer`.
  - The six-argument specialization regression covers the stack path that G3
    changes.  The default `-mfix4300` policy and final erratum repair remain
    unchanged.  Physical N64 validation passes at 3186.275 PCC versus
    4411.809 GCC KFLOPS in the stable 16-rep row, or 72.22% of GCC.  PCC is
    0.06% below G2 while the GCC control changes -0.001%, so the 3.44% QEMU
    gain does not reproduce on hardware.  G3 is retained for the corrected
    specialization semantics and smaller code, not as a performance gain.
  - Keep helper calls only where they preserve required IEEE/NaN behavior; do
    not replace correctness-sensitive helper paths with raw FPU instructions
    unless the existing N64/Malta tests prove equivalence.

Target-independent or shared optimizer work:

- [ ] Implement integer strength reduction outside the MIPS backend where
  practical.
  - A narrow backend-only power-of-two cleanup for MIPS multiply, divide, and
    modulo is already in place, including unsigned and signed power-of-two
    divide, and MIPS32R2 now has an ISA-gated real `mul` rule; this item
    remains open for the general middle-end transform and non-power-of-two
    constant division/modulo.
  - Optimize multiply, divide, and modulo by constants using shifts/adds or
    magic-number sequences when this is valid for signedness and overflow
    rules.
  - Let MIPS provide costs so expensive VR4300 division is avoided, while other
    targets can opt in through their own costs.
  - Add focused tests for signed and unsigned edge cases before enabling it
    broadly.
- [ ] Improve inlining/static specialization without putting IPA logic in the
  backend.
  - PCC currently does not get GCC-like wins from automatic specialization of
    small `static` helpers such as the Linpack BLAS kernels.
  - Add a modest frontend/middle-end mechanism for small static leaf functions
    and constant arguments, with strict size limits.
  - Keep this as a separate phase from MIPS scheduling so performance gains are
    attributable.
  - [x] The first bounded static-specialization step is software-complete on
    2026-07-12.  Hosted `-O2`/`-O3` direct calls to a file-local function may
    select one clone when up to six arguments contain useful `-1`, `0`, or `1`
    constants.  Four-argument calls need one constant; five/six-argument calls
    need two.  Calls after the definition, variadic functions, `-O`, `-Os`,
    and freestanding/kernel compilations stay on the old path.
  - The frontend reuses the existing saved-inline body representation but does
    not enable the known-problematic general `-xinline` path.  Constant formal
    TEMP initializers are replaced in the copied body, then normal SSA folding
    removes dead increment branches.  At most one signature is cloned per
    function.  A generic direct call or address use keeps the original body;
    otherwise the clone replaces it, avoiding text growth for the Linpack
    case.
  - Linpack selects exactly seven helpers: `idamax`, `dscal_r`, `dscal_ur`,
    `daxpy_r`, `daxpy_ur`, `ddot_r`, and `ddot_ur`.  `dgefa` and `dgesl` remain
    generic.  VR4300 changes from E5
    `2375/2248/127/429/227/71622` to
    `2289/2172/117/431/226/70371`
    instructions/non-nops/nops/loads/stores/assembly-bytes.  MIPS32R2 hard
    float is `2412/2309/103/549/272/69823`; big- and little-endian output is
    identical.  Soft-float assembly also specializes the same seven integer
    increment parameters.
  - Permanent `ssaspecialize001` coverage checks the specialized direct call,
    a variable-argument generic fallback, and a function-pointer address use.
    Host probes additionally require one clone at `-O2` and no clone at `-Os`
    or `-ffreestanding` in all six CPU/endian/float profiles.
  - Cross regression compiles 331/334 cases with only the three established
    expected failures.  Native VR4300 regression passes 294/294 runtime cases,
    including `ssaspecialize001`, with zero unexpected compile/runtime
    failures.  All six PCC-kernel/PCC-rootfs QEMU profiles pass
    `/root/pcc-smoke-all.sh` with `PCC_SMOKE_ALL_FAILURES 0` and
    `PCC_SMOKE_ALL_RC:0`; kernels retain
    `-msoft-float -fomit-frame-pointer` and do not contain specialization
    clones.
- [ ] Add benchmark-oriented regression tests.
  - Keep normal PCC correctness gates mandatory after every optimization.
  - Add microbenchmarks for load-use scheduling, branch delay filling,
    integer divide by constants, `double` dot-product loops, `daxpy`, and
    call-heavy small static helpers.
  - Reject changes that improve Linpack by relying on undefined behavior or by
    regressing existing PCC tests.

Milestone order:

- [x] Milestone A: scripted baseline and assembly counters, no codegen change.
- [ ] Milestone B: remove provably redundant `nop` sequences from jump and
  memory templates.
  - Control-transfer, store-side, paired-load, omit-frame epilogue,
    call-delay-slot, callee-saved restore, helper-call delay-slot, and
    stack-argument memory `FUNARG`, narrow unsigned conversion, long-long
    compare delay-slot, conservative integer load-delay, VR4300-only
    shift-before-load delay fill, final move/GPR-ALU/signed-imm16-`li`
    load-delay/control-delay, non-RA GPR call-delay, and HI/LO independent
    `lw` delay-fill patches, VR4300 post-delay-slot load-delay trim,
    VR4300 integer and FPU load-use interlock cleanup, HI/LO post-`mflo`
    load-gap fill, HI/LO post-`mfhi`/`mflo` nop trim, and canonical
    zero-offset stack-argument emission are complete and green;
    MIPS32R2 one-instruction integer multiply and MIPS32R2 interlocked
    dependent-load `nop` cleanup are also complete and green.  MIPS32R2
    FPU compare-to-branch spacer cleanup, MIPS32R2 `mtc1`-to-convert spacer
    trim, load-gap label-only skip trim, and MIPS32R2 `jr $ra` frame-load
    return-delay fill are complete and green.  VR4300 strict `jr $ra`
    frame-load return-delay fill is complete and green.  Safe integer
    conditional branch delay-slot fill for simple GPR operations and strict FP
    stores is complete and green.
    The first binary FP compute dependency window is complete in C3;
    unary/conversion FP dependencies and broader load scheduling still need
    the compact late scheduler.
- [x] Milestone C: add conservative late delay-slot/load-use scheduling.
  - The first MIPS32R2 load-use window is complete in `11f66467`, and the
    branch-delay-across-load window is complete in `668a7d88`.  The first
    binary FPU dependency window is complete in `0331d3cf`; C4 adds the
    bounded HI/LO window in `3c2092b7`.  C5 removes redundant VR4300 multiply
    padding through hardware MCI rules in `af5be45d`; software/QEMU and
    physical N64 gates pass.  General memory scheduling,
    unary/conversion FP latency, and alias modelling are deferred until a
    measured workload justifies them.
- [x] Milestone D: reduce `$fp` and stack traffic in MIPS call/frame lowering.
  - MIPS call-argument liveness and leaf `$ra` omission, including the
    omit-FP/kernel path, are complete and green.  Omit-FP callee-save slot
    trimming is also complete and green.  D1 fixed register-only call areas
    are complete in `34c0b9d8`; D2 fixed scalar stack-argument areas are
    software-complete in `194d264e`/`5aa462ed`.  Clean N64 build and weak-link
    fixes are in `776e41af`; all six QEMU profiles and the corrected clean-PCC
    real-N64 hardware gate pass.
  - D3 promotes ordinary MIPS32R2 scalar/pointer register parameters into
    compiler `TEMP`s at `-O`, removing 210 Linpack loads and 17 stores.  It is
    extended to VR4300 by D4 after C5 removed the artificial multiply padding;
    VR4300 also removes 210 loads and 17 stores.  All cross/native and
    six-profile QEMU gates pass; the D4 physical N64 gate passes.
- [x] Milestone E: target-independent strength reduction with MIPS costs.
  - The backend-only MIPS power-of-two multiply/divide/modulo special cases
    including signed power-of-two divide, plus MIPS32R2 ISA-gated multiply,
    are complete and green.  More general loop forms remain deliberately
    deferred; E5 supplies the measured canonical SSA transform needed for the
    scoped milestone without adding a general loop optimizer.
  - The first machine-independent SSA substep is complete in `a0803d4b`.
    Exact repeated integer `MUL` subtrees over immutable SSA names are
    materialized once within a basic block.  Calls and asm delimit regions;
    memory accesses do not invalidate scalar SSA values.  Linpack drops 11
    static multiply sites in both VR4300 and MIPS32R2 assembly.  Cross/native
    regression and all six PCC-kernel/PCC-rootfs QEMU profiles pass.
    Real N64 validation also passes, but the stable PCC result is 3094.330
    KFLOPS versus GCC at 4411.954 KFLOPS (70.14%).  The -0.20% change from E3
    is measurement noise, so local multiply CSE improves code size but does
    not move the hardware performance ratio.
  - This local CSE is not loop induction-variable strength reduction.  The E5
    substep below separately derives a loop-carried scale from a canonical SSA
    CFG while rejecting ambiguous control flow and variable induction steps.
  - The first loop induction-variable strength reduction is software-complete
    in `ba59488e`.  A canonical SSA loop with one entry, one latch, a `+1` or
    `-1` induction step, and a dominating invariant stride replaces
    `index * stride` with a scaled phi and one add/subtract per iteration.
    Only one scale is introduced per induction phi to limit live-range growth.
    A target cost hook enables it for MIPS III/VR4300 HI/LO multiplication and
    leaves MIPS32R2 `mul` unchanged after QEMU measured a register-pressure
    regression from applying the transform there.
  - VR4300 Linpack drops from 48 to 23 integer multiply instructions.  All six
    host and full PCC-kernel/PCC-rootfs QEMU profiles pass, as do 293/293
    native VR4300 runtime regressions.  Real N64 validation passes at 3163.024
    KFLOPS versus GCC at 4412.343 KFLOPS (71.69%), a measured 2.22% PCC gain
    over E4.  This closes the scoped Milestone E hardware gate.
- [x] Milestone F: small static inlining/specialization outside the backend.
  - [x] F1 bounded constant-argument static cloning is software-complete.  It
    is a frontend/SSA feature, not MIPS backend IPA, and is limited to hosted
    `-O2+`, one clone per pre-definition static callee, and small control
    constants.  Cross/native regression and all six QEMU smoke profiles pass.
  - [x] The clean GCC-kernel/PCC-hard-float-a.out N64 comparison image is
    `sys/mips/n64/builds/20260712-milestone-f1-static-specialization-gcc-kernel/pcc-debug.z64`,
    SHA-256
    `30be99a022c0c10e3e2eb64b914071ba09ee252b49f095e00fdfc06eefcfb685`.
    It carries `.build-mode.gcc.1.0.0.1`, default `-mfix4300`, and separate
    GCC- and PCC-built Linpack runtimes.
  - [x] Physical VR4300 validation passes.  GCC/PCC Linpack report zero status;
    `N64_PCC_DEBUG_END` and `N64_PCC_DEBUG_RUNNER_RC` are zero, and the final
    `N64_PCC_DEBUG_RC_END` marker is present.  The stable 16-rep result is
    3178.200 PCC versus 4416.677 GCC KFLOPS, or 71.96%.  PCC improves 0.48%
    over E5 while GCC changes 0.10%, closing the scoped milestone.
- [ ] Milestone G: repeat Malta, Malta64/R4000, and real N64 benchmark gates;
  decide whether further work belongs in PCC or in source-level hot paths.
  - [x] G2 software gate: hard-float GPR-passed FP parameter promotion plus
    the exact FPU load/move gap fill pass host, cross, native, strict A/B, and
    all six full QEMU profiles.
  - [x] The clean G2 GCC-kernel/PCC-hard-float-a.out-userland comparison image
    is
    `sys/mips/n64/builds/20260712-milestone-g2-fpu-param-temp-gcc-kernel/pcc-debug.z64`,
    SHA-256
    `9106cbd1b3cac395d35d4c243815a42426186199c19f73a58bc89d417abeb854`.
  - [x] Physical N64 validation passes.  Both Linpack binaries return zero and
    report 15-digit precision; `N64_PCC_DEBUG_END` and
    `N64_PCC_DEBUG_RUNNER_RC` are zero.  The terminal
    `N64_PCC_DEBUG_RC_END` marker is present without a numeric value.  The
    stable 16-rep result is 3188.274 PCC versus 4411.854 GCC KFLOPS, or
    72.27%.  This closes G2; Milestone G remains open because the medium-term
    75-85% target has not yet been reached.
  - [x] G3 software gate: partial static specialization now handles selected
    hard-float stack parameters and omits their dead call-side values while
    preserving o32 slots.  Strict A/B improves 3.44%; cross/native regression
    and all six full QEMU profiles pass.
  - [x] The clean G3 GCC-kernel/PCC-hard-float-a.out-userland comparison image
    is
    `sys/mips/n64/builds/20260712-milestone-g3-stack-specialization-gcc-kernel/pcc-debug.z64`,
    SHA-256
    `87ca5dce22afd995da205704126bf573d389224503f29f9b1e5a439c3c4fe748`.
  - [x] Physical N64 validation passes.  Both Linpack binaries return zero and
    report 15-digit precision; `N64_PCC_DEBUG_END` and
    `N64_PCC_DEBUG_RUNNER_RC` are zero.  The terminal
    `N64_PCC_DEBUG_RC_END` marker is present without a numeric value.  The
    stable 16-rep result is 3186.275 PCC versus 4411.809 GCC KFLOPS, or
    72.22%.  This is statistically neutral against G2 and closes the scoped
    G3 correctness/code-size gate, while Milestone G performance work remains
    open.
  - [x] G4 software gate: the existing local SSA scale CSE also materializes
    repeated exact constant shifts on hard-float VR4300 and MIPS32R2.  VR4300
    Linpack removes 58 instructions and MIPS32R2 removes 37 with no load,
    store, nop, or control-flow growth.  Soft-float remains byte-identical to
    G3.  Strict Malta64 A/B improves 0.51%; cross/native regression and all
    six full PCC-kernel/PCC-rootfs QEMU profiles pass.
  - [x] Build the clean G4 GCC-kernel/PCC-hard-float-a.out-userland image:
    `sys/mips/n64/builds/20260712-milestone-g4-constant-shift-cse-gcc-kernel/pcc-debug.z64`,
    SHA-256
    `23a82ec406f916b4f040cb3c14281368901afe21784cfa74fca8af542123fcba`.
  - [ ] Physically validate the G4 image before crediting the small QEMU result
    as a VR4300 performance gain.
  - [ ] After the G4 hardware result, inspect remaining Linpack address and
    loop overhead before selecting another narrowly scoped transform.

Success criteria:

- [x] Short term: reduce PCC Linpack `nop` count and text size materially while
  keeping all PCC correctness gates green.
- [ ] Medium term: reach roughly 75-85% of GCC performance on real N64 with
  backend-only and ABI/scheduler work.
- [ ] Stretch target: reach 90-100% of GCC on representative workloads after
  adding small target-independent inlining/specialization and strength
  reduction, without making MIPS backend maintenance substantially harder.

## Phase 6: Cleanup and Commit Gates

- [x] Remove temporary build-only hacks.
  - The normal PCC rootfs no longer uses the diagnostic shell-stub image,
    explicit `/var` mount workaround, temporary `-DCROSS_COMPILING`, or N64
    USBNet no-op compatibility symbols.
  - `N64_DEBUG_UART_ONLY=1` remains as an intentional diagnostic build mode.
- [x] Keep generated build artifacts out of git unless they are source inputs
  already tracked by the project.
- [x] Update `docs/PCC_PORTING_NOTES.md` with the final target details.
- [x] Run `git diff --check` outside imported upstream vendor whitespace where
  applicable.
- [x] Commit each stable milestone separately.
  - This documentation/build-artifact cleanup is kept as its own milestone
    after the rootfs layout and ASCII manpage commits.
