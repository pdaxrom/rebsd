/*
 * Keep the prebuilt GCC and on-target PCC tests on one canonical source.
 * The latter is installed in /root by the MIPS root filesystem manifest.
 */
#include "../../sys/mips/rootfs/root/off64-smoke.c"
