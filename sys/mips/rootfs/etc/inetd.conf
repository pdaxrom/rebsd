#
# ReBSD/MIPS inetd configuration.
#
# Plain TELNET is intentionally disabled by default.  Enable only on trusted
# lab networks.  For the encrypted lab mode, run telnetd manually with -K or
# add a private service line that passes "-i -K key".
#
#telnet stream tcp nowait root /usr/libexec/telnetd telnetd -i
