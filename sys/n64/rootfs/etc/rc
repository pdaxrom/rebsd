#!/bin/sh
HOME=/; export HOME
PATH=/bin:/sbin; export PATH

# N64 root is a read-only cartridge romdisk. Keep first boot quiet and avoid
# fsck/mount/update/cron until writable filesystems are available.
exit 0
