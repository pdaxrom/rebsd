#
# Placeholder kconfig template for the experimental i686 port.
# The early boot milestone, generic allocator, pmap, vmspace, copy I/O and
# u-area allocator and context switch are built directly by sys/i386/Makefile.
# The complete machine-independent file list will be connected after the
# process MD gate.
#
